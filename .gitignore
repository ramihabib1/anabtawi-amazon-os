# Secrets — never commit
.env
engine/.env
**/.env

# Python
engine/.venv/
.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.ruff_cache/

# Logs
logs/
engine/logs/

# Cached DataDoe exports — regenerable, and the raw daily ones carry the seller UUID
# + buyer geography. Keep the folder, don't track the contents.
data/*.csv

# Live cockpit — regenerated each run from state/inbox/index.json (the source of truth).
deliverables/dashboard.html

# OS
.DS_Store
