---
phase: 08-reversible-write-apply-spine-stop-the-bleed
plan: 03
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - engine/src/habibos/apply.py
  - engine/scripts/apply_action.py
  - engine/tests/test_apply.py
autonomous: true
requirements: [WRITE-03, WRITE-06, WRITE-07, EXEC-01]

must_haves:
  truths:
    - "A reversible write runs end-to-end through the spine: build → denylist gate → margin gate (auto-pass spend-down) → magnitude cap → ledger lookup → FIND-before-write → dryRun classify → apply (dryRun:false → actionId) → poll loop to terminal → FIND-echo confirm → ledger write + decisions.md/brain log."
    - "The spine short-circuits on the FIRST refusal at every step (denylist, gate, cap, ledger-hit, find-miss, dryRun-stop, poll-error) — mirroring gate.evaluate's branch-order discipline; a dryRun STOP never lets a dryRun:false fire."
    - "The spine sets delta_spend ≤ 0 for pause/negative/bid-down so the margin gate auto-passes (the spine, not the operator, sets the sign for reversible actions)."
    - "The poll loop drives datadoe.poll_action_status + lifecycle.classify_poll to terminal; after 20 attempts without terminal it surfaces unresolved_in_flight carrying the actionId (resumable), never assumes success."
    - "Every applied write (including standing-approved auto actions) appends a dated decisions.md line + a brain/raw note carrying what/why/expected-effect/actionId — autonomy ≠ silence."
    - "The first apply of the phase requires the one-time canary confirm (--confirm-canary writes a data/ marker); subsequent reversible moves auto-apply."
    - "A 412/disabled or ERROR poll surfaces a clean typed refusal (action_type_disabled / action_error) and does NOT retry."
  artifacts:
    - path: "engine/src/habibos/apply.py"
      provides: "The reversible-write apply spine"
      exports: ["apply", "build_payload"]
    - path: "engine/scripts/apply_action.py"
      provides: "Thin argv→apply→JSON CLI mirroring gate_action.py, with --confirm-canary"
      contains: "logged_call"
  key_links:
    - from: "engine/src/habibos/apply.py"
      to: "engine/src/habibos/datadoe.py"
      via: "poll_action_status drives the in-flight→terminal loop"
      pattern: "poll_action_status"
    - from: "engine/src/habibos/apply.py"
      to: "state/decisions.md"
      via: "append one dated line per applied write (D-11)"
      pattern: "decisions"
    - from: "engine/scripts/apply_action.py"
      to: "engine/src/habibos/apply.py"
      via: "argv → apply.apply → asdict → stdout"
      pattern: "apply.apply"
---

<objective>
Write the apply spine — the linear, typed orchestration twin of gate.evaluate that threads a
ProposedAction through every gate to a logged, idempotent reversible write — plus the thin CLI
with the canary one-time confirm and the decisions.md/brain log seam. GREEN test_apply.

Purpose: This is the load-bearing new orchestration (RESEARCH State-of-the-Art: "the full async
write loop finally runs end-to-end"). It reuses every Phase 6/7 primitive verbatim
(classify_dryrun, classify_poll, poll_action_status, find_cache.lookup/write, gate.evaluate,
logged_call) and the Plan-02 gates (denylist, ledger, magnitude). Python never opens a socket —
the agent supplies the dryRun response, the actionId, each poll response, and the FIND echo.
Output: apply.py, apply_action.py, GREEN test_apply.py.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-reversible-write-apply-spine-stop-the-bleed/08-RESEARCH.md
@.planning/phases/08-reversible-write-apply-spine-stop-the-bleed/08-PATTERNS.md
@.planning/phases/08-reversible-write-apply-spine-stop-the-bleed/08-SCHEMA-PIN.md
@CLAUDE.md

<interfaces>
<!-- Everything the spine wires — exact signatures, no codebase exploration needed. -->

From engine/src/habibos/lifecycle.py:
```python
def classify_dryrun(resp: dict) -> Proceed | Refusal      # Refusal code="dryrun_invalid"
def classify_poll(resp: dict) -> PollOutcome | Refusal    # codes: action_error | unknown_status
```
From engine/src/habibos/datadoe.py:
```python
def poll_action_status(get_status) -> str   # tenacity 20-attempt loop, terminal on ACTION_TERMINAL
ACTION_TERMINAL = {COMPLETED, PARTIALLY_COMPLETED, COMPLETED_WITH_ISSUES, ERROR, CANCELED, BLOCKED_*}
```
From engine/src/habibos/find_cache.py:
```python
def lookup(entity_type, ad_product, entity_id) -> dict | Refusal   # miss find_cache_miss; A2 stale_find_cache (Plan 02)
def write(entity_type, ad_product, find_result) -> Path
```
From engine/src/habibos/gate.py:
```python
def evaluate(frame, action, catalog_path, marketplace=None) -> GateVerdict | GateRefusal
# spend-down (not is_spend_increasing) → GateVerdict(auto_pass=True), NO frame/catalog read needed
```
From engine/src/habibos/denylist.py (Plan 02):
```python
def check(action, owned_path) -> None | GateRefusal       # own_asin_denied
def extract_target_asins(action) -> list[str]
```
From engine/src/habibos/ledger.py (Plan 02):
```python
def action_hash(entity_type, entity_id, action_type, params) -> str
def lookup(entity_type, entity_id, action_type, params) -> None | LedgerHit | InFlight   # carry action_id
def append(record: dict) -> None
```
From engine/src/habibos/magnitude.py (Plan 02):
```python
def check(action, marketplace="CA") -> None | GateRefusal  # over_cap; None when cap absent (permissive)
```
From engine/src/habibos/result.py (Plan 01 extended):
```python
ProposedAction(sku, action_type, delta_spend, marketplace="CA", entity_type=None, entity_id=None, params={})
AppliedResult(action_id, status, hash, applied_at, marketplace="CA")
GateRefusal(reason, code, breached_value=None, ceiling=None, source=None)
Refusal(reason, code, export_id=None)
```
</interfaces>

<cli_and_log_analogs>
CLI template (thin, logged_call-wrapped, status-from-isinstance, repo-root default paths):
@engine/scripts/gate_action.py
decisions.md append seam to clone (one dated pipe-delimited line):
@engine/scripts/inbox.py
The decisions.md line format (existing):
@state/decisions.md
The RED contract this plan GREENs:
@engine/tests/test_apply.py
</cli_and_log_analogs>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: apply.py — the spine (build → gates → ledger → FIND → dryRun → apply → poll → echo)</name>
  <files>engine/src/habibos/apply.py, engine/tests/test_apply.py</files>
  <read_first>
    - engine/tests/test_apply.py (the RED contract from Plan 01 — the spine/poll/dryRun-stop/canary/log/cap/disabled test names to GREEN)
    - engine/src/habibos/gate.py (the `evaluate` branch-order short-circuit discipline to replicate; the spend-down auto-pass at lines 150-156)
    - engine/src/habibos/lifecycle.py (classify_dryrun / classify_poll — drive, do not rewrite)
    - engine/src/habibos/datadoe.py (poll_action_status — the loop primitive to drive to terminal, lines 65-82)
    - engine/src/habibos/find_cache.py, denylist.py, ledger.py, magnitude.py (the gates the spine threads, Plan 02)
    - .planning/phases/08-reversible-write-apply-spine-stop-the-bleed/08-SCHEMA-PIN.md (the per-action-type build_payload field map)
  </read_first>
  <action>
    Create `engine/src/habibos/apply.py`. Implement `build_payload(action) -> dict` per
    action_type against the 08-SCHEMA-PIN field map (pause → campaign-state, negative →
    negative-target create, bid-down → target bid); for pause it sends only mutable fields and
    does NOT echo `adProduct`. Implement the spine `apply(action, *, dryrun_resp, start_apply,
    get_status, find_echo, owned_path, catalog_path=None, gate_frame=None, confirm_canary=False)
    -> AppliedResult | Refusal | GateRefusal` where the keyword callables are the agent-supplied
    MCP results/closures (Python opens NO socket). Branch order, short-circuit on the FIRST
    refusal (mirror gate.evaluate):
      1. denylist.check(action, owned_path) → GateRefusal(own_asin_denied) STOP.
      2. The spine sets delta_spend ≤ 0 for pause/negative/bid-down (Open Q3) then
         gate.evaluate(...) → auto_pass for spend-down; a GateRefusal STOPs.
      3. magnitude.check(action) → GateRefusal(over_cap) STOP (None when seeded permissive).
      4. ledger.lookup(entity_type, entity_id, action_type, normalized_params): LedgerHit →
         return a no-op AppliedResult on the recorded actionId (never re-fire start); InFlight →
         SKIP start, resume at poll on its action_id (D-10).
      5. CANARY: if no canary marker file (data/phase8_canary_confirmed) AND confirm_canary is
         False → Refusal(canary_unconfirmed) STOP; if confirm_canary True → proceed then write
         the marker after success; if marker present → auto (D-08/D-09).
      6. find_cache.lookup(entity_type, ad_product, entity_id) → MISS Refusal(find_cache_miss) /
         stale Refusal(stale_find_cache) STOP.
      7. classify_dryrun(dryrun_resp) → Refusal(dryrun_invalid) STOPs before ANY dryRun:false.
      8. call start_apply() (the agent's actions_start dryRun:false) → actionId. A 412/disabled
         response surfaces Refusal(action_type_disabled) and does NOT retry.
      9. poll: datadoe.poll_action_status(get_status) driving classify_poll per response to
         terminal; ERROR/CANCELED → Refusal(action_error); 20 attempts w/o terminal →
         Refusal(unresolved_in_flight, carrying actionId).
      10. find_echo confirm: re-lookup the mutated entity and compare the action-type's mutated
          field (keyed off 08-SCHEMA-PIN) against the intended value build_payload set, per action type —
          pause → entity `state == "PAUSED"`; bid-down → target `bid == new_bid` (the payload bid);
          negative-keyword → the negated target is PRESENT on the campaign. The echo-confirm asserts
          field-equality between the echoed entity and build_payload's intended value; ANY mismatch
          (or absent expected field) → typed `Refusal(find_echo_mismatch)` STOP — never treat an
          unconfirmed write as applied (SC-1 / WRITE-03).
      11. ledger.append(record) + log_decision(action, actionId) (Task 2 seam) → AppliedResult.
    Wrap every agent-handed op in logged_call (NEVER pass the seller UUID). Use typed
    Refusal/GateRefusal at every edge — never a bare None/raise.
  </action>
  <acceptance_criteria>
    - `apply.py` exports `apply` and `build_payload`; the pause `build_payload` output has no `adProduct` key.
    - `cd engine && uv run pytest tests/test_apply.py::test_reversible_write_end_to_end tests/test_apply.py::test_dryrun_refusal_blocks_apply tests/test_apply.py::test_poll_loop_to_terminal tests/test_apply.py::test_disabled_action_refuses -x` exits 0.
    - `cd engine && uv run pytest tests/test_apply.py::test_magnitude_cap_refuses_when_set tests/test_apply.py::test_magnitude_cap_seeded_permissive -x` exits 0.
    - A dryRun STOP path never calls `start_apply` (asserted in test_dryrun_refusal_blocks_apply).
    - The FIND-echo confirm compares the action-type's mutated field against build_payload's intended value per 08-SCHEMA-PIN (pause→`state=="PAUSED"`, bid-down→target `bid==new_bid`, negative→negated target present); a field mismatch returns `Refusal(find_echo_mismatch)` and the write is NOT recorded as applied (asserted in `test_apply.py::test_find_echo_mismatch`).
  </acceptance_criteria>
  <verify>
    <automated>cd engine && uv run pytest tests/test_apply.py -k "not logs and not canary" -x</automated>
  </verify>
  <done>The spine threads all gates in branch order, short-circuits on first refusal, drives the poll loop to terminal, never opens a socket; the named test_apply contracts GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: apply_action.py CLI + canary marker + decisions.md/brain log seam</name>
  <files>engine/scripts/apply_action.py, engine/src/habibos/apply.py</files>
  <read_first>
    - engine/tests/test_apply.py (test_canary_one_time_confirm + test_apply_logs_decision_and_brain — the contracts to GREEN)
    - engine/scripts/gate_action.py (the thin argv→evaluate→JSON template: argparse, logged_call, end_fields["status"] from isinstance(result, (Refusal,GateRefusal)), dataclasses.asdict, repo-root default paths)
    - engine/scripts/inbox.py (the `_log_decision` DECISIONS.open("a") append seam to clone)
    - state/decisions.md (the existing `YYYY-MM-DD | area | SKU/campaign | change | rationale | expected impact | result` line format)
  </read_first>
  <action>
    Create `engine/scripts/apply_action.py` mirroring `gate_action.py`: argparse the ProposedAction
    fields (`--sku --action-type --delta-spend --marketplace --entity-type --entity-id` + a way to
    pass params/the dryRun-resp/echo fixtures or the agent wiring), add the `--confirm-canary`
    flag (RESEARCH Pattern 5): marker absent + flag not passed → `canary_unconfirmed` refusal;
    flag passed → proceed then write `data/phase8_canary_confirmed`; marker present → auto-apply
    (D-08/D-09). Wrap the `apply.apply(...)` call in `habibos_logging.logged_call("apply_action",
    marketplace=...)`; set `end_fields["status"] = "refused" if isinstance(result, (Refusal,
    GateRefusal)) else "applied"` so a refusal can NEVER serialize as a pass; `json.dump(
    dataclasses.asdict(result), sys.stdout)`. NEVER pass the seller UUID into logged_call.
    In `apply.py` (or a small log-seam function the CLI/spine calls), implement
    `log_decision(action, action_id, result) -> None` (D-11, WRITE-06): clone inbox._log_decision
    to append ONE dated pipe-delimited line to `state/decisions.md`
    (`YYYY-MM-DD | ppc | [<SKU>] <entity> (<campaignId>) | <change> via apply spine (actionId
    <ACT>) | <why> | <expected impact> | <terminal status>`) AND write ONE `brain/raw/` note
    (what changed, why, expected effect, actionId). STANDING-APPROVED auto actions log too
    (autonomy ≠ silence). Repo-root-relative paths; tests monkeypatch them to tmp_path. NEVER
    inline the seller UUID in either the decisions line or the brain note.
  </action>
  <acceptance_criteria>
    - `apply_action.py` has a `--confirm-canary` flag; invoking the CLI with the marker absent and the flag NOT passed yields JSON `code == "canary_unconfirmed"`.
    - After a `--confirm-canary` apply succeeds, the `data/phase8_canary_confirmed` marker exists and a subsequent apply does not require the flag.
    - `log_decision` appends one line to decisions.md AND writes one brain/raw note, both carrying the actionId; neither contains the seller UUID.
    - `cd engine && uv run pytest tests/test_apply.py::test_canary_one_time_confirm tests/test_apply.py::test_apply_logs_decision_and_brain -x` exits 0.
    - `end_fields["status"]` is derived from `isinstance(result, (Refusal, GateRefusal))` (a refusal cannot serialize as "applied").
  </acceptance_criteria>
  <verify>
    <automated>cd engine && uv run pytest tests/test_apply.py -k "canary or logs" -x && grep -qE "confirm[-_]canary" engine/scripts/apply_action.py</automated>
  </verify>
  <done>Thin CLI with the one-time canary marker + the decisions.md/brain log seam; standing-approved actions still log; no seller UUID; the two contract tests GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent (MCP) → spine | The agent supplies dryRun response, actionId, poll responses, FIND echo; the spine trusts only typed-validated shapes (classify_*, find_cache). |
| spine → live Amazon account | `start_apply()` is the one real account mutation — gated by denylist, gate, cap, ledger, canary, dryRun before it can fire. |
| spine → state/decisions.md + brain/raw | Operator-facing decision record; must carry the actionId and never the seller UUID. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | Elevation of Privilege | a dryRun STOP still firing the real write | mitigate | Branch order short-circuits: classify_dryrun Refusal STOPs before any `start_apply`; test_dryrun_refusal_blocks_apply asserts start_apply is never called. |
| T-08-11 | Tampering | canary-confirm bypass (first apply auto-fires) | mitigate | The spine refuses `canary_unconfirmed` when the marker is absent and `--confirm-canary` not passed; marker written only after success (D-08/D-09). |
| T-08-12 | Tampering | applying against a disabled action type (412) treated as a bug/retry | mitigate | A 412/disabled response → `action_type_disabled` typed refusal, no retry (RESEARCH Pitfall 4). |
| T-08-13 | Tampering (false reconcile) | treating a stale export as write confirmation | mitigate | Reconcile ONLY via poll-to-terminal + FIND-echo; classify_poll refuses an export status as unknown_status (WRITE-05 reuse). |
| T-08-14 | Repudiation | a standing-approved auto write leaving no record | mitigate | log_decision appends decisions.md + brain note for EVERY applied write incl. auto (D-11, WRITE-06). |
| T-08-15 | Information Disclosure | seller UUID into logged_call / decisions.md / brain note | mitigate | logged_call carries only op+marketplace; decisions/brain lines reviewed to exclude the UUID (CLAUDE.md hard rule 5). |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH audit N/A). |
</threat_model>

<verification>
- `cd engine && uv run pytest tests/test_apply.py` fully GREEN (spine, poll loop, dryRun-stop, canary, log, cap, disabled-action).
- `cd engine && uv run pytest` full suite green (no Phase 6/7 regression).
- The pause build_payload omits `adProduct`; a dryRun STOP never fires start_apply.
</verification>

<success_criteria>
- The reversible-write spine runs end-to-end and short-circuits on the first refusal at every gate.
- The canary one-time confirm gates the first apply; subsequent reversible moves auto-apply.
- Every applied write (incl. auto) logs a decisions.md line + brain note with the actionId; no seller UUID.
</success_criteria>

<output>
Create `.planning/phases/08-reversible-write-apply-spine-stop-the-bleed/08-03-SUMMARY.md` when done.
</output>
