---
phase: 08-reversible-write-apply-spine-stop-the-bleed
plan: 02
type: execute
wave: 1
depends_on: ["08-01"]
files_modified:
  - engine/src/habibos/ledger.py
  - engine/src/habibos/denylist.py
  - engine/src/habibos/magnitude.py
  - engine/config/owned_asins.toml
  - engine/config/thresholds.toml
  - engine/src/habibos/find_cache.py
autonomous: true
requirements: [WRITE-07, EXEC-01]

must_haves:
  truths:
    - "Re-running an already-COMPLETED action is a no-op: a content hash over {entity_type + entity_id + action_type + normalized_params} (NOT date) matches a COMPLETED ledger entry and returns a LedgerHit instead of re-firing actions_start."
    - "An in-flight actionId in the ledger resumes at poll on its action_id, never re-fires the start."
    - "A payload that targets an owned ASIN is a hard GateRefusal(own_asin_denied) before BUILD→DRYRUN; a pause/bid-down with no target ASIN passes the denylist cleanly."
    - "The owned-ASIN set is read from a dedicated engine/config/owned_asins.toml (the full ~25+ owned set incl. B07TV972JT), NOT from sku_catalog.toml's COGS-gated rows."
    - "The magnitude-cap mechanism exists and refuses (over_cap, never clamps) when a bounded-% is configured, but is seeded permissive (absent key → pass at any magnitude) so live reversible behavior matches D-01."
    - "A FIND cache file older than the documented staleness window is rejected (stale_find_cache) for a write-bearing lookup."
  artifacts:
    - path: "engine/src/habibos/ledger.py"
      provides: "Content-hash idempotency ledger over data/actions_ledger.jsonl"
      exports: ["action_hash", "lookup", "append"]
    - path: "engine/src/habibos/denylist.py"
      provides: "Own-ASIN pre-dryRun refuse-gate"
      contains: "own_asin_denied"
    - path: "engine/src/habibos/magnitude.py"
      provides: "Seeded-permissive bounded-% magnitude cap"
      contains: "over_cap"
    - path: "engine/config/owned_asins.toml"
      provides: "The dedicated authoritative owned-ASIN denylist source"
      contains: "B07TV972JT"
  key_links:
    - from: "engine/src/habibos/denylist.py"
      to: "engine/config/owned_asins.toml"
      via: "tomllib read of the owned-ASIN set"
      pattern: "tomllib|owned_asins"
    - from: "engine/src/habibos/ledger.py"
      to: "data/actions_ledger.jsonl"
      via: "append-only jsonl content-hash store"
      pattern: "actions_ledger"
    - from: "engine/src/habibos/magnitude.py"
      to: "engine/config/thresholds.toml"
      via: "max_reversible_pct_change dial, absent → permissive"
      pattern: "max_reversible_pct_change"
---

<objective>
Build the three pre-dryRun engine pieces the spine orchestrates: the idempotency ledger, the
own-ASIN denylist gate, and the seeded-permissive magnitude cap — plus the dedicated owned-ASIN
config and the A2 FIND-cache staleness window. GREEN their RED tests from Plan 01.

Purpose: These are the genuinely-new, must-be-hand-written-and-tested pieces (RESEARCH "Key
insight": ~80% of Phase 8 is wiring, but the ledger hash, denylist extraction, and seeded cap
are the narrow new logic that gates a real account write). Each gets its own focused module +
test (mirroring gate.py/test_gate.py, find_cache.py/test_find_cache.py).
Output: ledger.py, denylist.py, magnitude.py, owned_asins.toml, a thresholds.toml dial, and the
find_cache staleness window — all GREEN.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-reversible-write-apply-spine-stop-the-bleed/08-RESEARCH.md
@.planning/phases/08-reversible-write-apply-spine-stop-the-bleed/08-PATTERNS.md
@.planning/phases/08-reversible-write-apply-spine-stop-the-bleed/08-SCHEMA-PIN.md
@.claude/skills/anabtawi-context/SKILL.md

<interfaces>
<!-- Analogs the executor clones — exact line anchors from PATTERNS.md. -->

From engine/src/habibos/find_cache.py (the DATA_DIR + lookup pattern to clone for ledger; the lookup to extend for A2):
```python
DATA_DIR = Path(__file__).resolve().parents[2] / "data"   # repo-root/data; tests monkeypatch
def lookup(entity_type, ad_product, entity_id) -> dict | Refusal  # newest-wins glob, miss → Refusal("…","find_cache_miss")
# dated filenames; sorted(glob, reverse=True) == date sort (the A2 window plugs in here)
```

From engine/src/habibos/gate.py (the tomllib catalog read + hard-refuse pattern to clone for denylist):
```python
def _load_catalog(catalog_path) -> dict:        # tomllib.load; missing file RAISES (no silent fallback)
    with Path(catalog_path).open("rb") as fh: data = tomllib.load(fh)
    return data.get("sku", {})
# refuse pattern: return GateRefusal("SKU not in catalog table", code="sku_not_in_catalog")
# spend-down branch returns GateVerdict(auto_pass=True) with NO read
```

From engine/src/habibos/thresholds.py (the missing→None dial pattern; for the cap, None means PERMISSIVE not refuse):
```python
def read(key: str, marketplace: str = "CA") -> float | None  # "" or absent → None
# CONFIG_PATH = parents[2]/"config"/"thresholds.toml"  (resolves to engine/config/thresholds.toml)
```

From engine/src/habibos/result.py (typed grammar — reuse; AppliedResult + new codes added in Plan 01):
```python
GateRefusal(reason, code, breached_value=None, ceiling=None, source=None)
Refusal(reason, code, export_id=None)
ProposedAction(sku, action_type, delta_spend, marketplace="CA", entity_type=None, entity_id=None, params={})
```
</interfaces>

<owned_asin_source>
<!-- Operator decision resolved this session (RESEARCH Open Q4): a SEPARATE dedicated source. -->
The full authoritative owned-ASIN set is the SKU→ASIN table in @.claude/skills/anabtawi-context/SKILL.md
(lines 36-60: ~25 ASINs incl. B0GKWQJNYR, B0FY6N6TRH, B0FXXN7HGB, B0FXXQHDHP, B0FY6MFJV5,
B0FY6M2LHX, B0FXX2QVF8, B0FSL5KQ9Q, plus the 16 catalog ASINs) PLUS the named private-label
exclusion B07TV972JT (the Alreef #1 assorted ASIN — owned, must be excluded from ALL SP/SB/SD
targeting per @brain/raw/2026-06-17_baklava-rebuild-research.md). The remnant SKUs with no ASIN
(ZK-4NDS etc.) contribute no ASIN row. Do NOT invent an ASIN; seed every ASIN that is
authoritative from that table + B07TV972JT, and if the full ~30 cannot be confirmed, seed what
IS authoritative and note the gap in the file header.
</owned_asin_source>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ledger.py — content-hash idempotency over data/actions_ledger.jsonl</name>
  <files>engine/src/habibos/ledger.py</files>
  <read_first>
    - engine/tests/test_ledger.py (the RED contract from Plan 01 — the exact function names + assertions to GREEN)
    - engine/src/habibos/find_cache.py (DATA_DIR pattern + newest-wins scan to clone)
    - engine/src/habibos/logging.py (the jsonl append seam, lines 26/31-39 — append-only, never edit past lines)
    - .planning/phases/08-reversible-write-apply-spine-stop-the-bleed/08-RESEARCH.md (Pattern 3 — record schema, normalized-params-per-action-type, lookup semantics)
    - .planning/phases/08-reversible-write-apply-spine-stop-the-bleed/08-SCHEMA-PIN.md (the pinned field names that define normalized_params identity)
  </read_first>
  <action>
    Create `engine/src/habibos/ledger.py` with `DATA_DIR = Path(__file__).resolve().parents[2]
    / "data"` (clone find_cache; tests monkeypatch it) and the ledger at `DATA_DIR /
    "actions_ledger.jsonl"`. Implement `action_hash(entity_type, entity_id, action_type, params)
    -> str` = `"sha256:" + sha256` over `json.dumps({entity_type, entity_id, action_type,
    normalized_params}, sort_keys=True, separators=(",",":"))` — identity is entity+action+params,
    NEVER date (D-10; a date defeats the no-op-on-rerun guarantee). Normalize params per the
    08-SCHEMA-PIN map: lowercase + strip keyword text, round bids to 2 dp, sort dict keys; document
    the normalization in the module docstring (it is load-bearing). Implement `lookup(entity_type,
    entity_id, action_type, params) -> None | LedgerHit | InFlight`: scan the jsonl newest-last;
    a matching hash with a COMPLETED status → `LedgerHit(action_id)` (caller no-ops, never
    re-fires start); a matching hash with an in-flight status (PENDING/IN_PROGRESS/WAITING_*) →
    `InFlight(action_id)` (caller resumes at poll); no entry → None (proceed). Implement
    `append(record: dict) -> None` appending one JSON object per line (the Pattern-3 record schema:
    hash, entity_type, entity_id, action_type, normalized_params, action_id, status, ad_product,
    applied_at, marketplace) — `DATA_DIR.mkdir(parents=True, exist_ok=True)`, open("a"), never edit
    past lines. Define `LedgerHit`/`InFlight` as small frozen dataclasses carrying `action_id`.
    NO socket — local file I/O only.
  </action>
  <acceptance_criteria>
    - `engine/src/habibos/ledger.py` exports `action_hash`, `lookup`, `append` (`grep -nE "def (action_hash|lookup|append)" engine/src/habibos/ledger.py`).
    - `action_hash(...)` over the same entity+action+params on two different dates returns an identical string (date excluded).
    - `cd engine && uv run pytest tests/test_ledger.py -x` exits 0 (all three contract tests GREEN).
  </acceptance_criteria>
  <verify>
    <automated>cd engine && uv run pytest tests/test_ledger.py -x</automated>
  </verify>
  <done>ledger.py GREENs test_ledger; hash excludes date; COMPLETED→no-op, in-flight→resume; append-only jsonl.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: denylist.py + owned_asins.toml — own-ASIN hard refuse-gate</name>
  <files>engine/src/habibos/denylist.py, engine/config/owned_asins.toml</files>
  <read_first>
    - engine/tests/test_denylist.py (the RED contract from Plan 01)
    - engine/src/habibos/gate.py (the `_load_catalog` tomllib read + the `GateRefusal(... code="sku_not_in_catalog")` hard-refuse pattern to clone — lines 104-113, 174-179)
    - engine/config/sku_catalog.toml (the [sku."…"] header-discipline + "never invent an ASIN" rules to clone verbatim)
    - .claude/skills/anabtawi-context/SKILL.md (lines 36-60 — the authoritative SKU→ASIN table that seeds the owned set)
    - .planning/phases/08-reversible-write-apply-spine-stop-the-bleed/08-RESEARCH.md (Pattern 4 + A7 — the ASIN-extraction-per-payload challenge; pause/bid-down carry no ASIN → pass cleanly)
    - .planning/phases/08-reversible-write-apply-spine-stop-the-bleed/08-SCHEMA-PIN.md (the target `expression`/`value` field where a target ASIN lives, pinned in Plan 01)
  </read_first>
  <action>
    Create `engine/config/owned_asins.toml` (the dedicated denylist source — operator decision,
    RESEARCH Open Q4). Clone `sku_catalog.toml`'s header discipline verbatim (never invent an
    ASIN; absent = surfaced; this is BROADER than the gate's 16-row COGS catalog and is
    deliberately uncoupled from COGS so Phase 7's catalog semantics stay clean). Seed every
    authoritative ASIN from the anabtawi-context SKU→ASIN table PLUS B07TV972JT (the Alreef
    private-label exclusion). Structure: a flat `owned_asins = [ "B0…", … ]` array (or `[asin."B0…"]`
    rows). If the full ~30 owned set cannot be confirmed, seed what IS authoritative and document
    the gap in the file header — do NOT invent.
    Create `engine/src/habibos/denylist.py`: `_load_owned(path) -> set[str]` (clone
    gate._load_catalog's tomllib read; missing file RAISES, no silent fallback). `extract_target_asins(action)
    -> list[str]` reads the target ASIN(s) from the payload using the field pinned in
    08-SCHEMA-PIN (a target `expression`/`value`); a pause/bid-down carries NO target ASIN →
    returns [] → passes cleanly (A7). `check(action, owned_path) -> None | GateRefusal`: if any
    extracted ASIN ∈ the owned set → `GateRefusal("payload targets an owned ASIN", code="own_asin_denied",
    source=<owned_asins.toml>)` (hard refusal, never warn-and-confirm, D-07); else None. NO socket.
  </action>
  <acceptance_criteria>
    - `engine/config/owned_asins.toml` exists, contains `B07TV972JT`, and contains ≥ the 16 catalog ASINs plus the additional anabtawi-context ASINs (`grep -c "B0" engine/config/owned_asins.toml` ≥ 20).
    - `denylist.py` reads owned ASINs from `owned_asins.toml` (the dedicated source), NOT from `sku_catalog.toml` for the owned set (`grep -q "owned_asins" engine/src/habibos/denylist.py`).
    - `cd engine && uv run pytest tests/test_denylist.py -x` exits 0 (own-ASIN refused; pause passes; owned-set-from-dedicated-source).
  </acceptance_criteria>
  <verify>
    <automated>cd engine && uv run pytest tests/test_denylist.py -x && grep -q "B07TV972JT" engine/config/owned_asins.toml && echo OK</automated>
  </verify>
  <done>owned_asins.toml seeded (incl. B07TV972JT, broader than the COGS catalog); denylist.py hard-refuses own-ASIN targeting pre-dryRun, passes pauses cleanly; test_denylist GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: magnitude.py (seeded-permissive cap) + thresholds.toml dial + find_cache A2 staleness window</name>
  <files>engine/src/habibos/magnitude.py, engine/config/thresholds.toml, engine/src/habibos/find_cache.py</files>
  <read_first>
    - engine/tests/test_magnitude_cap.py (the RED contract from Plan 01)
    - engine/tests/test_find_cache.py (the extended test_stale_cache_refused contract from Plan 01)
    - engine/src/habibos/gate.py (the strict-breach-never-clamp pattern, lines 226-233, to clone for over_cap)
    - engine/src/habibos/thresholds.py (the read→None dial; for the cap None means PERMISSIVE, document the asymmetry)
    - engine/src/habibos/find_cache.py (the lookup to extend with the staleness window)
    - engine/config/thresholds.toml (the [marketplace.CA] block + the commented-empty-key convention, e.g. days_of_cover_floor="")
  </read_first>
  <action>
    Create `engine/src/habibos/magnitude.py`: `check(action, marketplace="CA") -> None |
    GateRefusal`. Read `thresholds.read("max_reversible_pct_change", marketplace)`; if None
    (absent/empty) → PERMISSIVE, return None (pass at ANY magnitude — D-01/D-02 seeded permissive;
    document the asymmetry vs the gate floor where None means refuse). If a cap IS set and the
    action's bounded-% change (from action.params, e.g. the bid delta vs prior bid) exceeds it →
    `GateRefusal("reversible move exceeds the magnitude cap", code="over_cap", breached_value=<pct>,
    ceiling=<cap>)` — strict-breach, NEVER clamps to the limit (mirror gate.py:226-233 / GATE-04).
    Add the dial to `engine/config/thresholds.toml` under `[marketplace.CA]` as a commented,
    absent-by-default key `max_reversible_pct_change = ""` with a comment that empty = permissive
    (D-02; tightening later is a config edit, not code) — mirror the `days_of_cover_floor = ""`
    convention.
    Extend `engine/src/habibos/find_cache.py` with the A2 staleness window (Claude's discretion,
    documented dial): add a module constant `STALE_HOURS = 24` (or read `find_cache_staleness_hours`
    from thresholds, default 24) with a comment, and in `lookup` (or a thin wrapper for
    write-bearing lookups) reject a cache file whose date tail is older than the window →
    `Refusal("stale FIND cache — re-FIND required", code="stale_find_cache")`. Keep the change in
    the one tested place; do NOT silently hard-code without the documented dial.
  </action>
  <acceptance_criteria>
    - `magnitude.py` exports `check` and returns None when the cap key is absent (`max_reversible_pct_change` empty), and a `GateRefusal(code="over_cap")` (no clamp) when set and breached.
    - `engine/config/thresholds.toml` contains `max_reversible_pct_change` seeded empty with a "permissive" comment.
    - `find_cache.py` rejects an over-window cache file with `Refusal(code="stale_find_cache")`; the staleness window is a named, commented dial (not a bare literal).
    - `cd engine && uv run pytest tests/test_magnitude_cap.py tests/test_find_cache.py -x` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd engine && uv run pytest tests/test_magnitude_cap.py tests/test_find_cache.py -x && grep -q "max_reversible_pct_change" engine/config/thresholds.toml</automated>
  </verify>
  <done>Magnitude cap built + tested but seeded permissive (absent→pass); thresholds dial added; A2 staleness window enforced as a documented dial; both test files GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ProposedAction params → engine gates | The denylist + magnitude cap read untrusted params (target ASIN, bid %) before any real write. |
| owned_asins.toml → denylist | The authoritative owned-ASIN set; a missing/wrong entry could let an owned-ASIN target through. |
| data/actions_ledger.jsonl → idempotency | The ledger is the only thing preventing a double-apply on re-run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering | double-apply on re-run (repeated mutation / over-spend) | mitigate | Content-hash ledger keyed entity+action+params NOT date; COMPLETED hash → LedgerHit no-op; test_completed_hash_is_noop + test_hash_excludes_date enforce it (WRITE-07). |
| T-08-05 | Tampering (self-cannibalization) | advertising an owned ASIN on our own page | mitigate | denylist.py hard `GateRefusal(own_asin_denied)` pre-dryRun, owned set from the dedicated owned_asins.toml incl. B07TV972JT (EXEC-01, D-06/D-07). |
| T-08-06 | Tampering | idempotency-key collision via weak/dated identity | mitigate | sha256 over canonical sorted-key JSON with documented param normalization; date excluded; test_hash_excludes_date. |
| T-08-07 | Spoofing/Tampering | writing against a stale entity ID | mitigate | A2 staleness window in find_cache.lookup → `stale_find_cache` refusal for write-bearing lookups (RESEARCH Pitfall 3). |
| T-08-08 | Denial of (own) Service | a reversible move killing a winner | accept | Magnitude-cap mechanism built + tested but seeded permissive by explicit operator decision (D-01/D-02); tightening is a thresholds.toml dial, no code change. |
| T-08-09 | Information Disclosure | seller UUID in the ledger / config | mitigate | Ledger records carry only entity/action/actionId/marketplace; no seller UUID field; owned_asins.toml holds public ASINs only (CLAUDE.md hard rule 5). |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH audit N/A). |
</threat_model>

<verification>
- `cd engine && uv run pytest tests/test_ledger.py tests/test_denylist.py tests/test_magnitude_cap.py tests/test_find_cache.py` all GREEN.
- `cd engine && uv run pytest` full suite green (no regression to Phase 6/7).
- owned_asins.toml contains B07TV972JT + the broader owned set; the cap dial is seeded empty (permissive).
</verification>

<success_criteria>
- Idempotency ledger no-ops a COMPLETED re-run and resumes an in-flight actionId; hash excludes date.
- Own-ASIN denylist hard-refuses targeting an owned ASIN pre-dryRun from the dedicated source; passes pauses.
- Magnitude cap built + tested, seeded permissive; A2 staleness window enforced as a documented dial.
</success_criteria>

<output>
Create `.planning/phases/08-reversible-write-apply-spine-stop-the-bleed/08-02-SUMMARY.md` when done.
</output>
