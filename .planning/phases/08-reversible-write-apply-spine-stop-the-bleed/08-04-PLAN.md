---
phase: 08-reversible-write-apply-spine-stop-the-bleed
plan: 04
type: execute
wave: 3
depends_on: ["08-03"]
files_modified:
  - tasks/stop-the-bleed-batch.md
  - data/actions_ledger.jsonl
  - state/decisions.md
  - brain/raw/2026-06-23_stop-the-bleed-batch.md
autonomous: false
requirements: [WRITE-03, WRITE-06, EXEC-01]

must_haves:
  truths:
    - "Rami has enabled the reversible Ads action types (campaign-state pause, negative-keyword create, target-state bid-down) in DataDoe Settings → Actions before any real apply — surfaced as a blocking human checkpoint; a real apply against a disabled type returns action_type_disabled, not a silent failure."
    - "The canary runs first: exactly ONE reversible write (a single dead-SKU campaign pause) end-to-end — apply → poll actions_get to COMPLETED → FIND-echo confirm → reconciled — with the one-time --confirm-canary."
    - "After the canary confirms, the stop-the-bleed batch runs under standing-approved permissive auto-apply: pause the WHOLE campaigns for the 4 dead SKUs (FX-M8MA, 9Z-KUHZ, VH-ZTOC, ZK-4NDS) + per-campaign exact-match negative-keyword harvest, all one batch."
    - "Campaign identity comes from a live *_FIND cache (the agent FINDs the dead-SKU campaigns, find_cache.write persists, the spine lookups) — NOT from sku_catalog.toml (only 2 of the 4 dead SKUs are in it)."
    - "Negative harvest is exact-by-default; the keep-positive allowlist protects `baklava gift box` (never negated); watch-don't-negate terms are bid-down not negated; no phrase/broad negatives in this batch."
    - "Every applied pause/negative logs a dated decisions.md line + brain/raw note (what/why/expected-effect/actionId); reconciliation is via actions_get COMPLETED + FIND echo, never a next-day DataDoe export."
  artifacts:
    - path: "tasks/stop-the-bleed-batch.md"
      provides: "The runbook prompt that drives the canary + batch via apply_action.py"
      contains: "confirm-canary"
    - path: "brain/raw/2026-06-23_stop-the-bleed-batch.md"
      provides: "The institutional-memory note of the first real writes"
      contains: "actionId"
  key_links:
    - from: "tasks/stop-the-bleed-batch.md"
      to: "engine/scripts/apply_action.py"
      via: "the runbook calls the apply CLI per campaign-pause and per negative"
      pattern: "apply_action"
    - from: "tasks/stop-the-bleed-batch.md"
      to: "data/actions_ledger.jsonl"
      via: "each applied write appends an idempotency record"
      pattern: "actions_ledger"
---

<objective>
Apply the apply spine to the runbook: clear the org-enable blocking precondition, run the
canary, then run the standing-approved stop-the-bleed batch that flips ~43% of spend (~$279/mo)
off the 4 dead SKUs with ~$0 sales loss — the cleanest possible first real-write proof.

Purpose: SC-1..4 / EXEC-01 — the bleed is the proof the spine works end-to-end against the live
account. This plan is the execution runbook + the two non-code gates (the org-enable human
checkpoint and the one-time canary confirm). All engine code already exists (Plans 01-03); this
plan wires it to the live account through the agent's MCP calls.
Output: the batch runbook task, the canary + batch applied + reconciled, the ledger + decisions
+ brain log of the first real writes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-reversible-write-apply-spine-stop-the-bleed/08-RESEARCH.md
@.planning/phases/08-reversible-write-apply-spine-stop-the-bleed/08-CONTEXT.md
@.claude/skills/datadoe-query/SKILL.md
@brain/raw/2026-06-16_ppc-coverage-gap.md
@brain/raw/2026-06-20_datadoe-write-path-gating.md
@state/decisions.md
@CLAUDE.md

<dead_sku_runbook>
<!-- The 4 dead SKUs the batch pauses (from @brain/raw/2026-06-16 + anabtawi-context table). -->
FX-M8MA-MMSA  (ASIN B0FTSM2HSJ, Baklava 180g DUP, EXIT — "pause ads")   — in sku_catalog
9Z-KUHZ-FU2I  (ASIN B0FTSMTDGP, Baklava 400g DUP, EXIT — "stop ads")    — in sku_catalog
VH-ZTOC-GW1Q  (ASIN B0FTM5PBZW, Maamoul Pistachio 550g, EXIT)           — NOT in sku_catalog
ZK-4NDS-MNA9  (inactive remnant, no ASIN/COGS)                          — NOT in sku_catalog
Campaign identity for ALL FOUR comes from a live *_FIND (a pause is spend-decreasing → the
gate auto-passes with no catalog read; do NOT route dead-SKU identity through sku_catalog —
RESEARCH Pitfall 5). keep-positive allowlist: `baklava gift box` (never negated).
</dead_sku_runbook>

<org_enable_precondition>
<!-- Blocking, not deferred-scope (RESEARCH Environment Availability + Deferred Ideas). -->
DataDoe → Settings → Actions org-enable for: campaign-state (pause), negative-keyword create,
target-state (bid-down). OFF BY DEFAULT. BUILD + dryRun work without it; the canary apply +
the batch are BLOCKED until Rami flips the switches. A real apply against a disabled type
returns a 412 → action_type_disabled refusal (do not retry — SKILL line 192).
</org_enable_precondition>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the stop-the-bleed batch runbook (tasks/stop-the-bleed-batch.md)</name>
  <files>tasks/stop-the-bleed-batch.md</files>
  <read_first>
    - brain/raw/2026-06-16_ppc-coverage-gap.md (the 4 dead SKUs, the ~$279/mo bleed, keep-positive / watch-don't-negate intent)
    - .claude/skills/datadoe-query/SKILL.md (the *_FIND-before-write + actions_start/actions_get tools the runbook drives; the reconcile-via-status discipline; the 412-disabled rule)
    - engine/scripts/apply_action.py (the CLI the runbook invokes per pause/negative — Plan 03)
    - .planning/phases/08-reversible-write-apply-spine-stop-the-bleed/08-CONTEXT.md (D-03/D-04/D-05 batch scope + D-08/D-09 canary sequencing)
  </read_first>
  <action>
    Create `tasks/stop-the-bleed-batch.md` — a runbook prompt (mirror the existing tasks/*.md
    cadence-prompt style) that the agent executes. It MUST sequence:
      1. PRECONDITION: confirm the DataDoe Settings → Actions org-enable for the reversible set
         (campaign-state, negative-keyword create, target-state) — point to the Task-2 checkpoint;
         a 412 means not-yet-enabled (action_type_disabled, do not retry).
      2. FIND: for each of the 4 dead SKUs (FX-M8MA, 9Z-KUHZ, VH-ZTOC, ZK-4NDS) the agent runs
         `*_FIND` to get the live campaignId(s), hands the result to `find_cache.write`. Identity
         comes from FIND, never from sku_catalog (RESEARCH Pitfall 5).
      3. CANARY: run ONE campaign-pause through `apply_action.py --confirm-canary` end-to-end —
         apply → poll actions_get to COMPLETED → FIND-echo confirm → reconcile. Verify it logs to
         decisions.md + brain/raw with the actionId. This is the one-time gate (D-08/D-09).
      4. BATCH (after canary COMPLETED): pause the remaining whole campaigns for the 4 dead SKUs
         (D-05 — whole campaigns, no empty ENABLED shell) + per-campaign EXACT-match negative
         harvest (D-04 — exact-by-default; keep-positive allowlist protects `baklava gift box`,
         NEVER negated; watch-don't-negate terms → bid-down NOT negated; NO phrase/broad
         negatives). Each move runs the full spine via the CLI; standing-approved auto-apply
         (D-01) — but each STILL logs (D-11).
      5. RECONCILE: each pause/negative via `actions_get` COMPLETED + a `*_FIND` echo — NEVER a
         next-day DataDoe export (≤24h lag, WRITE-05).
    The runbook must state explicitly that idempotency is automatic (the ledger no-ops a re-run)
    and that the seller UUID is never inlined in any logged line.
  </action>
  <acceptance_criteria>
    - `tasks/stop-the-bleed-batch.md` exists, names the 4 dead SKUs, references `apply_action.py` and `--confirm-canary`, and states the keep-positive `baklava gift box` allowlist.
    - The runbook routes campaign identity through `*_FIND` / `find_cache`, NOT `sku_catalog.toml` (`grep -qi "FIND" tasks/stop-the-bleed-batch.md` and no instruction to read sku_catalog for dead-SKU identity).
    - The runbook specifies reconcile-via-actions_get-COMPLETED + FIND echo, never via a DataDoe export.
    - The runbook states exact-only negatives (no phrase/broad) and watch-don't-negate → bid-down.
  </acceptance_criteria>
  <verify>
    <automated>test -f tasks/stop-the-bleed-batch.md && grep -qiE "confirm[-_]canary" tasks/stop-the-bleed-batch.md && grep -qi "baklava gift box" tasks/stop-the-bleed-batch.md && grep -qi "FIND" tasks/stop-the-bleed-batch.md && echo OK</automated>
  </verify>
  <done>The batch runbook exists and encodes D-03/D-04/D-05 + the canary sequencing + reconcile-via-status discipline; campaign identity routed through FIND not the catalog.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Org-enable the reversible Ads action types (Rami flips the Settings → Actions switches)</name>
  <files>(none — this is a DataDoe UI/DB setting, not a repo file)</files>
  <read_first>
    - brain/raw/2026-06-20_datadoe-write-path-gating.md (the 4-gate chain + the Settings → Actions org-enable precondition, verified live 2026-06-20)
    - .claude/skills/datadoe-query/SKILL.md (the 412-disabled rule — line 192)
  </read_first>
  <action>
    PAUSE for Rami. This action has no CLI/API surface Claude can drive — the DataDoe
    Settings → Actions org-enable is a UI/DB toggle, OFF by default, that is a 4th access-control
    gate beyond our own approval. Surface the blocking precondition: the full spine + CLI are
    built, tested, and dry-run-ready (Plans 01-03), and the ONLY remaining gate before a real
    reversible write is this org-enable. Wait for Rami to enable the three reversible action types
    (campaign-state pause, negative-keyword create exact-match, target-state bid-down) and leave
    CAMPAIGNS_ADD / CAMPAIGNS_REMOVE OFF (Phase 10). Do NOT auto-advance.
  </action>
  <what-built>The full apply spine + CLI (Plans 01–03) is built, tested, and dry-run-ready. The ONLY remaining gate before a real reversible write is the DataDoe org-enable, which is a UI/DB setting NOT in git and which Claude cannot flip.</what-built>
  <how-to-verify>
    1. Open DataDoe → Settings → Actions.
    2. Enable the reversible action types this phase uses: campaign-state update (pause),
       negative-keyword create (TARGETS add, exact match), and target-state update (bid-down).
       Leave CAMPAIGNS_ADD / CAMPAIGNS_REMOVE OFF (those are Phase 10).
    3. Confirm back here. (As a smoke check, an `actions_start dryRun:true` for a campaign pause
       should validate; the disabled state surfaces as a 412 only on a real `dryRun:false`.)
  </how-to-verify>
  <acceptance_criteria>
    - Rami confirms the three reversible action types (campaign-state, negative-keyword create, target-state) are org-enabled in DataDoe Settings → Actions.
    - CAMPAIGNS_ADD / CAMPAIGNS_REMOVE remain OFF (deferred to Phase 10).
  </acceptance_criteria>
  <verify>
    <human-check>Rami has enabled the three reversible Ads action types in DataDoe Settings → Actions and left CAMPAIGNS_ADD/REMOVE off.</human-check>
  </verify>
  <resume-signal>Reply "enabled" once the three reversible action types are switched on (or name any that DataDoe does not expose).</resume-signal>
  <done>The reversible Ads action types are org-enabled in DataDoe; the canary apply is unblocked.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run the canary + stop-the-bleed batch against the live account (the first real writes)</name>
  <files>data/actions_ledger.jsonl, state/decisions.md, brain/raw/2026-06-23_stop-the-bleed-batch.md</files>
  <read_first>
    - tasks/stop-the-bleed-batch.md (the runbook from Task 1 — the exact sequence to execute)
    - engine/scripts/apply_action.py (the CLI + --confirm-canary flag — Plan 03)
    - .claude/skills/datadoe-query/SKILL.md (FIND-before-write, dryRun discipline, reconcile-via-actions_get)
  </read_first>
  <action>
    The agent executes tasks/stop-the-bleed-batch.md end-to-end via the DataDoe MCP + the
    apply_action.py CLI: FIND the 4 dead-SKU campaigns (cache via find_cache), run the CANARY
    single pause with `--confirm-canary`, reconcile it to COMPLETED + FIND echo, then — only after
    the canary reconciles — run the standing-approved batch (whole-campaign pauses + exact-match
    negatives, keep-positive protected). Append a ledger record + decisions.md line + brain/raw
    note per applied move. PAUSE for Rami to spot-check Campaign Manager before marking complete.
    Do NOT auto-advance past the human verification.
  </action>
  <what-built>
    The agent executes tasks/stop-the-bleed-batch.md: FINDs the 4 dead-SKU campaigns (caching via
    find_cache), runs the CANARY single pause through `apply_action.py --confirm-canary`
    (apply → poll actions_get to COMPLETED → FIND-echo confirm → reconcile, logging decisions.md +
    brain/raw with the actionId), then — only after the canary reconciles COMPLETED — runs the
    standing-approved batch: whole-campaign pauses for the 4 dead SKUs + per-campaign exact-match
    negative harvest (keep-positive `baklava gift box` protected; watch-don't-negate → bid-down;
    no phrase/broad). Each applied write appends an idempotency record to data/actions_ledger.jsonl
    and logs decisions.md + brain/raw. Reconcile each via actions_get COMPLETED + FIND echo, never
    a next-day DataDoe export. A re-run of any move is a ledger no-op.
  </what-built>
  <how-to-verify>
    1. Confirm the canary pause reached `actions_get` COMPLETED + the FIND echo shows the campaign
       `state:"PAUSED"`, and a decisions.md line + brain/raw note carry the actionId.
    2. Confirm the batch paused the whole campaigns for all 4 dead SKUs (no empty ENABLED shell)
       and applied the exact-match negatives, with `baklava gift box` NOT negated.
    3. Spot-check in Amazon Campaign Manager that the 4 dead-SKU campaigns are paused and the
       ~$279/mo (~43%) spend is off them, with healthy MAINTAIN-tier SKUs untouched.
    4. Confirm `data/actions_ledger.jsonl` has one COMPLETED record per applied move and that
       re-running a move is a no-op (the ledger short-circuits).
    5. Confirm no seller UUID appears in decisions.md, the brain note, or logs/external-calls.jsonl.
  </how-to-verify>
  <acceptance_criteria>
    - `data/actions_ledger.jsonl` has one COMPLETED record per applied move and parses as JSONL.
    - No seller UUID appears in decisions.md, the brain note, or logs/external-calls.jsonl.
    - Rami confirms in Campaign Manager that the 4 dead-SKU campaigns are paused (~$279/mo off) and `baklava gift box` was never negated.
  </acceptance_criteria>
  <verify>
    <automated>test -s data/actions_ledger.jsonl && python3 -c "import json; [json.loads(l) for l in open('data/actions_ledger.jsonl') if l.strip()]; print('ledger OK')" && ! grep -RIl "$AMAZON_CA_SELLER_ID" state/decisions.md brain/raw/2026-06-23_stop-the-bleed-batch.md 2>/dev/null</automated>
    <human-check>The 4 dead-SKU campaigns are paused in Campaign Manager (~$279/mo off), `baklava gift box` was never negated, each move reconciled via actions_get COMPLETED + FIND echo, and decisions.md + brain/raw log every applied write with its actionId.</human-check>
  </verify>
  <resume-signal>Reply "reconciled" once the canary + batch are applied, spot-checked in Campaign Manager, and logged; or describe any move that did not reach COMPLETED.</resume-signal>
  <done>The canary + stop-the-bleed batch are applied, polled to COMPLETED, FIND-echo confirmed, reconciled, idempotency-recorded, and logged to decisions.md + brain/raw — the dead-SKU bleed is stopped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → DataDoe Settings | The org-enable switch is a 4th access-control gate beyond our own approval; off by default. |
| agent (MCP) → live Amazon account | The canary + batch are the first real `dryRun:false` mutations; gated by the full spine + the canary confirm. |
| applied writes → decisions.md / brain / ledger | The durable record + idempotency truth of the first real writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-16 | Elevation of Privilege | a real write firing against a disabled action type | mitigate | Org-enable is a blocking human checkpoint (Task 2); a disabled type returns `action_type_disabled` (412, no retry) at apply (RESEARCH Pitfall 4). |
| T-08-17 | Tampering | the batch auto-firing before the spine is proven | mitigate | The canary (Task 3 step 1) is a one-time confirm that must reconcile COMPLETED before the batch runs (D-08/D-09). |
| T-08-18 | Tampering (collateral) | negating `baklava gift box` and killing a positive term | mitigate | keep-positive allowlist enforced in the runbook + verified in step 2; exact-only, no phrase/broad (D-04). |
| T-08-19 | Tampering | stale FIND cache pausing a wrong/dead campaign ID | mitigate | Live `*_FIND` before each pause + the A2 staleness window (Plan 02) → `stale_find_cache` refusal; FIND-echo confirms the right entity changed. |
| T-08-20 | Tampering (false reconcile) | confirming a write via a next-day DataDoe export | mitigate | Reconcile ONLY via actions_get COMPLETED + FIND echo; the runbook forbids export-as-confirmation (WRITE-05). |
| T-08-21 | Information Disclosure | seller UUID leaking into decisions.md / brain / logs | mitigate | Verified-absent in step 5 + the automated grep gate (CLAUDE.md hard rule 5). |
| T-08-22 | Repudiation | a standing-approved auto move leaving no record | mitigate | Every applied write logs decisions.md + brain + a ledger record with the actionId (D-11, WRITE-06). |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH audit N/A). |
</threat_model>

<verification>
- The org-enable checkpoint is cleared before any real apply (Task 2).
- The canary pause reconciles COMPLETED + FIND echo before the batch runs.
- The 4 dead-SKU campaigns are paused (~$279/mo off), `baklava gift box` never negated, every move logged + ledger-recorded; no seller UUID anywhere.
- `cd engine && uv run pytest` still green (no code change here, but the suite must remain green at phase gate).
</verification>

<success_criteria>
- The first real reversible writes land end-to-end (build → dryRun → apply → poll → COMPLETED → FIND echo → reconcile → log), gated behind the org-enable + canary.
- The stop-the-bleed batch flips ~43% of spend off the 4 dead SKUs with ~$0 sales loss; idempotent and logged.
- EXEC-01 enforced: dead-SKU campaigns paused + exact negatives harvested, keep-positive protected, identity from FIND not the catalog.
</success_criteria>

<output>
Create `.planning/phases/08-reversible-write-apply-spine-stop-the-bleed/08-04-SUMMARY.md` when done.
</output>
