---
phase: 08-reversible-write-apply-spine-stop-the-bleed
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - engine/tests/fixtures/actions_start_dryrun_pause.json
  - engine/tests/fixtures/actions_start_dryrun_negative.json
  - engine/tests/fixtures/actions_start_dryrun_biddown.json
  - engine/tests/fixtures/actions_get_inflight.json
  - engine/tests/fixtures/campaigns_find_echo.json
  - engine/tests/fixtures/actions_ledger_seed.jsonl
  - engine/tests/fixtures/owned_asins_test.toml
  - .planning/phases/08-reversible-write-apply-spine-stop-the-bleed/08-SCHEMA-PIN.md
  - engine/src/habibos/result.py
  - engine/tests/test_apply.py
  - engine/tests/test_ledger.py
  - engine/tests/test_denylist.py
  - engine/tests/test_magnitude_cap.py
  - engine/tests/test_find_cache.py
autonomous: false
requirements: [WRITE-03, WRITE-06, WRITE-07, EXEC-01]

must_haves:
  truths:
    - "The live actions_start payload schema for each reversible action type (pause campaign-state, exact-match negative create, bid-down target-state) is captured as a sanitized fixture from a real actions_details_schema_get + dryRun:true round-trip — never fabricated."
    - "Every Wave-0 test file imports the not-yet-written modules and fails RED on the missing import (test_apply, test_ledger, test_denylist, test_magnitude_cap) — the intended Wave-0 state."
    - "ProposedAction carries optional entity_type / entity_id / params with defaults so the Phase-7 gate CLI contract (test_gate_action.py) stays green."
    - "AppliedResult exists as the spine's typed positive counterpart (action_id, terminal status, hash, applied_at, marketplace)."
  artifacts:
    - path: "engine/tests/fixtures/actions_start_dryrun_pause.json"
      provides: "Sanitized pinned dryRun:true validated response for a campaign-state pause"
      contains: "VALIDATED"
    - path: "engine/src/habibos/result.py"
      provides: "Extended ProposedAction + new AppliedResult dataclass"
      contains: "AppliedResult"
    - path: "engine/tests/test_apply.py"
      provides: "RED contract for the apply spine, poll loop, canary, log, magnitude cap, disabled-action"
      contains: "from habibos import apply"
    - path: "engine/tests/test_ledger.py"
      provides: "RED contract for content-hash idempotency + in-flight resume + date-excluded hash"
      contains: "from habibos import ledger"
    - path: "engine/tests/test_denylist.py"
      provides: "RED contract for own-ASIN hard refusal + pause-passes + owned-set source"
      contains: "from habibos import denylist"
    - path: ".planning/phases/08-reversible-write-apply-spine-stop-the-bleed/08-SCHEMA-PIN.md"
      provides: "The pinned field-name map per action type, captured live, the source-of-truth for build_payload"
      contains: "actions_details_schema_get"
  key_links:
    - from: "engine/src/habibos/result.py"
      to: "engine/scripts/gate_action.py"
      via: "ProposedAction defaults keep the Phase-7 CLI contract green"
      pattern: "entity_type|entity_id|params"
    - from: "engine/tests/test_apply.py"
      to: "engine/tests/fixtures/actions_start_dryrun_pause.json"
      via: "fixture-anchored RED assertions"
      pattern: "fixtures"
---

<objective>
Pin the three live reversible-action payload schemas, capture them as sanitized fixtures,
extend the typed grammar, and lay down every RED Wave-0 test contract the GREEN modules
(Plans 02–03) implement against.

Purpose: Phase 8 lands the FIRST real account writes. The single architecturally-important
unknown is the exact DataDoe `actions_start` payload schema per reversible action type — and
RESEARCH (Pitfall 1, Assumptions A1–A3, Open Question 1) is explicit that it was NOT reachable
read-only and MUST NOT be fabricated. This plan pins it live, then writes the executable
contract everything else satisfies. RED-first matches Phases 6 & 7.
Output: 3 pinned dryRun fixtures + echo/in-flight/ledger-seed fixtures, `08-SCHEMA-PIN.md`,
extended `result.py`, and 4 new + 1 extended RED test files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-reversible-write-apply-spine-stop-the-bleed/08-RESEARCH.md
@.planning/phases/08-reversible-write-apply-spine-stop-the-bleed/08-PATTERNS.md
@.planning/phases/08-reversible-write-apply-spine-stop-the-bleed/08-VALIDATION.md
@.claude/skills/datadoe-query/SKILL.md
@CLAUDE.md

<interfaces>
<!-- Extracted from the repo — the executor uses these directly, no exploration needed. -->

From engine/src/habibos/result.py (the type the spine threads — EXTEND in place):
```python
@dataclass(frozen=True)
class ProposedAction:
    sku: str
    action_type: str          # "bid_up" | "budget_up" | "pause" | "negative" | "bid_down"
    delta_spend: float        # >0 spend-increasing (gated); <=0 spend-decreasing (auto-pass)
    marketplace: str = "CA"
    @property
    def is_spend_increasing(self) -> bool: return self.delta_spend > 0

@dataclass(frozen=True)
class GateRefusal:
    reason: str; code: str
    breached_value: float | None = None
    ceiling: float | None = None
    source: str | None = None

@dataclass(frozen=True)
class Refusal:
    reason: str; code: str
    export_id: str | None = None

@dataclass(frozen=True)
class Proceed:        # classify_dryrun returns this on a clean dryRun
    action_type: str

@dataclass(frozen=True)
class PollOutcome:    # classify_poll returns this per actions_get response
    status: str; state: str  # "done" | "done_with_issues" | "in_flight"
    issues: list[str] = field(default_factory=list)
```

From engine/src/habibos/lifecycle.py (the classifiers the spine drives — reuse verbatim):
```python
def classify_dryrun(resp: dict) -> Proceed | Refusal   # Refusal code="dryrun_invalid"
def classify_poll(resp: dict) -> PollOutcome | Refusal # codes: action_error | unknown_status
```

From engine/src/habibos/datadoe.py (the poll primitive + pinned enum — reuse verbatim):
```python
ACTION_IN_FLIGHT       = {"PENDING","IN_PROGRESS","WAITING_EXTERNAL_PROCESSING"}
ACTION_TERMINAL_SUCCESS= {"COMPLETED"}
ACTION_TERMINAL_PARTIAL= {"PARTIALLY_COMPLETED","COMPLETED_WITH_ISSUES"}
ACTION_TERMINAL_FAIL   = {"ERROR","CANCELED","BLOCKED_NO_TOKENS","BLOCKED_INVALID_INPUT"}  # no FAILED; CANCELED one L
def poll_action_status(get_status) -> str  # tenacity, stop_after_attempt(20), wait_exponential_jitter(initial=1,max=15)
```

From engine/src/habibos/find_cache.py (FIND-before-write + echo — reuse verbatim):
```python
DATA_DIR = Path(__file__).resolve().parents[2] / "data"  # monkeypatched in tests
def write(entity_type, ad_product, find_result) -> Path
def lookup(entity_type, ad_product, entity_id) -> dict | Refusal  # miss code="find_cache_miss"
# _ENTITY_MAP keys: campaigns/adGroups/targets/ads
```
</interfaces>

<fixture_conventions>
Existing sanitized fixtures the new ones mirror (style + no-seller-UUID discipline):
@engine/tests/fixtures/dryrun_validated.json
@engine/tests/fixtures/dryrun_invalid.json
@engine/tests/fixtures/actions_get_completed.json
@engine/tests/fixtures/actions_get_error.json
@engine/tests/fixtures/campaigns_find.json
RED test style + tmp_path monkeypatch:
@engine/tests/test_find_cache.py
@engine/tests/test_gate.py
</fixture_conventions>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Pin the three reversible-action payload schemas LIVE (recon — agent runs the MCP)</name>
  <files>engine/tests/fixtures/actions_start_dryrun_pause.json, engine/tests/fixtures/actions_start_dryrun_negative.json, engine/tests/fixtures/actions_start_dryrun_biddown.json, .planning/phases/08-reversible-write-apply-spine-stop-the-bleed/08-SCHEMA-PIN.md</files>
  <read_first>
    - .claude/skills/datadoe-query/SKILL.md (the four actions_* tools; the live-confirmed `CAMPAIGNS_UPDATE` "do NOT echo adProduct" caveat at lines 174-175; the 412-disabled rule at line 192; FIND-before-write + dryRun discipline)
    - .planning/phases/08-reversible-write-apply-spine-stop-the-bleed/08-RESEARCH.md (Pitfall 1, Pitfall 2, Assumptions A1-A3, Open Question 1 — the documented pin point)
  </read_first>
  <action>
    For EACH of the three reversible action types, the AGENT (not Python — Python never opens a
    socket) runs the DataDoe MCP read-only/validation calls and records the real field names:
    (a) `actions_details_schema_get` for the action type, then (b) `actions_start dryRun:true`
    with a candidate payload, iterating until `validation.valid:true` with `issues:[]`.
    The three action types to pin:
      1. PAUSE a campaign — the campaign-state write (expected `AMAZON_ADS_CAMPAIGNS_UPDATE`,
         sending only mutable fields `{campaignId, state:"PAUSED"}`; do NOT echo `adProduct`).
      2. ADD an exact-match NEGATIVE keyword — pin whether it is a `TARGETS_ADD`-style
         negative-target create, its match-type literal (`NEGATIVE_EXACT` vs `negativeExact`),
         and the attachment level (campaign vs ad-group) — resolves RESEARCH Pitfall 2 / A2.
      3. BID-DOWN a target/keyword — the `bid` write on a targetId/keywordId — resolves A3.
    Write the captured, SANITIZED dryRun-validated responses to the three fixture files in
    files_modified, each mirroring the `dryrun_validated.json` shape (`type`, `status:"VALIDATED"`,
    `validation.valid:true`, `validation.issues:[]`). Strip any seller UUID, account id, or token
    from every fixture. Record the pinned field-name map (per action type: exact request field
    names + nesting + match-type literal + negative attachment level) in a new `08-SCHEMA-PIN.md`
    under the phase dir — the single source of truth `build_payload` (Plan 03) and the ledger
    normalized-params map (Plan 02) are written against.
    If the live MCP is unreachable this session, STOP and surface the blocker — do NOT fabricate
    field names (CLAUDE.md hard rule 4); the whole phase depends on real schemas.
  </action>
  <acceptance_criteria>
    - The three fixture files exist and each parses as JSON with `status == "VALIDATED"` and `validation.valid == true` and `validation.issues == []`.
    - `grep -rIl "$AMAZON_CA_SELLER_ID" engine/tests/fixtures/` returns nothing (no seller UUID literal in any fixture).
    - `08-SCHEMA-PIN.md` exists and names, per action type, the exact request field names, the negative match-type literal, and the negative attachment level (campaign vs ad-group).
    - The pause fixture payload does NOT contain an `adProduct` key (the live validator rejects it — SKILL line 174).
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import json,glob; [json.load(open(f)) for f in glob.glob('engine/tests/fixtures/actions_start_dryrun_*.json')]; print('OK')"</automated>
    <human-check>The three dryRun fixtures were captured from a real actions_details_schema_get + dryRun:true round-trip (not hand-authored), and 08-SCHEMA-PIN.md records the live field map.</human-check>
  </verify>
  <done>Three sanitized, live-validated dryRun fixtures + 08-SCHEMA-PIN.md exist; no seller UUID; no `adProduct` on the pause payload.</done>
</task>

<task type="auto">
  <name>Task 2: Extend result.py — ProposedAction fields + AppliedResult + capture supporting fixtures</name>
  <files>engine/src/habibos/result.py, engine/tests/fixtures/actions_get_inflight.json, engine/tests/fixtures/campaigns_find_echo.json, engine/tests/fixtures/actions_ledger_seed.jsonl, engine/tests/fixtures/owned_asins_test.toml</files>
  <read_first>
    - engine/src/habibos/result.py (the dataclasses to extend — ProposedAction at the class def; GateVerdict as the frozen-dataclass analog for AppliedResult)
    - engine/tests/test_gate_action.py (the Phase-7 CLI contract that MUST stay green — it constructs ProposedAction with only sku/action_type/delta_spend/marketplace)
    - engine/tests/fixtures/actions_get_completed.json, campaigns_find.json (style for the new echo / in-flight / seed fixtures)
  </read_first>
  <action>
    Extend `engine/src/habibos/result.py` IN PLACE (do not fork). Add to `ProposedAction` three
    OPTIONAL fields with defaults so the Phase-7 gate ignores them and `test_gate_action.py`
    stays green (RESEARCH Open Q2): `entity_type: str | None = None` ("campaigns"|"targets"|"adGroups"),
    `entity_id: str | None = None` (the FIND-verified id), `params: dict = field(default_factory=dict)`.
    Add a new frozen dataclass `AppliedResult` (mirror `GateVerdict`'s frozen style) carrying:
    `action_id: str`, `status: str` (the terminal wire status), `hash: str` (the ledger content
    hash), `applied_at: str` (ISO), `marketplace: str = "CA"` — the spine's typed positive
    counterpart to a Refusal. Document the new `GateRefusal`/`Refusal` codes this phase introduces
    as a docstring note (they reuse `GateRefusal`/`Refusal`, no new dataclass): `own_asin_denied`,
    `over_cap`, `stale_find_cache`, `canary_unconfirmed`, `action_type_disabled`,
    `unresolved_in_flight`.
    Also capture the four supporting fixtures: `actions_get_inflight.json` (an in-flight
    actions_get response, status `IN_PROGRESS`, mirror `actions_get_completed.json`),
    `campaigns_find_echo.json` (a campaigns FIND result whose one campaign reflects
    `state:"PAUSED"` for the echo-confirm), `actions_ledger_seed.jsonl` (2-3 sanitized ledger
    lines per RESEARCH Pattern 3 record schema: one COMPLETED, one in-flight), and
    `owned_asins_test.toml` (a small owned-ASIN catalog fixture mirroring `sku_catalog_test.toml`
    — include B07TV972JT + a couple owned ASINs; used by test_denylist).
  </action>
  <acceptance_criteria>
    - `result.py` contains `class AppliedResult` and `ProposedAction` has `entity_type`, `entity_id`, and `params` with defaults.
    - `cd engine && uv run pytest tests/test_gate_action.py tests/test_gate.py` exits 0 (Phase-7 contract unbroken by the ProposedAction extension).
    - `engine/tests/fixtures/actions_get_inflight.json`, `campaigns_find_echo.json`, `actions_ledger_seed.jsonl`, `owned_asins_test.toml` all exist and parse.
    - No seller UUID literal in any new fixture.
  </acceptance_criteria>
  <verify>
    <automated>cd engine && uv run pytest tests/test_gate_action.py tests/test_gate.py -x && python -c "from habibos.result import AppliedResult, ProposedAction; a=ProposedAction(sku='X',action_type='pause',delta_spend=0); assert a.entity_id is None and a.params=={}; print('OK')"</automated>
  </verify>
  <done>ProposedAction extended (defaults preserve the Phase-7 CLI), AppliedResult added, supporting fixtures captured; Phase-7 tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write the RED Wave-0 test contracts (test_apply / test_ledger / test_denylist / test_magnitude_cap + extend test_find_cache)</name>
  <files>engine/tests/test_apply.py, engine/tests/test_ledger.py, engine/tests/test_denylist.py, engine/tests/test_magnitude_cap.py, engine/tests/test_find_cache.py</files>
  <read_first>
    - engine/tests/test_find_cache.py (the tmp_path DATA_DIR monkeypatch fixture + Refusal-shape assertions to clone)
    - engine/tests/test_gate.py (RED-on-import baseline + fixture-anchored refusal-code assertions)
    - .planning/phases/08-reversible-write-apply-spine-stop-the-bleed/08-VALIDATION.md (the Wave-0 gap list + the Phase-Requirements→Test map naming each test function)
    - engine/src/habibos/find_cache.py (the lookup signature the A2 staleness test extends)
  </read_first>
  <behavior>
    test_ledger.py (WRITE-07): test_completed_hash_is_noop (a COMPLETED hash → LedgerHit, no second start);
      test_inflight_resumes_at_poll (in-flight actionId → resume, never re-fire start);
      test_hash_excludes_date (same entity+action+params on two dates → identical hash).
    test_denylist.py (EXEC-01): test_own_asin_refused (a target ASIN in the owned set → GateRefusal code="own_asin_denied");
      test_pause_passes_denylist (a pause with no target ASIN → passes cleanly, None);
      test_owned_set_from_dedicated_source (the owned set is read from the dedicated owned_asins source, NOT sku_catalog cogs rows).
    test_magnitude_cap.py (SC-5/D-02): test_magnitude_cap_refuses_when_set (cap configured + over-cap reversible → GateRefusal code="over_cap", never clamps);
      test_magnitude_cap_seeded_permissive (cap absent/None → reversible passes at any magnitude).
    test_apply.py (WRITE-03/06): test_reversible_write_end_to_end (build→dryRun(Proceed)→apply→poll-to-COMPLETED→echo→AppliedResult);
      test_dryrun_refusal_blocks_apply (dryRun STOP short-circuits before any dryRun:false);
      test_poll_loop_to_terminal (drives poll via mocked get_status to terminal);
      test_apply_logs_decision_and_brain (tmp_path-monkeypatched decisions.md + brain note carry the actionId);
      test_canary_one_time_confirm (first apply requires confirm marker, then auto);
      test_disabled_action_refuses (a 412/disabled or ERROR poll → typed refusal, no retry).
    test_find_cache.py extension (A2): test_stale_cache_refused (a cache file older than the staleness window → Refusal code="stale_find_cache" for a write-bearing lookup).
  </behavior>
  <action>
    Create the four new test files and extend `test_find_cache.py`, each with the RED-on-import
    baseline (`from habibos import apply` / `ledger` / `denylist` / `magnitude` — failing RED
    until Plans 02-03 create the modules; the intended Wave-0 state, documented in each module
    docstring like `test_find_cache.py`). Use the `tmp_path` + `monkeypatch.setattr(<module>,
    "DATA_DIR", tmp_path)` fixture for ledger and the apply log seam; anchor every payload
    assertion on the Task-1/Task-2 fixtures (no inline fabricated schemas). Assert typed-refusal
    SHAPE (isinstance Refusal/GateRefusal + `.code == "..."`), mirroring test_find_cache.py:57-64.
    Name the test functions EXACTLY as in the behavior block (08-VALIDATION.md test map depends
    on these names). Do NOT implement any production module here — RED only.
  </action>
  <acceptance_criteria>
    - All four new test files + the extended test_find_cache exist and contain the exact test-function names listed in <behavior>.
    - `cd engine && uv run pytest tests/test_apply.py tests/test_ledger.py tests/test_denylist.py tests/test_magnitude_cap.py` fails RED with a collection/import error on the missing `habibos.apply`/`ledger`/`denylist`/`magnitude` modules (NOT an assertion pass — confirms Wave-0 RED state).
    - No test contains an inline fabricated DataDoe payload schema — all payload shapes are loaded from the Task-1/Task-2 fixtures.
    - `grep -rIn "$AMAZON_CA_SELLER_ID" engine/tests/` returns nothing.
  </acceptance_criteria>
  <verify>
    <automated>cd engine && uv run pytest tests/test_ledger.py tests/test_denylist.py tests/test_magnitude_cap.py tests/test_apply.py 2>&1 | grep -qiE "no module named 'habibos.(apply|ledger|denylist|magnitude)'|ModuleNotFoundError|ImportError" && echo "RED-as-expected"</automated>
  </verify>
  <done>Four RED test files + extended test_find_cache exist with the named contract functions; they fail RED on the missing modules; no fabricated schemas, no seller UUID.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent (MCP) → engine | The agent hands DataDoe `actions_*` JSON responses to Python; the captured fixtures cross this boundary as test inputs. |
| committed fixtures → git | Sanitized response payloads enter the repo; must carry no secret. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | fabricated payload schema in fixtures | mitigate | Task 1 captures fixtures from a real `actions_details_schema_get` + `dryRun:true` round-trip; the human-action checkpoint blocks on confirming they are live-captured, not hand-authored (RESEARCH Pitfall 1). |
| T-08-02 | Information Disclosure | seller UUID leaking into a committed fixture | mitigate | Sanitize every fixture; acceptance greps `$AMAZON_CA_SELLER_ID` to zero across `engine/tests/fixtures/` (CLAUDE.md hard rule 5). |
| T-08-03 | Tampering | extending ProposedAction breaks the Phase-7 gate contract | mitigate | New fields are optional with defaults; acceptance re-runs `test_gate_action.py` + `test_gate.py` green. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | This phase installs NO packages (RESEARCH Package Legitimacy Audit: N/A); all code reuses already-pinned deps. No legitimacy checkpoint needed. |
</threat_model>

<verification>
- `cd engine && uv run pytest tests/test_gate.py tests/test_gate_action.py` green (Phase-7 contract intact).
- The four new test files fail RED on missing modules (intended Wave-0 state).
- Three live-captured dryRun fixtures + 08-SCHEMA-PIN.md present; no seller UUID anywhere under `engine/tests/fixtures/`.
</verification>

<success_criteria>
- All three reversible payload schemas pinned LIVE and recorded (08-SCHEMA-PIN.md + 3 fixtures), never fabricated.
- result.py extended (ProposedAction defaults + AppliedResult) with the Phase-7 CLI contract green.
- Four RED test files + extended test_find_cache lay the executable contract for Plans 02–03.
</success_criteria>

<output>
Create `.planning/phases/08-reversible-write-apply-spine-stop-the-bleed/08-01-SUMMARY.md` when done.
</output>
