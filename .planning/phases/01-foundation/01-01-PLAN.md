---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .python-version
  - uv.lock
  - .gitignore
  - .env.example
  - .env
  - CLAUDE.md
  - tests/conftest.py
  - tests/test_env.py
  - tests/test_schema.py
  - tests/test_config_seed.py
  - tests/test_context.py
  - docs/datadoe-source-map.md
autonomous: false
requirements: [CONN-01, CONN-02, CONN-03]
must_haves:
  truths:
    - "Operator can run hermes mcp test for DataDoe, Supabase, and Gmail/Drive and see each respond"
    - "DataDoe is confirmed read-only — its tool surface exposes no write/mutate tool"
    - "AMAZON_CA_SELLER_ID is resolved and present in the repo .env, never inlined in source"
    - "A DataDoe source map records each confirmed source's columns, SP/SB/SD scope, date-column timezone basis, and premium Profit-by-SKU enablement, produced empirically via exports_sources_get"
    - "pytest runs and the Wave 0 test stubs exist for every automatable Phase 1 requirement"
  artifacts:
    - path: "pyproject.toml"
      provides: "uv project + locked Phase-1 deps + pytest config"
      contains: "[tool.pytest.ini_options]"
    - path: ".env.example"
      provides: "Committed template of required env vars (no secrets)"
      contains: "AMAZON_CA_SELLER_ID="
    - path: "tests/conftest.py"
      provides: "Postgres fixture for schema/seed integration tests"
    - path: "tests/test_env.py"
      provides: "CONN-02 guard — UUID in .env, not in source"
    - path: "tests/test_schema.py"
      provides: "DATA-01/02/03 stubs (consumed by plan 01-02)"
    - path: "tests/test_config_seed.py"
      provides: "DATA-04 stub (consumed by plan 01-04)"
    - path: "tests/test_context.py"
      provides: "CTX-01/02 stub (consumed by plan 01-03)"
    - path: "docs/datadoe-source-map.md"
      provides: "Empirical DataDoe source map (CONN-03)"
      contains: "Profit by SKU"
  key_links:
    - from: ".env"
      to: ".gitignore"
      via: ".env listed as ignored so the seller UUID is never committed"
      pattern: "^\\.env$"
    - from: "tests/test_env.py"
      to: "src/ and db/"
      via: "grep assertion that the literal UUID does not appear in tracked source"
      pattern: "5692b95f"
---

<objective>
Stand up the repo's Python substrate (uv project, locked deps, pytest + all Wave 0 test stubs), verify the three live MCP connections read-only, cache the amazon.ca seller UUID to `.env`, and produce the empirical DataDoe source map.

Purpose: Everything downstream assumes these exist — the test scaffold gates every later task's verification, the source map gates the exact schema column names (plan 01-02), and the cached UUID + verified read-only connections are the trust precondition for Phase 2's live queries.
Output: `pyproject.toml`/`uv.lock`, `.env`/`.env.example`/`.gitignore`, `tests/` scaffold (6 files), and `docs/datadoe-source-map.md`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
@docs/04-data-tools-reference.md

<interfaces>
<!-- Locked stack pins (RESEARCH §Standard Stack, re-verified PyPI 2026-06-08). Use EXACTLY these. -->
sqlalchemy==2.0.50  alembic==1.18.4  psycopg[binary]==3.3.4  pgvector==0.4.2
pydantic==2.13.4  python-dotenv==1.2.2  structlog==26.1.0  supabase==2.31.0
dev: ruff  pytest
Target interpreter: uv python pin 3.12 (Hermes runtime is 3.11.15; helpers may target 3.12 independently — RESEARCH §Installation).
Hermes Agent runtime: v0.16.0 (2026.6.5) — live-verified (RESEARCH §Standard Stack); CLAUDE.md still says v0.15.2 and must be corrected to v0.16.0 in this plan.

<!-- DataDoe call order (docs/04). UUID resolution + source enumeration only — NO export pull in Phase 1. -->
sellers_and_vendors_list -> resolves seller UUID
exports_sources_get(query, sellerOrVendorIds) -> source + columns
Known amazon.ca seller UUID: 5692b95f-f3f0-4063-9c1c-40177c54f408 (confirm via the call; cache to .env, never inline)

<!-- Wave 0 test files this plan must create (VALIDATION.md §Wave 0). Stubs assert against schema/config/context that LATER plans create — so they may xfail/skip until those plans run, but the files + test function names must exist now. -->
tests/test_env.py::test_seller_id_in_env_not_code        (CONN-02 — assert NOW)
tests/test_schema.py::test_tables_and_vector_ext         (DATA-01 — consumed by 01-02)
tests/test_schema.py::test_snapshot_idempotency_key      (DATA-02 — consumed by 01-02)
tests/test_schema.py::test_cogs_scd_fx                    (DATA-03 — consumed by 01-02)
tests/test_config_seed.py::test_required_thresholds_seeded (DATA-04 — consumed by 01-04)
tests/test_context.py::test_soul_and_context_deployed    (CTX-01/02 — consumed by 01-03)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold uv project + pytest + all Wave 0 test stubs + correct Hermes pin</name>
  <files>pyproject.toml, .python-version, uv.lock, .gitignore, CLAUDE.md, tests/conftest.py, tests/test_env.py, tests/test_schema.py, tests/test_config_seed.py, tests/test_context.py</files>
  <read_first>
    - .planning/phases/01-foundation/01-VALIDATION.md (§Wave 0 Requirements + §Per-Task Verification Map — the exact test file/function names and the commands they must satisfy)
    - .planning/phases/01-foundation/01-RESEARCH.md (§Installation for the exact `uv add` line; §Validation Architecture for the framework + run commands; §Recommended Project Structure for the `db/` and `tests/` layout; §Standard Stack + §State of the Art — Hermes runtime is live-verified v0.16.0, supersede the v0.15.2 pin)
    - CLAUDE.md (§Stack — locked pins; secrets-in-.env rule; the three v0.15.2 Hermes references to correct)
  </read_first>
  <action>
    Run `uv init` and `uv python pin 3.12`. Add the locked runtime deps and dev deps via a single `uv add` per RESEARCH §Installation (sqlalchemy==2.0.50, alembic==1.18.4, "psycopg[binary]==3.3.4", pgvector==0.4.2, pydantic==2.13.4, python-dotenv==1.2.2, structlog==26.1.0, supabase==2.31.0; dev: ruff, pytest). Add a `[tool.pytest.ini_options]` block to `pyproject.toml` setting `testpaths = ["tests"]`. Create `.gitignore` that ignores `.env` (and `.venv/`, `__pycache__/`).
    Correct the stale Hermes version pin in `CLAUDE.md`: the live runtime is v0.16.0 (Agent v0.16.0 / 2026.6.5, per RESEARCH §Standard Stack + §State of the Art), but CLAUDE.md still reads v0.15.2 in three places (the Stack table runtime row, the Version Compatibility row, and the release-source line) and the Phase-1 plans rely on v0.16.0 behaviors. Update every `v0.15.2` occurrence in CLAUDE.md to `v0.16.0` (keep the surrounding prose accurate — e.g. the desktop-preview date/source reference for v0.16.0). Do not change any other CLAUDE.md content.
    Create the six Wave 0 test files with the EXACT function names listed in the VALIDATION.md verification map. `tests/conftest.py` provides a Postgres fixture (disposable schema / test DB) used by the schema and config-seed integration tests — read the DB URL from `.env` (`SUPABASE_DB_URL`, direct connection). For tests whose target artifacts are created by later plans (schema, config rows, deployed context), write the assertion logic now but guard with `pytest.importorskip`/skip-if-missing so the suite is green at this plan's end and turns red→green as 01-02/01-03/01-04 land. `test_env.py::test_seller_id_in_env_not_code` must assert NOW: `AMAZON_CA_SELLER_ID` is set in the loaded env AND the literal UUID `5692b95f` does not appear anywhere under tracked source (`src/`, `db/`, `tests/`).
    If `uv` is not on PATH, install it first (RESEARCH §Environment Availability flags this); fall back to pip+venv only if install fails, and note the deviation in the SUMMARY.
  </action>
  <verify>
    <automated>uv run pytest -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `[tool.pytest.ini_options]`
    - `uv run pytest -x -q` exits 0 (skips allowed for not-yet-built artifacts; no errors)
    - All six files exist: tests/conftest.py, tests/test_env.py, tests/test_schema.py, tests/test_config_seed.py, tests/test_context.py and contain the exact function names from VALIDATION.md §Per-Task Verification Map
    - `.gitignore` contains a line matching `^\.env$`
    - Hermes version corrected in CLAUDE.md: `grep -c "v0.15.2" CLAUDE.md` returns 0, and the Runtime/Stack line reads v0.16.0
  </acceptance_criteria>
  <done>uv project initialized with locked pins; pytest runs green; all 6 Wave 0 test stubs exist with the VALIDATION.md function names; `.env` is gitignored; CLAUDE.md Hermes pin corrected to v0.16.0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Verify MCP connections read-only + cache seller UUID (CONN-01, CONN-02)</name>
  <files>.env.example, .env</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (§Pitfall 5 — config.yaml `servers:` reads empty, wiring is an execution gate; §Code Examples — the `hermes mcp list`/`test` + UUID-cache commands; Assumption A1 — confirm the direct vs pooled Supabase connection strings from the Supabase project settings; Assumption A2 — resolve the actual registered server names from `hermes mcp list`)
    - docs/04-data-tools-reference.md (DataDoe is read-only; its tool surface is sellers_and_vendors_list / exports_sources_get / exports_create / exports_get / exports_raw_download — NO write tool)
    - CLAUDE.md (Hard rule 1: data layer READ-ONLY; secrets in `.env` only — never inline the seller UUID)
  </read_first>
  <what-built>
    Automated up to this gate: ran `hermes mcp list`, and if DataDoe/Supabase/Gmail-Drive servers were absent, wired them via `hermes mcp add`/`install`. Ran `hermes mcp test <name>` for each. Confirmed DataDoe read-only by enumerating its tool surface (no write/mutate tool exists — confirmed by inspection, NOT by attempting a write). Resolved the amazon.ca seller UUID via `sellers_and_vendors_list`, wrote `AMAZON_CA_SELLER_ID=` to the gitignored `.env`, and added `AMAZON_CA_SELLER_ID=` + `SUPABASE_DB_URL=` + `SUPABASE_DB_URL_POOLED=` placeholders to the committed `.env.example`. Confirmed from the Supabase project Settings > Database page which connection string is the direct (5432) one and which is the pooled (6543) one, and set `SUPABASE_DB_URL` (direct) + `SUPABASE_DB_URL_POOLED` (pooled) in `.env` accordingly.
  </what-built>
  <how-to-verify>
    1. In the Hermes desktop workspace (or CLI), run `hermes mcp list` — confirm DataDoe, Supabase, and Gmail/Drive appear as wired servers.
    2. Run `hermes mcp test datadoe`, `hermes mcp test supabase`, and `hermes mcp test <gmail-server-name>` — each must round-trip a tool call and respond.
    3. Confirm the executor captured the transcripts and that DataDoe's enumerated tool list contains NO write/mutate tool.
    4. Confirm `.env` (gitignored) contains `AMAZON_CA_SELLER_ID` and that the resolved value matches the amazon.ca seller.
    5. Confirm `.env` carries `SUPABASE_DB_URL` (direct/5432) and `SUPABASE_DB_URL_POOLED` (pooled/6543) and that which-is-which was read from the Supabase project Settings > Database page, not assumed.
  </how-to-verify>
  <acceptance_criteria>
    - `hermes mcp test datadoe|supabase|<gmail>` transcripts captured in the SUMMARY, each showing a successful response
    - DataDoe tool enumeration shows no write/mutate capability
    - `.env` contains `AMAZON_CA_SELLER_ID=<uuid>`
    - `.env.example` contains `AMAZON_CA_SELLER_ID=` (no value), `SUPABASE_DB_URL=`, `SUPABASE_DB_URL_POOLED=`
    - `SUPABASE_DB_URL` is the DIRECT (5432) connection string and `SUPABASE_DB_URL_POOLED` is the pooled (6543) string, both confirmed from the Supabase project Settings > Database page (not assumed)
    - `grep -rn '5692b95f' src/ db/ tests/` returns nothing (UUID not inlined)
  </acceptance_criteria>
  <resume-signal>Type "approved" once all three connections respond, DataDoe is confirmed read-only, and the direct/pooled Supabase strings are confirmed — or describe what failed.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Produce empirical DataDoe source map (CONN-03)</name>
  <files>docs/datadoe-source-map.md</files>
  <read_first>
    - docs/04-data-tools-reference.md (§Confirmed sources — the source IDs to enumerate: Profit by SKU & Date (PREMIUM), Search Term Performance, Ad Performance by Campaign & Date, Ad Campaigns raw, etc.; the `exports_sources_get(query, sellerOrVendorIds)` call signature)
    - .planning/phases/01-foundation/01-RESEARCH.md (§Pitfall 1 — the map MUST be empirical, never guessed; §Open Questions (RESOLVED) Q4 — confirm premium Profit-by-SKU enablement; Assumption A5)
  </read_first>
  <what-built>
    Automated up to this gate: called `exports_sources_get` against the live DataDoe MCP (using the cached `AMAZON_CA_SELLER_ID`) for each confirmed source and wrote `docs/datadoe-source-map.md`. For each confirmed source the map records: the columns returned, the ad-program scope (which of SP/SB/SD it covers), the date-column timezone basis, and whether premium "Profit by SKU & Date" is enabled on the plan (and its SKU coverage if determinable). No export was pulled — this is source enumeration only (the first live export pull is Phase 2 / DATA-05, deliberately deferred).
  </what-built>
  <how-to-verify>
    1. Open `docs/datadoe-source-map.md`. Confirm every confirmed source from docs/04 has a row with columns + SP/SB/SD scope + timezone basis + (for the profit source) premium enablement status.
    2. Confirm the premium "Profit by SKU & Date" enablement line is filled from the live call, not left as a guess/TODO. If premium is disabled or has gaps, confirm the map says so explicitly (this feeds the Phase-2 fallback decision).
    3. Confirm no values look invented (e.g. column lists match what `exports_sources_get` actually returned).
  </how-to-verify>
  <acceptance_criteria>
    - `docs/datadoe-source-map.md` lists each confirmed source with: columns, SP/SB/SD scope, date-column timezone basis, and premium-enablement status
    - The file contains the string "Profit by SKU" with an explicit enabled/disabled determination
    - No "TODO"/"guess"/placeholder column lists remain
  </acceptance_criteria>
  <resume-signal>Type "approved" once the source map is complete and empirically grounded, or note what is missing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo working tree → git remote | Secrets (seller UUID, Supabase URL/keys) must not cross into committed files |
| Python helpers → DataDoe MCP | Read-only boundary; no write tool may exist or be assumed |
| local dev → PyPI | Dependency download — supply-chain surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information disclosure | seller UUID / Supabase creds | mitigate | `.env` gitignored (Task 1); `.env.example` carries empty placeholders only; `test_env.py` greps tracked source for the literal UUID and fails if present |
| T-01-02 | Elevation of privilege | DataDoe MCP scope | mitigate | Confirm read-only by enumerating the tool surface (Task 2); no write/mutate tool exists; never build an SP-API/Ads client (CLAUDE.md hard rules 1-3) |
| T-01-03 | Spoofing of truth | DataDoe source columns | mitigate | Source map produced empirically via `exports_sources_get` (Task 3), never guessed — prevents schema built on fabricated columns |
| T-01-SC | Tampering | uv/PyPI installs | mitigate | All deps are CLAUDE.md-locked pins, PyPI-verified in RESEARCH §Package Legitimacy Audit; run `hermes security` (OSV.dev) against the venv after `uv add` as the install-time gate (slopcheck unavailable) |
</threat_model>

<verification>
- `uv run pytest -x -q` exits 0
- `grep -c "v0.15.2" CLAUDE.md` returns 0 (Hermes pin corrected to v0.16.0)
- `hermes mcp test` transcripts captured for DataDoe, Supabase, Gmail/Drive
- `.env` carries `AMAZON_CA_SELLER_ID`, `SUPABASE_DB_URL` (direct), `SUPABASE_DB_URL_POOLED` (pooled); `grep -rn '5692b95f' src/ db/ tests/` returns nothing
- `docs/datadoe-source-map.md` complete with premium enablement determined
</verification>

<success_criteria>
- uv project + locked deps + pytest config exist; all 6 Wave 0 test stubs present and green/skipped
- CLAUDE.md Hermes pin corrected to v0.16.0
- Three MCP connections verified responding; DataDoe confirmed read-only
- Seller UUID cached to `.env`, never inlined; direct/pooled Supabase strings confirmed; `.env.example` committed
- Empirical DataDoe source map recorded with premium Profit-by-SKU enablement status
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done.
</output>
