---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .hermes-context/SOUL.md
  - .hermes-context/business-context.md
  - scripts/backup.sh
  - docs/backup-policy.md
autonomous: false
requirements: [CTX-01, CTX-02, CTX-03]
must_haves:
  truths:
    - "A version-controlled SOUL.md persona exists (numbers-first, terse, recommends-never-acts, never invents pricing/thresholds) and is deployed to ~/.hermes/SOUL.md, replacing the stub"
    - "A version-controlled business-context.md (business, ~30 SKUs, guardrails, FX policy, marketplace scope) exists and is deployed into ~/.hermes"
    - "The ~/.hermes + Supabase backup target and cadence are decided, documented, and the native hermes backup restore path is tested once"
  artifacts:
    - path: ".hermes-context/SOUL.md"
      provides: "Operator persona — canonical git copy"
      contains: "recommend"
    - path: ".hermes-context/business-context.md"
      provides: "Business + SKU + guardrail context — canonical git copy"
    - path: "scripts/backup.sh"
      provides: "On-open backup: hermes backup --quick + pg_dump"
      contains: "hermes backup"
    - path: "docs/backup-policy.md"
      provides: "Documented backup target + cadence + tested restore"
  key_links:
    - from: ".hermes-context/SOUL.md"
      to: "~/.hermes/SOUL.md"
      via: "deploy/copy step replaces the template stub"
      pattern: "SOUL"
    - from: "scripts/backup.sh"
      to: "~/.hermes + Supabase"
      via: "hermes backup --quick (zip) + pg_dump of the truth store"
      pattern: "pg_dump"
---

<objective>
Replace the `~/.hermes` SOUL.md stub with the real operator persona, load the Habib business context, keep both version-controlled in `.hermes-context/`, and decide + document + test the backup approach for `~/.hermes` and Supabase.

Purpose: The persona enforces the anti-fabrication, recommends-never-acts voice at the runtime layer; the business context gives the agent the ~30-SKU catalog and guardrails it reasons over; the backup makes the (otherwise loss-prone) `~/.hermes` recoverable. This track is independent of the DataDoe/schema work and runs in parallel.
Output: `.hermes-context/SOUL.md`, `.hermes-context/business-context.md`, `scripts/backup.sh`, `docs/backup-policy.md`, plus the deployed `~/.hermes` copies.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md

<interfaces>
<!-- Source of truth = git (.hermes-context/), runtime = ~/.hermes (RESEARCH §Architectural Responsibility Map). -->
<!-- Live state (RESEARCH §Runtime State Inventory): ~/.hermes/SOUL.md is a TEMPLATE STUB, memories/ is empty.
     So CTX-01/CTX-02 are genuinely unbuilt — this plan creates them, then deploys. -->

Persona traits to encode in SOUL.md (REQUIREMENTS CTX-02 + STATE.md §Key Decisions / Todos):
  - numbers-first, terse
  - recommends, never acts (proposal voice; never "I negated…"/"I changed…")
  - never invents pricing or thresholds — thresholds read from Supabase `config`, missing → refusal
  - cites provenance on every number; states program scope (don't sum SP/SB/SD)

Business context to encode (CTX-01):
  - Habib Distribution / Anabtawi Sweets, ~30 SKUs on amazon.ca FBA, US FBA expansion underway
  - guardrails: data layer read-only, no autonomous Amazon writes, money logic hand-written
  - FX policy: supplier invoices USD, CA settles CAD (mirrors cogs SCD+FX)
  - marketplace scope: amazon.ca first; amazon.com US-ready in schema

<!-- Backup (RESEARCH §Don't Hand-Roll + §Code Examples + §Pitfall 4): NATIVE, on-open, NOT clock-cron. -->
hermes backup --quick   (config/state/.env/auth/cron — fast)
hermes backup -o <zip>  (full: skills, sessions, data)
pg_dump "$SUPABASE_DB_URL" > <sql>   (the Supabase truth store)
hermes import <zip>     (restore path — test once)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author + deploy SOUL.md persona and business context (CTX-01, CTX-02)</name>
  <files>.hermes-context/SOUL.md, .hermes-context/business-context.md</files>
  <read_first>
    - CLAUDE.md (§Project / §Hard rules — the binding voice: recommends-never-acts, never invents thresholds, read-only; the ~30-SKU Anabtawi Sweets / amazon.ca business framing)
    - .planning/STATE.md (§Key Decisions + §Todos/Watch-fors — the exact guardrails: program-scope labelling, recommendation voice, missing-threshold refusal)
    - .planning/phases/01-foundation/01-RESEARCH.md (§Runtime State Inventory — SOUL.md is a stub, memories/ empty; §Pattern 5 if any skill frontmatter is touched)
    - ~/.hermes/SOUL.md (the current template stub being replaced — read it so the deployed file is a clean replacement, not an append)
    - tests/test_context.py (the assertion this content must satisfy: non-stub SOUL.md + business-context.md, both deployed to ~/.hermes — created in plan 01-01)
  </read_first>
  <action>
    Write `.hermes-context/SOUL.md` encoding the operator persona traits from the interfaces block (numbers-first, terse, recommends-never-acts, never invents pricing/thresholds, cites provenance, states program scope). It must NOT be the template stub. Write `.hermes-context/business-context.md` encoding the business/SKU/guardrail/FX/marketplace context. Then DEPLOY both into `~/.hermes`: copy `SOUL.md` to `~/.hermes/SOUL.md` (replacing the stub) and place `business-context.md` where Hermes loads context (e.g. `~/.hermes/memories/` or the context location confirmed from the live install — verify the exact path at execution time, RESEARCH flagged `memories/` empty). Keep git as the source of truth; the `~/.hermes` copies are deployments.
    Flip the skip guard on `tests/test_context.py::test_soul_and_context_deployed` so it asserts the deployed, non-stub files exist.
  </action>
  <verify>
    <automated>uv run pytest tests/test_context.py::test_soul_and_context_deployed -x</automated>
  </verify>
  <acceptance_criteria>
    - `.hermes-context/SOUL.md` exists, is non-stub, and contains the recommends-never-acts + never-invent-thresholds voice (e.g. contains "recommend" and a no-invented-thresholds clause)
    - `.hermes-context/business-context.md` exists and names Anabtawi Sweets / amazon.ca / ~30 SKUs + the read-only guardrail
    - `~/.hermes/SOUL.md` matches the git copy (stub replaced)
    - `uv run pytest tests/test_context.py::test_soul_and_context_deployed -x` exits 0
  </acceptance_criteria>
  <done>Persona + business context authored in git, deployed to ~/.hermes (stub replaced), and the context test passes.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Decide + document + test the backup approach (CTX-03)</name>
  <files>scripts/backup.sh, docs/backup-policy.md</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (§Don't Hand-Roll — use native `hermes backup`/`import`, not a custom tar; §Code Examples backup commands; §Pitfall 4 — on-open NOT clock-cron, Mac-awake constraint; §Open Questions Q3 — target + cadence undecided)
    - CLAUDE.md (Runtime: Hermes Mac desktop, Mac-awake only, no assumed unattended cron; back up ~/.hermes with Time Machine + explicit copy)
  </read_first>
  <decision>Where do the `~/.hermes` zip + Supabase dump land, and on what trigger?</decision>
  <context>
    The runtime is the Hermes Mac desktop app — it runs only while the Mac is awake, so clock-cron is unreliable. RESEARCH recommends native `hermes backup --quick` + `pg_dump`, triggered on-open/on-demand, with Time Machine as a disk-level belt-and-suspenders. The operator must choose the target directory and cadence; the executor has already drafted `scripts/backup.sh` (hermes backup --quick + pg_dump of `$SUPABASE_DB_URL`) and `docs/backup-policy.md`, and will test one restore (`hermes import`) before this is considered done.
  </context>
  <options>
    <option id="local-plus-timemachine">
      <name>Local ~/Backups/ + Time Machine, run on-open</name>
      <pros>Zero extra service; Time Machine already versions the disk; simplest to test-restore</pros>
      <cons>Single-machine — an offline copy (external disk/Drive) is a manual add-on</cons>
    </option>
    <option id="local-plus-drive">
      <name>Local ~/Backups/ + push to the connected Google Drive, run on-open</name>
      <pros>Off-machine copy survives disk loss; uses the already-wired Drive MCP</pros>
      <cons>More moving parts; Drive push must itself be reliable on-open</cons>
    </option>
  </options>
  <acceptance_criteria>
    - `scripts/backup.sh` contains `hermes backup` (or `hermes backup --quick`) AND `pg_dump`
    - `docs/backup-policy.md` states the chosen target directory, the on-open (not clock-cron) trigger, and the tested restore step
    - One `hermes import` restore was exercised and the result noted in the SUMMARY
  </acceptance_criteria>
  <resume-signal>Select: local-plus-timemachine or local-plus-drive (or specify another target/cadence).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git (.hermes-context/) → ~/.hermes runtime | Deploy boundary; the runtime persona must match the reviewed git copy |
| ~/.hermes → backup target | The backup zip carries `.env`/auth — its destination is a secrets boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Repudiation / availability | ~/.hermes loss | mitigate | Native `hermes backup` + `pg_dump`, on-open; tested `hermes import` restore before relying on it (Pitfall 4) |
| T-03-02 | Information disclosure | backup zip contains .env/auth | mitigate | Backup target documented as a non-shared local/owned location; do not push the full secret-bearing zip to a third party without noting the exposure in backup-policy.md |
| T-03-03 | Spoofing of truth | persona drift | mitigate | SOUL.md encodes never-invent-thresholds + recommends-never-acts; git is the reviewable source of truth, ~/.hermes is a deployment of it |
</threat_model>

<verification>
- `uv run pytest tests/test_context.py::test_soul_and_context_deployed -x` exits 0
- `~/.hermes/SOUL.md` is the non-stub persona (matches the git copy)
- `scripts/backup.sh` contains `hermes backup` + `pg_dump`; `docs/backup-policy.md` documents target + on-open cadence + tested restore
</verification>

<success_criteria>
- SOUL.md persona (numbers-first, terse, recommends-never-acts, never invents thresholds) version-controlled + deployed, stub replaced
- Business context version-controlled + deployed into ~/.hermes
- Backup target + cadence decided, documented, and restore tested once — on-open, not clock-cron
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done.
</output>
