---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - db/migrations/versions/0002_seed_config.py
autonomous: false
requirements: [DATA-04]
must_haves:
  truths:
    - "The config table is non-empty after the seed migration runs against live Supabase"
    - "config carries the operator's REAL thresholds — min margin %, ACOS ceiling, days-of-cover — keyed by (key, marketplace='amazon.ca')"
    - "No placeholder/guessed threshold values remain (empty/placeholder config is forbidden)"
  artifacts:
    - path: "db/migrations/versions/0002_seed_config.py"
      provides: "Reviewable Alembic data migration seeding real thresholds"
      contains: "min_margin_pct"
  key_links:
    - from: "0002_seed_config.py"
      to: "config"
      via: "INSERT of (key, marketplace, value) rows for the three thresholds"
      pattern: "acos_ceiling"
---

<objective>
Seed the `config` table with the operator's REAL thresholds via a reviewable Alembic data migration, then push it and verify the table is non-empty with the required keys.

Purpose: An empty `config` is a fabrication invitation — the agent, lacking a real threshold to read, would invent one (the cardinal sin of this project). Seeding real, operator-supplied values keyed `(key, marketplace)` is the data-integrity counterpart to the provenance schema. This must be the operator's actual numbers, not guesses.
Output: `db/migrations/versions/0002_seed_config.py` + a seeded live `config` table.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md

<interfaces>
<!-- config schema (from plan 01-02 / ARCHITECTURE.md): PRIMARY KEY (key, marketplace); value jsonb NOT NULL;
     marketplace DEFAULT 'amazon.ca'. -->
<!-- Required seed keys (REQUIREMENTS DATA-04 + RESEARCH §Open Questions Q2 + §Pitfall 2): -->
  min_margin_pct          — operator's minimum acceptable margin %
  acos_ceiling            — operator's ACOS ceiling
  days_of_cover_trigger   — operator's days-of-cover restock trigger
<!-- All keyed (key, 'amazon.ca'). Values are REAL operator-supplied numbers — blocking input, NOT guesses. -->
<!-- This is a reviewed Alembic DATA migration (RESEARCH §Pitfall 2), NOT a dashboard insert. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Collect the operator's REAL threshold values (DATA-04)</name>
  <files>db/migrations/versions/0002_seed_config.py</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (§Pitfall 2 — empty config forbidden, real values are blocking; §Open Questions Q2 — the three numbers are operator input)
    - CLAUDE.md (Money-touching logic is hand-written + reviewed; thresholds read from `config`, never invented; missing threshold → refusal)
    - .planning/research/ARCHITECTURE.md (§config table — keyed (key, marketplace), value jsonb)
  </read_first>
  <decision>What are the operator's real min margin %, ACOS ceiling, and days-of-cover trigger for amazon.ca?</decision>
  <context>
    These three thresholds gate every spend/margin decision in later phases. They MUST be the operator's actual numbers — the project treats an empty or placeholder config as a fabrication invitation. The executor will write them into a reviewable Alembic data migration (`0002_seed_config.py`) keyed `(key, 'amazon.ca')`; nothing is inferred or defaulted.
  </context>
  <options>
    <option id="provide-now">
      <name>Operator provides all three values now</name>
      <pros>Unblocks the seed migration immediately; config is real at phase end</pros>
      <cons>Requires the operator to have the numbers ready</cons>
    </option>
    <option id="provide-partial">
      <name>Operator provides what they have; flag any unknown as a hard blocker (not a default)</name>
      <pros>Honest about what's known; never silently defaults a missing threshold</pros>
      <cons>Phase 1 cannot close DATA-04 until all three are supplied</cons>
    </option>
  </options>
  <acceptance_criteria>
    - All three values (min_margin_pct, acos_ceiling, days_of_cover_trigger) are captured as concrete operator-supplied numbers (no placeholders/defaults)
    - The values are recorded in the SUMMARY alongside the seed migration
  </acceptance_criteria>
  <resume-signal>Provide the three values (min margin %, ACOS ceiling, days-of-cover), or state which are unknown so they remain a blocker rather than a default.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Author seed migration, push it, and verify config non-empty (DATA-04)</name>
  <files>db/migrations/versions/0002_seed_config.py, tests/test_config_seed.py</files>
  <read_first>
    - db/migrations/versions/0001_enable_vector_and_core_tables.py (the config table definition this migration seeds — from plan 01-02)
    - tests/test_config_seed.py (the stub from plan 01-01: test_required_thresholds_seeded — flip its skip guard)
    - .planning/phases/01-foundation/01-RESEARCH.md (§Pattern 4 — push on the DIRECT connection; §Code Examples)
  </read_first>
  <action>
    Author `0002_seed_config.py` as an Alembic data migration inserting the three operator-supplied rows into `config` — `(min_margin_pct, 'amazon.ca', <value>)`, `(acos_ceiling, 'amazon.ca', <value>)`, `(days_of_cover_trigger, 'amazon.ca', <value>)` — with `value` as JSONB and a `description` per row; downgrade deletes those three keys. Use the REAL values captured in Task 1; do NOT write placeholders.
    Push with `uv run alembic upgrade head` against the DIRECT `SUPABASE_DB_URL`. Then flip the skip guard on `tests/test_config_seed.py::test_required_thresholds_seeded` so it asserts the config table is non-empty AND the three required keys exist for `marketplace='amazon.ca'`.
    If live credentials cannot be supplied non-interactively, the operator runs the upgrade and pastes the result (this plan is non-autonomous).
  </action>
  <verify>
    <automated>uv run pytest tests/test_config_seed.py::test_required_thresholds_seeded -x</automated>
  </verify>
  <acceptance_criteria>
    - `db/migrations/versions/0002_seed_config.py` contains `min_margin_pct`, `acos_ceiling`, and `days_of_cover_trigger`
    - `uv run alembic upgrade head` exits 0 against live Supabase; `alembic current` shows 0002 applied
    - `uv run pytest tests/test_config_seed.py::test_required_thresholds_seeded -x` exits 0 (config non-empty, three keys present for amazon.ca)
    - No placeholder/guess values remain in the migration
  </acceptance_criteria>
  <done>config seeded with the operator's real thresholds via a reviewed Alembic data migration, pushed live, and the seed test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator input → config seed migration | Real thresholds cross into the reviewed migration; placeholders forbidden |
| seed migration → live config table | The numbers every later spend decision reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing of truth | empty/placeholder config | mitigate | Real operator values are a BLOCKING input (Task 1); migration forbidden to carry placeholders; test asserts non-empty + required keys (Pitfall 2) |
| T-04-02 | Tampering | config change path | mitigate | Seed is a reviewable Alembic data migration in git, not a dashboard insert (CLAUDE.md) |
| T-04-03 | Repudiation | which threshold was applied | mitigate | Each row carries a `description`; later phases cite the config row as provenance (trust spine, Phase 2) |
</threat_model>

<verification>
- `uv run alembic current` shows 0002 applied
- `uv run pytest tests/test_config_seed.py::test_required_thresholds_seeded -x` exits 0
- `db/migrations/versions/0002_seed_config.py` carries the three real keys, no placeholders
</verification>

<success_criteria>
- config seeded with the operator's REAL min margin %, ACOS ceiling, and days-of-cover, keyed (key, 'amazon.ca')
- Reviewed Alembic data migration (not dashboard); pushed to live Supabase
- Seed test green; config provably non-empty
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` when done.
</output>
