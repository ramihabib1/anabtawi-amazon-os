---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - db/models.py
  - db/alembic.ini
  - db/migrations/env.py
  - db/migrations/versions/0001_enable_vector_and_core_tables.py
autonomous: false
requirements: [DATA-01, DATA-02, DATA-03]
must_haves:
  truths:
    - "The five tables sku_master, cogs, metric_snapshots, decision_ledger, config exist in the live Supabase database after alembic upgrade head"
    - "The pgvector extension is enabled (CREATE EXTENSION runs as the first statement of the first migration, before any vector column)"
    - "metric_snapshots is unique on (marketplace, seller_sku, snapshot_date) and carries captured_at + datadoe_export_id + margin_source"
    - "cogs is an SCD with per-row FX (effective_from/to, unit_cost, cost_currency, fx_rate, fx_as_of, marketplace_currency), unique on (marketplace, seller_sku, effective_from)"
    - "marketplace is a first-class column on every fact and dimension"
  artifacts:
    - path: "db/models.py"
      provides: "SQLAlchemy 2.0 declarative models for the five tables"
      contains: "class"
    - path: "db/migrations/versions/0001_enable_vector_and_core_tables.py"
      provides: "First migration: vector extension + five tables + indexes"
      contains: "CREATE EXTENSION IF NOT EXISTS vector"
    - path: "db/migrations/env.py"
      provides: "Alembic env reading the DIRECT Supabase URL from .env"
  key_links:
    - from: "db/migrations/env.py"
      to: ".env (SUPABASE_DB_URL direct)"
      via: "migrations run on the direct (5432) connection, not the pooled one"
      pattern: "SUPABASE_DB_URL"
    - from: "0001_enable_vector_and_core_tables.py"
      to: "metric_snapshots"
      via: "UniqueConstraint on the idempotency grain key"
      pattern: "marketplace.*seller_sku.*snapshot_date"
---

<objective>
Define the five-table Supabase truth store as reviewable Alembic migrations (SQLAlchemy 2.0 models), push the migration against the LIVE Supabase database, and verify the schema by introspection.

Purpose: This is the numeric source of truth every later phase persists to. The schema must be exactly as the locked decisions specify (provenance-carrying `metric_snapshots`, FX-aware SCD `cogs`, pgvector enabled) — and the live DB must actually hold the tables, not just the migration files.
Output: `db/models.py`, the Alembic project under `db/`, the first migration, and a migrated live database.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
@docs/datadoe-source-map.md

<interfaces>
<!-- Locked schema (ARCHITECTURE.md §Supabase Schema lines 187-277). Reproduce EXACTLY, with ONE correction below. -->
<!-- CORRECTION (RESEARCH §Pattern 3, line 212): ARCHITECTURE.md names the metric_snapshots timestamp `ingested_at`.
     REQUIREMENTS DATA-02 + STATE.md mandate `captured_at`. USE `captured_at` — do NOT use `ingested_at`. -->

Five tables (all carry `marketplace text NOT NULL`):
  sku_master       — UNIQUE (marketplace, seller_sku)
  cogs             — SCD: effective_from/effective_to, unit_cost numeric(12,4), cost_currency, fx_rate numeric(12,6),
                     fx_as_of, marketplace_currency; UNIQUE (marketplace, seller_sku, effective_from)
  metric_snapshots — units/gross_sales/net_profit/total_cost/fba_fees/ad_spend/ad_sales/acos/tacos/roi/page_views/buybox_pct,
                     margin_source text NOT NULL ('datadoe_profit'|'cogs_fallback'), datadoe_export_id text,
                     captured_at timestamptz DEFAULT now();  UNIQUE (marketplace, seller_sku, snapshot_date);
                     INDEX (marketplace, snapshot_date); INDEX (seller_sku, snapshot_date)
  decision_ledger  — kind/subject_sku/summary/payload jsonb/proposed_at/approved_by/approved_at/approval_token/
                     executed_at/outcome (write/approval cols stay NULL in M1 — read-only)
  config           — key text, marketplace text DEFAULT 'amazon.ca', value jsonb NOT NULL, description, updated_at;
                     PRIMARY KEY (key, marketplace)

<!-- Migration ordering (RESEARCH §Pattern 1, §Pitfall 3): -->
First statement of 0001 = op.execute("CREATE EXTENSION IF NOT EXISTS vector")  BEFORE any table.
<!-- Phase 1 enables the extension but does NOT need a vector(dim) column yet (RESEARCH Assumption A4). -->

<!-- Connection (RESEARCH §Pattern 4, Assumption A1): migrations on the DIRECT (5432) connection = SUPABASE_DB_URL.
     Runtime/pooled (6543) = SUPABASE_DB_URL_POOLED, used by later phases, not here. Verify both strings exist in .env. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author SQLAlchemy models + Alembic project + first migration</name>
  <files>db/models.py, db/alembic.ini, db/migrations/env.py, db/migrations/versions/0001_enable_vector_and_core_tables.py</files>
  <read_first>
    - .planning/research/ARCHITECTURE.md (§Supabase Schema lines 187-277 — the verbatim DDL for all five tables and their constraints/indexes; §Schema decisions for the rationale)
    - .planning/phases/01-foundation/01-RESEARCH.md (§Pattern 1 extension-first ordering; §Pattern 2 cogs SCD+FX; §Pattern 3 metric_snapshots key + the `captured_at` vs `ingested_at` correction; §Pattern 4 direct vs pooled; §Code Examples `alembic init` ordering)
    - tests/test_schema.py (the three assertions this schema must satisfy: test_tables_and_vector_ext, test_snapshot_idempotency_key, test_cogs_scd_fx — created in plan 01-01)
    - docs/datadoe-source-map.md (keep metric_snapshots columns close to the Profit-by-SKU output to minimize transformation — RESEARCH §Pitfall 1)
  </read_first>
  <behavior>
    - test_tables_and_vector_ext: after `alembic upgrade head`, all five table names exist AND the `vector` extension is present in pg_extension
    - test_snapshot_idempotency_key: metric_snapshots has a UNIQUE constraint on (marketplace, seller_sku, snapshot_date) AND columns `captured_at`, `datadoe_export_id`, `margin_source` exist
    - test_cogs_scd_fx: cogs has columns effective_from, effective_to, unit_cost, cost_currency, fx_rate, fx_as_of, marketplace_currency AND UNIQUE (marketplace, seller_sku, effective_from)
  </behavior>
  <action>
    Scaffold Alembic under `db/` (`uv run alembic init db/migrations`); set `db/migrations/env.py` `target_metadata = Base.metadata` from `db/models.py` and read the DB URL from `.env` `SUPABASE_DB_URL` (the DIRECT connection). Register the pgvector type so autogenerate recognizes it.
    Write `db/models.py` as SQLAlchemy 2.0 declarative models for the five tables EXACTLY per the locked ARCHITECTURE.md DDL, applying the one correction: the metric_snapshots timestamp column is `captured_at` (not `ingested_at`). `marketplace` is NOT NULL on every table. Include every constraint and index named in the interfaces block.
    Author `0001_enable_vector_and_core_tables.py` whose FIRST upgrade statement is `op.execute("CREATE EXTENSION IF NOT EXISTS vector")`, then creates the five tables, the metric_snapshots unique constraint + two indexes, the cogs SCD unique constraint, the sku_master unique constraint, and the config composite primary key. Do NOT add a vector(dim) column in this migration (extension-only satisfies DATA-01; dimension choice is a later phase).
    Do not run the migration here — Task 2 is the blocking push.
    Deferred-verify intent (NOT a gap): this task's automated verify is deliberately the OFFLINE `uv run alembic upgrade head --sql` — it confirms the migration COMPILES and renders clean DDL with no DB write. The full live schema assertions for DATA-01/02/03 (`tests/test_schema.py::test_tables_and_vector_ext` / `::test_snapshot_idempotency_key` / `::test_cogs_scd_fx`) intentionally run in Task 3, AFTER the blocking live push in Task 2. The live push is a blocking human gate that cannot run mid-auto-task, so the introspection tests are sampled the moment the live DB exists — they are not skipped, only sequenced after the gate.
  </action>
  <verify>
    <automated>uv run alembic upgrade head --sql</automated>
  </verify>
  <acceptance_criteria>
    - `db/migrations/versions/0001_enable_vector_and_core_tables.py` first executable statement is `CREATE EXTENSION IF NOT EXISTS vector`
    - `uv run alembic upgrade head --sql` renders DDL for all five tables and exits 0 (offline SQL generation — no DB write). This offline render is the intended verify for THIS task; it proves the migration compiles. The live DATA-01/02/03 schema assertions in `tests/test_schema.py` deliberately run in Task 3 after the blocking live push in Task 2 — they are sequenced, not skipped.
    - `db/models.py` defines five models; metric_snapshots uses `captured_at` and NOT `ingested_at` (`grep -c ingested_at db/models.py` returns 0)
    - The rendered SQL contains a UNIQUE constraint matching `marketplace.*seller_sku.*snapshot_date`
  </acceptance_criteria>
  <done>Five-table schema authored as SQLAlchemy models + reviewable first migration; vector-extension-first ordering enforced; offline SQL renders clean; not yet pushed (live schema assertions deferred to Task 3 by design).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Push migration to live Supabase (alembic upgrade head)</name>
  <files>db/migrations/versions/0001_enable_vector_and_core_tables.py</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (§Pattern 4 + Assumption A1 — run on the DIRECT (5432) connection, NOT the pooled one; §Runtime State Inventory — verify the target Supabase schema is empty/known before first migration, reconcile any stale partial schema)
    - .env (SUPABASE_DB_URL must be the direct connection string; confirm it exists from plan 01-01 / set it now if missing)
  </read_first>
  <action>
    Confirm the target Supabase project is reachable and its schema is empty/known (RESEARCH §Runtime State Inventory — reconcile any pre-existing partial schema before migrating). Run `uv run alembic upgrade head` against the DIRECT `SUPABASE_DB_URL`. This task is flagged non-autonomous because it requires live Supabase credentials and writes to the live database; if the credentials cannot be supplied non-interactively, the operator runs the command and pastes the result. After it succeeds, confirm `alembic current` reports the 0001 revision as applied.
    This push is MANDATORY: migration files type-checking is not the same as the live DB holding tables. The DATA-01 test reads the live DB.
  </action>
  <verify>
    <automated>uv run alembic current</automated>
  </verify>
  <acceptance_criteria>
    - `uv run alembic upgrade head` exits 0 against the live DIRECT Supabase connection
    - `uv run alembic current` shows the 0001 revision applied
    - The vector extension and all five tables now exist in the live database
  </acceptance_criteria>
  <resume-signal>Type "approved" once `alembic upgrade head` has run successfully against live Supabase, or paste the error.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Verify live schema by introspection (DATA-01/02/03)</name>
  <files>tests/test_schema.py</files>
  <read_first>
    - tests/test_schema.py (the stubs created in plan 01-01 — flip any skip guards now that the live schema exists)
    - tests/conftest.py (the Postgres fixture; ensure it points at the migrated DB via SUPABASE_DB_URL)
    - .planning/phases/01-foundation/01-VALIDATION.md (§Per-Task Verification Map — the three exact test commands)
  </read_first>
  <action>
    Activate the three schema tests against the now-migrated live (or disposable-copy) database: remove any skip-if-missing guard so they assert for real. `test_tables_and_vector_ext` introspects for all five table names + the `vector` extension; `test_snapshot_idempotency_key` introspects the metric_snapshots unique constraint + presence of `captured_at`/`datadoe_export_id`/`margin_source`; `test_cogs_scd_fx` introspects the cogs SCD/FX columns + its unique constraint. Do not change the schema here — only the tests.
  </action>
  <verify>
    <automated>uv run pytest tests/test_schema.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_schema.py::test_tables_and_vector_ext -x` exits 0
    - `uv run pytest tests/test_schema.py::test_snapshot_idempotency_key -x` exits 0
    - `uv run pytest tests/test_schema.py::test_cogs_scd_fx -x` exits 0
  </acceptance_criteria>
  <done>All three schema integration tests pass against the live migrated database.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic migration → live Supabase | DDL crosses into the production truth store |
| Python helper → Supabase key | Key scope determines blast radius of a leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | schema change path | mitigate | All DDL via reviewable Alembic migrations in git; NEVER Supabase dashboard click-ops (CLAUDE.md + RESEARCH anti-pattern) |
| T-02-02 | Elevation of privilege | Supabase service-role key | mitigate | Use a least-privilege key for migrations; service-role only where strictly needed; never commit the key (.env only) |
| T-02-03 | Spoofing of truth | metric_snapshots provenance | mitigate | `datadoe_export_id` + `margin_source` + `captured_at` are NOT NULL/present so every future row is traceable — schema-level anti-fabrication control |
| T-02-04 | Denial of service / data loss | partial migration | mitigate | vector extension created FIRST (Pitfall 3) so the migration cannot fail mid-run on a missing extension; verify empty/known target schema before push (Task 2) |
</threat_model>

<verification>
- `uv run alembic upgrade head` applied to live Supabase; `alembic current` shows 0001
- `uv run pytest tests/test_schema.py -x -q` exits 0
- `grep -c ingested_at db/models.py` returns 0 (correction applied)
</verification>

<success_criteria>
- Five tables exist in the live Supabase DB via reviewed Alembic migration with pgvector enabled
- metric_snapshots unique on the grain key + carries captured_at/datadoe_export_id/margin_source
- cogs is an SCD with per-row FX, unique on (marketplace, seller_sku, effective_from)
- marketplace is first-class on every table
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done.
</output>
