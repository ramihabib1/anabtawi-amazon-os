---
phase: 06-find-lifecycle-substrate
plan: 04
type: execute
wave: 3
depends_on: ["06-01", "06-02", "06-03"]
files_modified:
  - .claude/skills/datadoe-query/SKILL.md
autonomous: false
requirements: [WRITE-01, WRITE-02, WRITE-05]
nyquist_compliant: true

must_haves:
  truths:
    - "The pinned ACTION_TERMINAL enum is confirmed against the LIVE DataDoe schema (actions_details_schema_get) and one live read-only actions_get — Success Criterion 4 live half"
    - "The reconcile loop's no-export discipline is confirmed: a live actions_get terminal status + a *_FIND echo is the ONLY confirmation path, never a next-day DataDoe export"
    - "The datadoe-query SKILL carries a write-action reference: the four actions_* tools, FIND-before-write, dryRun discipline, the pinned terminal enum, and reconcile-via-status-not-export"
    - "The full engine suite is green end-to-end (Plans 01+02+03 turned every red test green) before the phase gate"
  artifacts:
    - path: ".claude/skills/datadoe-query/SKILL.md"
      provides: "Write-action reference section (actions_*, FIND-before-write, dryRun, terminal enum, reconcile discipline)"
      contains: "actions_start"
      min_lines: 20
  key_links:
    - from: ".claude/skills/datadoe-query/SKILL.md"
      to: "engine/src/habibos/datadoe.py"
      via: "the SKILL's terminal-status list matches the pinned ACTION_TERMINAL set verbatim"
      pattern: "COMPLETED|PARTIALLY_COMPLETED|ERROR|CANCELED|BLOCKED"
    - from: ".claude/skills/datadoe-query/SKILL.md"
      to: "engine/scripts/classify_dryrun.py"
      via: "the SKILL points the agent at the classify CLIs for proceed/stop verdicts"
      pattern: "classify_dryrun|classify_poll"
---

<objective>
Close Phase 6's Success Criterion 4: confirm the pinned `actions_get` terminal-status enum
and response shape against the LIVE DataDoe schema (read-only — `actions_details_schema_get`
plus one historical `actions_get`), and add the write-action reference to the `datadoe-query`
SKILL so the operator/agent has the FIND-before-write + dryRun + reconcile-via-status playbook.
This is the Wave 3 capstone after the substrate (01) and the two classifier/cache cores
(02, 03) are green. ZERO real writes: no `actions_start dryRun:false` fires — only read-only
`actions_details_schema_get` / `actions_get`.

Purpose: The engine half of SC-4 (the pinned enum) lands in Plan 01; this plan supplies the
LIVE confirmation half (RESEARCH Open Question 1 / Assumption A1 — confirm the MCP wrapper
returns the REST `ActionStatusResponse` shape and the enum matches the pinned set) and the
operator-facing doc half. If the live schema diverges from the pinned set, that divergence is
surfaced here, before any later phase fires a real write.

Output: an updated `datadoe-query/SKILL.md` write-action reference; a verified-or-flagged
live enum confirmation captured at the checkpoint.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-find-lifecycle-substrate/06-RESEARCH.md
@.planning/phases/06-find-lifecycle-substrate/06-VALIDATION.md
@.planning/phases/06-find-lifecycle-substrate/06-01-SUMMARY.md
@.planning/phases/06-find-lifecycle-substrate/06-02-SUMMARY.md
@.planning/phases/06-find-lifecycle-substrate/06-03-SUMMARY.md
@.claude/skills/datadoe-query/SKILL.md

<interfaces>
<!-- Contracts established earlier this phase — the doc and confirmation reference these. -->

From engine/src/habibos/datadoe.py (pinned in Plan 01 — the enum the live schema must match):
  ACTION_IN_FLIGHT        = {"PENDING","IN_PROGRESS","WAITING_EXTERNAL_PROCESSING"}
  ACTION_TERMINAL_SUCCESS = {"COMPLETED"}
  ACTION_TERMINAL_PARTIAL = {"PARTIALLY_COMPLETED","COMPLETED_WITH_ISSUES"}
  ACTION_TERMINAL_FAIL    = {"ERROR","CANCELED","BLOCKED_NO_TOKENS","BLOCKED_INVALID_INPUT"}

From engine/scripts/ (built in Plan 02 — the seam the SKILL points the agent at):
  classify_dryrun.py  --artifact <dryRun response JSON>      -> proceed/stop JSON
  classify_poll.py    --artifact <actions_get response JSON> -> done/in_flight/refuse JSON

The four write-path MCP tools (RESEARCH §State of the Art, Sources):
  actions_start(type, sellerOrVendorId, dryRun, details)   # Phase 6 only ever dryRun:true
  actions_get(actionId)                                    # poll to terminal
  actions_list(...)                                        # enumerate prior actions
  actions_details_schema_get(...)                          # the read-only schema confirm (SC-4)

The full action type enum uses the AMAZON_ADS_ prefix (RESEARCH line 377), e.g.
  AMAZON_ADS_CAMPAIGNS_FIND / _UPDATE / _REMOVE / _ADD (one ad-product per FIND, maxItems:1).

Reconcile discipline (RESEARCH lines 207-208, WRITE-05): confirmation = actions_get->terminal
+ a *_FIND echo ONLY; NEVER a next-day DataDoe export (<=24h reporting lag).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the write-action reference section to datadoe-query/SKILL.md</name>
  <read_first>
    - .claude/skills/datadoe-query/SKILL.md (the file being modified — note its existing "## Hard rules" / "## The workflow (every pull)" structure and the "DataDoe is the read-only analytics layer" framing the new section must reconcile with)
    - engine/src/habibos/datadoe.py (the pinned ACTION_* sets — the SKILL's terminal list must match verbatim)
    - 06-RESEARCH.md §State of the Art (lines 368-377 — the four actions_* tools, the AMAZON_ADS_ type prefix, the 412/402/429 HTTP refusals), §"actions_get response — VERIFIED status enum" (329-344), Pitfall 4 (257-261)
    - 06-PATTERNS.md "datadoe-query/SKILL.md (MODIFIED)" reference
  </read_first>
  <action>
    Add a new section (e.g. `## Write actions — the gated actions_start path (PPC writes only)`)
    to .claude/skills/datadoe-query/SKILL.md. It must state: (1) the FOUR write-path tools —
    `actions_start`, `actions_get`, `actions_list`, `actions_details_schema_get` — and that the
    DataDoe data layer stays read-only for analysis; the only write surface is the gated
    `actions_start` path (CLAUDE.md hard rule 1). (2) The dryRun discipline: ALWAYS
    `actions_start dryRun:true` first; PROCEED only on `status=="VALIDATED"` + `validation.valid`
    + no issues; classification is done by the ENGINE, not in chat — point the agent at
    `engine/scripts/classify_dryrun.py --artifact <response.json>` and
    `classify_poll.py --artifact <response.json>`. (3) FIND-before-write: pull live
    `campaignId/adGroupId/targetId/adId` via the `*_FIND` tools (one ad-product per request,
    `adProductFilter.include` maxItems:1) and cache to `data/ads_<entity>_find_<date>.json`
    before any UPDATE/REMOVE; an uncached ID is refused at build. (4) The pinned terminal-status
    enum VERBATIM (`PENDING/IN_PROGRESS/WAITING_EXTERNAL_PROCESSING` in-flight; `COMPLETED`
    success; `PARTIALLY_COMPLETED/COMPLETED_WITH_ISSUES` done-with-issues; `ERROR/CANCELED/
    BLOCKED_NO_TOKENS/BLOCKED_INVALID_INPUT` failure) — note NO `FAILED` and `CANCELED` is one L.
    (5) Reconcile-via-status-not-export: confirm a write only via `actions_get`->terminal + a
    `*_FIND` echo, NEVER a next-day DataDoe export (<=24h lag). (6) The HTTP-level refusals the
    agent must NOT retry: 412 (action type disabled — org-enable in DataDoe Settings -> Actions),
    402 (no AI tokens); only 429 (rate limit, 2 req/s, honor `Retry-After`) is retryable.
    Use the full `AMAZON_ADS_`-prefixed type strings. Do NOT inline the seller UUID into the new
    section (the existing `sellerOrVendorId` header line already carries it once — do not duplicate).
  </action>
  <verify>
    <automated>cd /Users/mareekhalila/Documents/anabtawi-amazon-os && grep -q 'actions_start' .claude/skills/datadoe-query/SKILL.md && grep -q 'CANCELED' .claude/skills/datadoe-query/SKILL.md && grep -q 'classify_dryrun' .claude/skills/datadoe-query/SKILL.md && ! grep -qE 'CANCELLED|"FAILED"' .claude/skills/datadoe-query/SKILL.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The SKILL contains a write-action section: `grep -n "actions_start" .claude/skills/datadoe-query/SKILL.md` matches, and the section names all four tools (`actions_start`, `actions_get`, `actions_list`, `actions_details_schema_get`).
    - The terminal enum matches the pinned set verbatim: `grep -E "PARTIALLY_COMPLETED|COMPLETED_WITH_ISSUES|BLOCKED_NO_TOKENS|BLOCKED_INVALID_INPUT" .claude/skills/datadoe-query/SKILL.md` matches; `grep -E 'CANCELLED|"FAILED"' .claude/skills/datadoe-query/SKILL.md` returns nothing (no wrong roadmap names).
    - The SKILL points the agent at the engine classify CLIs: `grep -n "classify_dryrun\|classify_poll" .claude/skills/datadoe-query/SKILL.md` matches.
    - FIND-before-write + reconcile-not-via-export are documented: `grep -ni "find-before-write\|FIND" .claude/skills/datadoe-query/SKILL.md` and `grep -ni "never.*export\|not.*export" .claude/skills/datadoe-query/SKILL.md` both match.
    - No new UUID leak: `grep -c "5692b95f" .claude/skills/datadoe-query/SKILL.md` returns 1 (the pre-existing header line only — the new section adds none).
  </acceptance_criteria>
  <done>datadoe-query/SKILL.md has a write-action reference covering the four actions_* tools, FIND-before-write, dryRun discipline (pointing at the engine CLIs), the pinned terminal enum verbatim, reconcile-via-status-not-export, and the non-retryable HTTP refusals — adding no new UUID.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live read-only confirmation of the actions_get schema + terminal enum (Success Criterion 4)</name>
  <read_first>
    - engine/src/habibos/datadoe.py (the pinned ACTION_* sets being confirmed against the live schema)
    - 06-RESEARCH.md Open Question 1 (lines 391-394) and Assumption A1 (line 385); 06-VALIDATION.md "Manual-Only Verifications" (lines 70-72)
  </read_first>
  <action>
    Automate the read-only live confirmation BEFORE pausing for the human: the agent calls
    `actions_details_schema_get` for an `AMAZON_ADS_*_UPDATE` action type and reads its `status`
    enum, then `actions_list` to find any historical `actionId` and `actions_get(actionId)` on it
    (read-only — NO `actions_start dryRun:false`). The agent diffs the live status enum + response
    field names against the pinned `datadoe.ACTION_TERMINAL` set and the engine's pydantic model,
    then presents the match-or-divergence for human sign-off. Strip the seller UUID from any
    captured response before committing it as a fixture.
  </action>
  <what-built>
    The engine pins the verified `ACTION_TERMINAL` enum (Plan 01) and classifies dryRun/poll
    responses (Plan 02); the SKILL now documents the write path (Task 1). What remains is the
    LIVE half of Success Criterion 4 — confirming the DataDoe MCP `actions_get` wrapper returns
    the REST `ActionStatusResponse` shape and that its status enum matches the pinned set. This
    is read-only (no `actions_start dryRun:false`), so it cannot be unit-tested against a fixture
    and must run as a live agent MCP call.
  </what-built>
  <how-to-verify>
    1. The agent (NOT pytest) calls `actions_details_schema_get` for an `AMAZON_ADS_*_UPDATE`
       (or the broadest available) action type and reads the `status` enum from the returned
       schema.
    2. The agent calls `actions_list` to find any historical `actionId`, then `actions_get(actionId)`
       on it (read-only) and inspects the live response object's field names + `status` value.
    3. Compare the live status enum to the pinned set in `engine/src/habibos/datadoe.py`:
       `{PENDING, IN_PROGRESS, WAITING_EXTERNAL_PROCESSING, COMPLETED, PARTIALLY_COMPLETED,
       COMPLETED_WITH_ISSUES, ERROR, CANCELED, BLOCKED_NO_TOKENS, BLOCKED_INVALID_INPUT}`.
       Confirm there is NO `FAILED` and `CANCELED` is one L.
    4. Confirm the response field names match the engine's pydantic model (`status` top-level;
       error message under the error/result object) — i.e. the MCP did not rename/wrap the REST shape.
    5. If the live enum MATCHES: capture the confirming response (UUID-stripped) into
       `engine/tests/fixtures/actions_get_completed.json` (or a sibling) so the fixture is now a
       real capture, and note the confirmation date in the SUMMARY.
    6. If the live enum DIVERGES (an unmodeled status, a renamed field, or the MCP wraps the REST
       object): STOP — do not proceed to the phase gate. Re-pin `datadoe.ACTION_TERMINAL` from the
       live schema, update `test_action_terminal_enum_pinned`, and flag the divergence for the
       operator (this is exactly the drift the substrate was built to surface — RESEARCH Pitfall 5).
    Expected outcome: the live enum matches the pinned set; the MCP returns the REST shape verbatim.
  </how-to-verify>
  <resume-signal>Type "approved" if the live enum + shape match the pinned set, or describe the divergence (which status/field differs) so the enum is re-pinned before the phase gate.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live DataDoe schema -> pinned engine enum | The live enum may have drifted from the 2026-06-20 pin (private beta) |
| live actions_get response -> reconcile decision | A renamed/wrapped MCP field could make the classifier mis-read a real status |
| captured confirming response -> tracked fixture | A live capture could carry the seller UUID into a committed fixture |
| SKILL doc -> operator/agent behavior | A wrong terminal name in the doc could teach an infinite-poll or a wrong proceed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Tampering | pinned ACTION_TERMINAL vs live schema | mitigate | Task 2 confirms the live enum/shape against `actions_details_schema_get` + a live `actions_get`; a divergence STOPS the gate and re-pins the enum (RESEARCH Pitfall 5 / SC-4). |
| T-06-11 | Information Disclosure | a captured confirming response -> fixture | mitigate | Any live capture is UUID-stripped before committing; acceptance greps confirm `5692b95f` appears only in the pre-existing SKILL header line. |
| T-06-12 | Spoofing / Tampering | SKILL terminal-enum doc | mitigate | Task 1 pins the enum verbatim and forbids `CANCELLED`/`"FAILED"` in the doc; acceptance greps assert the wrong roadmap names are absent. |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (RESEARCH §Package Legitimacy Audit: zero new deps). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `grep -q "actions_start" .claude/skills/datadoe-query/SKILL.md` and the terminal enum + classify-CLI references are present; `grep -E 'CANCELLED|"FAILED"' .claude/skills/datadoe-query/SKILL.md` returns nothing.
- `cd engine && uv run pytest` is fully GREEN end-to-end (Plans 01+02+03 turned every red test green; test_lifecycle.py + test_find_cache.py + the pre-existing 16 all pass).
- The live read-only confirmation (Task 2) is approved OR its divergence is resolved by re-pinning the enum before the phase gate.
- `grep -c "5692b95f" .claude/skills/datadoe-query/SKILL.md` returns 1 (no new UUID added).
</verification>

<success_criteria>
- The `datadoe-query` SKILL carries the write-action reference (four actions_* tools, FIND-before-write, dryRun discipline via the engine CLIs, the pinned terminal enum verbatim, reconcile-via-status-not-export, non-retryable HTTP refusals).
- The pinned `ACTION_TERMINAL` enum + `actions_get` shape are confirmed against the live DataDoe schema (read-only), or any divergence is re-pinned before the gate.
- The full engine suite is green end-to-end.
</success_criteria>

<output>
Create `.planning/phases/06-find-lifecycle-substrate/06-04-SUMMARY.md` when done.
</output>
