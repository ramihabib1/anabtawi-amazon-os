---
phase: 06-find-lifecycle-substrate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/src/habibos/datadoe.py
  - engine/src/habibos/result.py
  - engine/tests/test_env.py
  - engine/tests/test_lifecycle.py
  - engine/tests/test_find_cache.py
  - engine/tests/fixtures/dryrun_validated.json
  - engine/tests/fixtures/dryrun_invalid.json
  - engine/tests/fixtures/actions_get_completed.json
  - engine/tests/fixtures/actions_get_error.json
  - engine/tests/fixtures/campaigns_find.json
  - data/ads_campaigns_find_2026-06-20.json
autonomous: true
requirements: [WRITE-01, WRITE-02, WRITE-05]

must_haves:
  truths:
    - "datadoe.ACTION_TERMINAL contains the verified enum verbatim (COMPLETED, PARTIALLY_COMPLETED, COMPLETED_WITH_ISSUES, ERROR, CANCELED, BLOCKED_NO_TOKENS, BLOCKED_INVALID_INPUT) with NO FAILED and CANCELED spelled one-L"
    - "The existing export TERMINAL set is untouched (still {COMPLETED, FAILED})"
    - "result.py exports Proceed and PollOutcome frozen dataclasses alongside the unchanged Refusal"
    - "test_lifecycle.py and test_find_cache.py exist with all named test cases and currently FAIL (red) because lifecycle.py / find_cache.py do not yet exist"
    - "test_env.py SCAN_SUFFIXES now includes \".json\" so the secret scan covers committed JSON fixtures/cache files going forward"
    - "Every fixture/cache .json file is free of the seller UUID prefix, proven by the explicit `grep -rl 5692b95f tests/fixtures/ data/` guard (the named UUID-leak control for these files)"
  artifacts:
    - path: "engine/tests/test_env.py"
      provides: "Secret-scan suffix set extended to cover committed JSON fixtures/cache files"
      contains: "SCAN_SUFFIXES"
    - path: "engine/src/habibos/datadoe.py"
      provides: "ACTION_IN_FLIGHT / ACTION_TERMINAL_SUCCESS / ACTION_TERMINAL_PARTIAL / ACTION_TERMINAL_FAIL / ACTION_TERMINAL constants"
      contains: "ACTION_TERMINAL"
    - path: "engine/src/habibos/result.py"
      provides: "Proceed + PollOutcome typed results"
      contains: "class Proceed"
    - path: "engine/tests/test_lifecycle.py"
      provides: "Failing contract tests for WRITE-01 / WRITE-05"
      contains: "test_action_terminal_enum_pinned"
    - path: "engine/tests/test_find_cache.py"
      provides: "Failing contract tests for WRITE-02"
      contains: "test_uncached_id_refuses"
    - path: "engine/tests/fixtures/campaigns_find.json"
      provides: "Sanitized CAMPAIGNS_FIND fixture seeded from the live capture"
      contains: "campaignId"
  key_links:
    - from: "engine/tests/test_lifecycle.py"
      to: "engine/src/habibos/datadoe.py"
      via: "import datadoe; assert datadoe.ACTION_TERMINAL == verified set"
      pattern: "ACTION_TERMINAL"
    - from: "engine/tests/test_lifecycle.py"
      to: "engine/src/habibos/result.py"
      via: "from habibos.result import Proceed, PollOutcome, Refusal"
      pattern: "Proceed|PollOutcome"
    - from: "engine/tests/test_env.py"
      to: "engine/tests/fixtures/*.json, data/*find*.json"
      via: "SCAN_SUFFIXES now includes .json — the secret scan covers committed JSON going forward"
      pattern: "SCAN_SUFFIXES"
---

<objective>
Lay the Phase 6 contract and Wave 0 test substrate: pin the verified `actions_get`
terminal-status enum into `datadoe.py`, add the typed `Proceed`/`PollOutcome` result types,
extend `test_env.py`'s secret scan to cover committed JSON, write the two failing test files
(`test_lifecycle.py`, `test_find_cache.py`) and all five captured/sanitized fixtures, and seed
the FIND cache file. After this plan the lifecycle/cache tests are RED (the classifier and cache
modules do not exist yet) — they become the executable contract that Plans 02 and 03 implement
against.

Purpose: Wave 0 first (per 06-VALIDATION.md). The enum is the one genuinely-new fact in this
phase and is load-bearing — a wrong enum causes an infinite poll. Pinning it verbatim and
asserting it in a test is the anti-fabrication guarantee for WRITE-05. Extending the secret
scan to `.json` makes the UUID-leak control REAL for the new committed fixtures (previously
`.json` was outside SCAN_SUFFIXES, so a UUID-bearing fixture would have passed test_env.py
silently — the explicit grep was the only guard).

Output: modified `datadoe.py` + `result.py` + `test_env.py`; new `test_lifecycle.py`,
`test_find_cache.py`; five `tests/fixtures/*.json`; one `data/ads_campaigns_find_2026-06-20.json`
cache seed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-find-lifecycle-substrate/06-RESEARCH.md
@.planning/phases/06-find-lifecycle-substrate/06-PATTERNS.md
@.planning/phases/06-find-lifecycle-substrate/06-VALIDATION.md

<interfaces>
<!-- Existing contracts the executor builds on. Extracted from the codebase — no exploration needed. -->

From engine/src/habibos/datadoe.py (the EXISTING export poll — DO NOT widen this set):
```
TERMINAL = {"COMPLETED", "FAILED"}            # export statuses — leave untouched
def _not_terminal(status): return status not in TERMINAL
@retry(stop=stop_after_attempt(20), wait=wait_exponential_jitter(initial=1, max=15),
       retry=retry_if_result(_not_terminal))
def poll_status(get_status) -> str: ...
def load_artifact(path_or_bytes): ...         # LOCAL file/bytes only — the seam
```

From engine/src/habibos/result.py (REUSE Refusal as-is; ADD Proceed + PollOutcome):
```
@dataclass(frozen=True)
class Refusal:
    reason: str
    code: str
    export_id: str | None = None
# Already imports: from dataclasses import dataclass, field ; from __future__ import annotations
```

From engine/tests/test_logging.py (the secret-hygiene + tmp_path idiom to mirror):
```
SELLER_UUID_PREFIX = "5692" + "b95f"          # assembled, never written literally
# monkeypatch hlog.LOG_PATH -> tmp_path ; hlog.configure() ; structlog.reset_defaults() teardown
# assert SELLER_UUID_PREFIX not in log_file.read_text()
```

From engine/tests/test_env.py (the secret-scan guard this plan EXTENDS):
```
SCAN_SUFFIXES = {".py", ".sql", ".md", ".toml", ".yaml", ".yml", ".sh", ".txt"}  # CURRENT — .json NOT yet in the set
TRACKED_SOURCE_DIRS = ("src", "db", "tests")
# IMPORTANT: as of HEAD, .json is OUTSIDE SCAN_SUFFIXES, so a UUID-bearing
# tests/**/*.json would PASS test_env.py silently. Task 1b adds ".json" to the
# set so the scan actually covers the new committed fixtures. Until then, the
# explicit `grep -rl 5692b95f tests/fixtures/ data/` check (Task 2) is the ONLY
# automated UUID-leak guard for these files.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin the verified ACTION_* status enum into datadoe.py and add Proceed/PollOutcome to result.py</name>
  <read_first>
    - engine/src/habibos/datadoe.py (the file being modified; existing TERMINAL block at lines 23-43)
    - engine/src/habibos/result.py (the file being modified; Refusal at lines 26-32, SkuRow/Answer at 35-66)
    - 06-RESEARCH.md "Extending the poll terminal set" (lines 359-365) and "actions_get response — VERIFIED status enum" (lines 329-344)
    - 06-PATTERNS.md "datadoe.py (MODIFY)" and "result.py (MODIFY — additive)" sections
  </read_first>
  <action>
    In `datadoe.py`, ADD (do NOT modify the existing `TERMINAL`/`poll_status`) a new named block below
    the existing TERMINAL block: `ACTION_IN_FLIGHT = {"PENDING","IN_PROGRESS","WAITING_EXTERNAL_PROCESSING"}`,
    `ACTION_TERMINAL_SUCCESS = {"COMPLETED"}`, `ACTION_TERMINAL_PARTIAL = {"PARTIALLY_COMPLETED","COMPLETED_WITH_ISSUES"}`,
    `ACTION_TERMINAL_FAIL = {"ERROR","CANCELED","BLOCKED_NO_TOKENS","BLOCKED_INVALID_INPUT"}`, and
    `ACTION_TERMINAL = ACTION_TERMINAL_SUCCESS | ACTION_TERMINAL_PARTIAL | ACTION_TERMINAL_FAIL`. There is
    NO `FAILED` (it is `ERROR`) and `CANCELED` is one L — use the research enum verbatim, never the roadmap's
    `COMPLETED/FAILED/CANCELLED`. Add a source comment `# verified against api.datadoe.com/api/v1/docs/json 2026-06-20`.
    Add an `_action_not_terminal(status)` predicate over `ACTION_TERMINAL` and a `poll_action_status(get_status)`
    that reuses the same tenacity `@retry` config as `poll_status` (stop_after_attempt(20), wait_exponential_jitter,
    retry_if_result over `_action_not_terminal`) — do NOT hand-roll a while+sleep loop. Note: `poll_action_status`
    is the in-flight→terminal poll PRIMITIVE; Phase 6 only proves single-response classification (Plan 02), and the
    full in-flight→terminal poll LOOP is driven end-to-end by Phase 8's apply spine — building the primitive here
    is intentional, not dead wiring.
    In `result.py`, ADD two frozen dataclasses mirroring the `SkuRow` shape: `Proceed(action_type: str)` for the
    dryRun PROCEED verdict, and `PollOutcome(status: str, state: str, issues: list[str] = field(default_factory=list))`
    where `state` is one of `"done" | "done_with_issues" | "in_flight"`. Reuse the already-imported
    `dataclass, field`. Leave `Refusal`, `SkuRow`, `Answer`, `DEFINITIONS`, `PREMIUM_SOURCE` unchanged.
  </action>
  <acceptance_criteria>
    - `grep -n "ACTION_TERMINAL" engine/src/habibos/datadoe.py` shows the union constant defined.
    - The existing export set is intact: `grep -n 'TERMINAL = {"COMPLETED", "FAILED"}' engine/src/habibos/datadoe.py` still matches.
    - `engine/src/habibos/result.py` defines `class Proceed` and `class PollOutcome` (grep confirms both).
    - `cd engine && uv run python -c "from habibos import datadoe; assert datadoe.ACTION_TERMINAL == {'COMPLETED','PARTIALLY_COMPLETED','COMPLETED_WITH_ISSUES','ERROR','CANCELED','BLOCKED_NO_TOKENS','BLOCKED_INVALID_INPUT'}; assert 'FAILED' not in datadoe.ACTION_TERMINAL; assert 'CANCELLED' not in datadoe.ACTION_TERMINAL"` exits 0.
    - `cd engine && uv run python -c "from habibos.result import Proceed, PollOutcome, Refusal"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd engine && uv run python -c "from habibos import datadoe; from habibos.result import Proceed, PollOutcome; assert datadoe.ACTION_TERMINAL == {'COMPLETED','PARTIALLY_COMPLETED','COMPLETED_WITH_ISSUES','ERROR','CANCELED','BLOCKED_NO_TOKENS','BLOCKED_INVALID_INPUT'}; assert 'FAILED' not in datadoe.ACTION_TERMINAL"</automated>
  </verify>
  <done>datadoe.py carries the verified ACTION_* sets + poll_action_status (the Phase 8 loop primitive); result.py exports Proceed + PollOutcome; the existing export TERMINAL and poll_status are unchanged; both imports succeed.</done>
</task>

<task type="auto">
  <name>Task 1b: Extend test_env.py secret scan to cover committed JSON fixtures/cache files</name>
  <read_first>
    - engine/tests/test_env.py (line 27 — SCAN_SUFFIXES; line 19 SELLER_UUID_PREFIX; lines 43-65 the scan loop. NOTE: as of HEAD, ".json" is NOT in SCAN_SUFFIXES, so JSON fixtures are NOT scanned.)
  </read_first>
  <action>
    In `engine/tests/test_env.py`, add `".json"` to the `SCAN_SUFFIXES` set so the existing UUID secret-scan
    (`test_seller_id_in_env_not_code`) now also walks committed `tests/**/*.json` fixtures/cache files. This makes
    the control REAL going forward — before this change a UUID-bearing JSON fixture would have passed test_env.py
    silently because `.json` was outside the scanned suffix set. Do NOT change `TRACKED_SOURCE_DIRS`, the
    `SELLER_UUID_PREFIX` assembly, or the scan loop logic; this is a one-token additive change to the suffix set,
    with the inline comment updated to note `.json` covers committed fixtures/cache files. Run this BEFORE Task 2
    creates the fixtures so the scan is live when those JSON files land.
  </action>
  <acceptance_criteria>
    - `grep -n 'SCAN_SUFFIXES' engine/tests/test_env.py` shows `.json` in the set.
    - `cd engine && uv run python -c "import importlib.util,pathlib; m=importlib.util.spec_from_file_location('te', pathlib.Path('tests/test_env.py')); mod=importlib.util.module_from_spec(m); m.loader.exec_module(mod); assert '.json' in mod.SCAN_SUFFIXES"` exits 0.
    - `cd engine && uv run pytest tests/test_env.py -x` stays green (no UUID currently in tracked source).
  </acceptance_criteria>
  <verify>
    <automated>cd engine && grep -q '"\.json"' tests/test_env.py && uv run pytest tests/test_env.py -x</automated>
  </verify>
  <done>test_env.py SCAN_SUFFIXES contains ".json"; the secret scan now covers committed JSON fixtures/cache files; test_env.py stays green. The threat-model mitigation for T-06-01 can now legitimately cite test_env.py.</done>
</task>

<task type="auto">
  <name>Task 2: Create and sanitize the five fixtures + the FIND cache seed</name>
  <read_first>
    - data/ads_sp_campaigns_live_2026-06-20.json (the live CAMPAIGNS_FIND capture to seed from — {"campaigns":[91 items]})
    - engine/tests/test_env.py (lines 19-45 — SELLER_UUID_PREFIX; SCAN_SUFFIXES now includes .json after Task 1b; TRACKED_SOURCE_DIRS includes tests)
    - 06-RESEARCH.md "actions_start dryRun:true response" (lines 307-327), "actions_get response" (329-344), "Terminal failure payload" (346-355)
    - 06-PATTERNS.md "Fixtures" section (lines 223-231) and the secret-hygiene paragraph (231)
  </read_first>
  <action>
    Create `tests/fixtures/campaigns_find.json` by copying `data/ads_sp_campaigns_live_2026-06-20.json` and
    STRIPPING any `sellerOrVendorId` field (and any other occurrence of the seller UUID prefix `5692b95f`) —
    keep the `{"campaigns":[...]}` shape and the per-item keys `campaignId, name, adProduct, state, budgets,
    optimizations, status`. Also write the same sanitized object to `data/ads_campaigns_find_2026-06-20.json`
    (the cache seed the find_cache lookup will read in Plan 03; sanitize it per hard rule 5). Create
    `tests/fixtures/dryrun_validated.json` as an `ActionStartResponse` with
    `actionId:null, status:"VALIDATED", dryRun:true, validation:{valid:true, issues:[]}, error:null`. Create
    `tests/fixtures/dryrun_invalid.json` with `status:"VALIDATED", dryRun:true, validation:{valid:false,
    issues:[{path:["details","campaigns",0,"budgets"], code:"BUDGET_TOO_LOW", message:"<verbatim>"}]}`. Create
    `tests/fixtures/actions_get_completed.json` as `{actionId:"<placeholder-non-uuid>", type:"AMAZON_ADS_CAMPAIGNS_UPDATE",
    status:"COMPLETED", details:{}}` (do NOT use the real seller UUID anywhere). Create
    `tests/fixtures/actions_get_error.json` as `{actionId:"<placeholder>", type:"AMAZON_ADS_CAMPAIGNS_UPDATE",
    status:"ERROR", error:{status:"ERROR", code:"EXECUTION_FAILED", message:"<verbatim user-friendly reason>"}}`.
    Use the `AMAZON_ADS_`-prefixed type strings (the full enum), not the shorthand.
    NOTE on provenance: `dryrun_validated.json` and `dryrun_invalid.json` are SPEC-DERIVED — hand-authored from the
    pinned `api.datadoe.com/api/v1/docs/json` ActionStartResponse shape — pending a real Ads dryRun capture, which is
    eyeballed live (read-only `actions_start dryRun:true`) at Plan 04 Task 2 before the phase gate. This is acceptable
    for Wave 0 RED tests because the shape is pinned; the live eyeball confirms the MCP wrapper does not rename/wrap it.
  </action>
  <acceptance_criteria>
    - All six files exist: `ls tests/fixtures/campaigns_find.json tests/fixtures/dryrun_validated.json tests/fixtures/dryrun_invalid.json tests/fixtures/actions_get_completed.json tests/fixtures/actions_get_error.json data/ads_campaigns_find_2026-06-20.json` (run from engine/ for the first five; data/ is repo-root).
    - No UUID leak (the NAMED, sole automated UUID-leak guard for these committed JSON files): `grep -rl "5692b95f" engine/tests/fixtures/ data/ads_campaigns_find_2026-06-20.json` returns nothing.
    - Shapes valid JSON and correct: `cd engine && uv run python -c "import json,pathlib; d=json.loads(pathlib.Path('tests/fixtures/dryrun_validated.json').read_text()); assert d['status']=='VALIDATED' and d['validation']['valid'] is True and d['validation']['issues']==[]"` exits 0.
    - `cd engine && uv run python -c "import json,pathlib; d=json.loads(pathlib.Path('tests/fixtures/actions_get_error.json').read_text()); assert d['status']=='ERROR' and d['error']['message']"` exits 0.
    - `cd engine && uv run pytest tests/test_env.py -x` stays green (with .json now scanned per Task 1b, this confirms no UUID landed in any committed fixture under tests/).
  </acceptance_criteria>
  <verify>
    <automated>cd engine && uv run pytest tests/test_env.py -x && test -z "$(grep -rl 5692b95f tests/fixtures/)" && test -z "$(grep -rl 5692b95f ../data/ads_campaigns_find_2026-06-20.json)"</automated>
  </verify>
  <done>Five sanitized fixtures + one cache seed exist with the verified ActionStartResponse / ActionStatusResponse / CAMPAIGNS_FIND shapes; no seller UUID anywhere (proven by the explicit grep, the named guard for these files); the dryRun fixtures are spec-derived pending the Plan 04 live eyeball; test_env.py still green (and now scanning .json).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write the failing contract tests (test_lifecycle.py, test_find_cache.py)</name>
  <read_first>
    - engine/tests/test_tacos.py (lines 22-49 fixture-load idiom; 177-183 isinstance/.code asserts)
    - engine/tests/test_logging.py (lines 25-32, 34-43, 85-94 — tmp_path/monkeypatch fixture + UUID-absence guard)
    - 06-VALIDATION.md "Per-Task Verification Map" (the exact test node names) and 06-RESEARCH.md Test Map (lines 433-441)
    - 06-PATTERNS.md "test_lifecycle.py" and "test_find_cache.py" sections
  </read_first>
  <behavior>
    - test_action_terminal_enum_pinned: asserts `datadoe.ACTION_TERMINAL` equals the verified set verbatim (SC-4).
    - test_dryrun_validated_proceeds: dryrun_validated.json → `lifecycle.classify_dryrun(...)` returns a `Proceed`.
    - test_dryrun_invalid_stops: dryrun_invalid.json → a `Refusal` whose `reason` carries the issue message verbatim and `code=="dryrun_invalid"`.
    - test_poll_terminal_classification: COMPLETED → PollOutcome(state="done"); PENDING/IN_PROGRESS/WAITING_EXTERNAL_PROCESSING → state="in_flight".
    - test_poll_error_surfaces_message: actions_get_error.json → a `Refusal` carrying `ActionExecutionErrorResult.message` verbatim, `code=="action_error"`.
    - test_unknown_status_refuses: an unmodeled status string → `Refusal(code="unknown_status")`, never assumed-terminal, never polled forever.
    - test_export_not_confirmation: a helper/guard proving the reconcile path refuses to treat a DataDoe export status as confirmation (e.g. `lifecycle.classify_poll` rejects/ignores an export-only sentinel, or a documented guard asserts no export-read in the reconcile path).
    - test_ops_logged_no_secret: classify ops run inside `logged_call` emit one `.start`+`.end` and the seller UUID prefix is absent from the log file.
    - test_find_write_and_lookup: find_cache.write(campaigns_find result) → `data/ads_*_find_*.json`; a cached campaignId looks up HIT.
    - test_uncached_id_refuses: a non-cached ID → `Refusal("stale/missing entity ID", "find_cache_miss")`.
  </behavior>
  <action>
    Create `tests/test_lifecycle.py` and `tests/test_find_cache.py` containing exactly the test node names from
    06-VALIDATION.md's map (`test_dryrun_validated_proceeds`, `test_dryrun_invalid_stops`,
    `test_poll_terminal_classification`, `test_poll_error_surfaces_message`, `test_unknown_status_refuses`,
    `test_action_terminal_enum_pinned`, `test_export_not_confirmation`, `test_ops_logged_no_secret` in lifecycle;
    `test_find_write_and_lookup`, `test_uncached_id_refuses` in find_cache). Import the targets as
    `from habibos import lifecycle, datadoe, find_cache` and `from habibos.result import Proceed, PollOutcome, Refusal`.
    Load fixtures with the `test_tacos.py` repo-root idiom (`REPO_ROOT = Path(__file__).resolve().parent.parent`).
    For `test_ops_logged_no_secret`, mirror `test_logging.py`'s `log_file` fixture (monkeypatch `hlog.LOG_PATH`,
    `hlog.configure()`, `structlog.reset_defaults()` teardown) and assert `SELLER_UUID_PREFIX not in log_file.read_text()`
    with the prefix assembled `"5692"+"b95f"` (never the literal). For `test_find_cache`, mirror the tmp_path fixture
    that `monkeypatch.setattr(find_cache, "DATA_DIR", tmp_path)`. Assert via `isinstance(r, Refusal)` / `isinstance(r, Proceed)`
    and `.code`, mirroring `test_tacos.py`. These tests MUST currently FAIL (lifecycle.py and find_cache.py do not exist).
    Do NOT import the real seller UUID anywhere in these files.
  </action>
  <acceptance_criteria>
    - Both files exist with the exact node names: `grep -c "def test_" tests/test_lifecycle.py` returns 8 and `tests/test_find_cache.py` returns 2 (run from engine/).
    - The named nodes are present: `grep -n "test_action_terminal_enum_pinned\|test_unknown_status_refuses\|test_export_not_confirmation\|test_ops_logged_no_secret" tests/test_lifecycle.py` and `grep -n "test_uncached_id_refuses" tests/test_find_cache.py`.
    - Tests are RED (collection succeeds, run fails because modules are missing): `cd engine && uv run pytest tests/test_lifecycle.py tests/test_find_cache.py` exits non-zero with import/attribute errors on `lifecycle`/`find_cache`, NOT collection syntax errors.
    - No UUID literal: `grep -c "5692b95f" tests/test_lifecycle.py tests/test_find_cache.py` returns 0 for each.
    - `cd engine && uv run pytest tests/test_env.py -x` still green.
  </acceptance_criteria>
  <verify>
    <automated>cd engine && uv run pytest tests/test_lifecycle.py tests/test_find_cache.py 2>&1 | grep -qiE "no module named 'habibos.lifecycle'|no module named 'habibos.find_cache'|module 'habibos' has no attribute" && echo RED-AS-EXPECTED</automated>
  </verify>
  <done>test_lifecycle.py (8 cases) and test_find_cache.py (2 cases) exist with the exact node names, import the not-yet-existing modules, currently fail RED, and leak no UUID. They are the executable contract Plans 02/03 implement against.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed fixtures/cache → git | Captured DataDoe JSON may carry the seller UUID into tracked files |
| roadmap-assumed enum → engine constant | A wrong/assumed status enum can cause an infinite poll downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | tests/fixtures/*.json, data/*find*.json | mitigate | Strip `sellerOrVendorId`/UUID prefix from every fixture (Task 2). The NAMED, sole automated UUID-leak guard for these JSON files is the explicit `grep -rl 5692b95f tests/fixtures/ data/ads_campaigns_find_2026-06-20.json` check (returns nothing). Task 1b ALSO adds `.json` to `test_env.py` SCAN_SUFFIXES so the standing secret scan covers committed JSON fixtures/cache files going forward (it did NOT before this plan). |
| T-06-02 | Tampering | datadoe.ACTION_TERMINAL | mitigate | Pin the verified enum verbatim with a source comment; `test_action_terminal_enum_pinned` asserts the set; NO `FAILED`, `CANCELED` one-L (Task 1). |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (RESEARCH §Package Legitimacy Audit: zero new deps). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `cd engine && uv run pytest tests/test_env.py -x` green, and `grep -q '"\.json"' engine/tests/test_env.py` confirms the scan now covers JSON.
- No UUID in any committed fixture/cache JSON: `grep -rl 5692b95f engine/tests/fixtures/ data/ads_campaigns_find_2026-06-20.json` returns nothing.
- `cd engine && uv run python -c "from habibos import datadoe; assert datadoe.ACTION_TERMINAL == {'COMPLETED','PARTIALLY_COMPLETED','COMPLETED_WITH_ISSUES','ERROR','CANCELED','BLOCKED_NO_TOKENS','BLOCKED_INVALID_INPUT'}"` exits 0.
- `cd engine && uv run pytest tests/test_lifecycle.py tests/test_find_cache.py` fails RED on missing `lifecycle`/`find_cache` modules (expected — they are implemented in Plans 02/03).
- The pre-existing suite (test_tacos/test_logging/test_env/etc.) stays green: `cd engine && uv run pytest --ignore=tests/test_lifecycle.py --ignore=tests/test_find_cache.py`.
</verification>

<success_criteria>
- `datadoe.ACTION_TERMINAL` matches the verified enum verbatim; export `TERMINAL` untouched.
- `result.py` exports `Proceed` + `PollOutcome`.
- `test_env.py` SCAN_SUFFIXES includes `.json` and the suite stays green.
- `test_lifecycle.py` (8 cases) + `test_find_cache.py` (2 cases) exist and are RED on the missing modules.
- Five sanitized fixtures + one cache seed exist; no seller UUID anywhere under `tests/` or in the cache seed (proven by the explicit grep guard).
- `test_env.py` and the rest of the pre-existing suite stay green.
</success_criteria>

<output>
Create `.planning/phases/06-find-lifecycle-substrate/06-01-SUMMARY.md` when done.
</output>
