---
phase: 07-margin-tiered-safety-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/src/habibos/result.py
  - engine/tests/test_gate.py
  - engine/tests/test_thresholds.py
autonomous: true
requirements: [GATE-01, GATE-02, GATE-03, GATE-04, GATE-05]

must_haves:
  truths:
    - "result.py exports ProposedAction, GateRefusal, GateVerdict as frozen dataclasses"
    - "ProposedAction.is_spend_increasing is True when delta_spend > 0, False otherwise"
    - "test_gate.py exists with one failing test per GATE-01..05 plus D-03/D-04 edges"
    - "the full pytest suite still collects and runs (test_thresholds.py repointed off the renamed key)"
  artifacts:
    - path: "engine/src/habibos/result.py"
      provides: "GateRefusal / GateVerdict / ProposedAction typed siblings of Refusal/Proceed"
      contains: "class GateRefusal"
    - path: "engine/tests/test_gate.py"
      provides: "RED contract tests for GATE-01..05 + D-03/D-04"
      contains: "def test_contribution_margin_pre_ad"
    - path: "engine/tests/test_thresholds.py"
      provides: "repointed empty-key refusal assertion (min_margin_pct no longer the empty key)"
      contains: "days_of_cover_floor"
  key_links:
    - from: "engine/tests/test_gate.py"
      to: "engine.src.habibos.result"
      via: "import GateRefusal, GateVerdict, ProposedAction"
      pattern: "from habibos.result import"
    - from: "engine/tests/test_gate.py"
      to: "engine/tests/fixtures/profit_by_sku_30d.csv"
      via: "tacos.parse(FIXTURE, COMPLETED)"
      pattern: "parse\\(FIXTURE"
---

<objective>
Lay down the gate's typed contracts and the RED test suite before any implementation —
the Wave 0 scaffold. Add `ProposedAction`, `GateRefusal`, `GateVerdict` to `result.py`
(siblings of the existing `Refusal`/`Proceed`), write `test_gate.py` with one failing
test per GATE-01..05 plus the D-03 (pre-ad back-out) and D-04 (unreliable COGS) edges,
and repoint the one `test_thresholds.py` assertion that breaks when `min_margin_pct` is
renamed and seeded in Plan 02.

Purpose: Define the interface contracts downstream plans (gate.py in Plan 03, CLI in
Plan 04) implement against — no scavenger hunt. Establish the Nyquist RED baseline so
`gate.py` is written test-first.
Output: Extended `result.py`, new `test_gate.py` (failing), repointed `test_thresholds.py`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-margin-tiered-safety-gate/07-CONTEXT.md
@.planning/phases/07-margin-tiered-safety-gate/07-RESEARCH.md
@.planning/phases/07-margin-tiered-safety-gate/07-PATTERNS.md

<interfaces>
<!-- Contracts this plan CREATES (Plan 03 gate.py implements against these). -->
<!-- Existing analogs to mirror, extracted from engine/src/habibos/result.py: -->

Existing frozen-dataclass grammar in result.py to imitate:
  Refusal(reason: str, code: str, export_id: str | None = None)
  Proceed(action_type: str)              # the "never a bare True" positive type
  PREMIUM_SOURCE = "DataDoe premium Profit by SKU & Date (57a0cb319c)"  # reuse for source=

New siblings to add (frozen dataclasses, RESEARCH Code Examples / PATTERNS lines 100-128):
  ProposedAction(sku, action_type, delta_spend, marketplace="CA")
     .is_spend_increasing -> bool  (delta_spend > 0)
  GateRefusal(reason, code, breached_value=None, ceiling=None, source=None)
     code ∈ {"no_threshold_set","no_margin","sku_not_in_catalog","over_ceiling"}
  GateVerdict(projected_tacos, ceiling, contribution_margin, auto_pass=False)

Fixture columns (engine/tests/fixtures/profit_by_sku_30d.csv header):
  sku,currency,ad_spend_sum,ad_sales_sum,total_sales_sum,profit_sum,total_cost_sum,units_sum
Fixture rows the tests assert on:
  EU-Z87B-ZRBZ: ad_spend_sum=0.5, total_sales_sum=279.95, profit_sum=100.27
                -> CM% = (100.27 + 0.5)/279.95*100 = 36.0 ; ceiling = 36.0 - 15 = 21.0
  9Z-KUHZ-FU2I: total_sales_sum=0  -> CM% None -> no_margin refusal
  ASW-H50:      total_sales_sum=0  -> CM% None -> no_margin refusal
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add gate types to result.py</name>
  <read_first>
    - engine/src/habibos/result.py (the file being modified — mirror Refusal lines 26-32 and Proceed lines 35-46 exactly; reuse PREMIUM_SOURCE line 23)
    - .planning/phases/07-margin-tiered-safety-gate/07-PATTERNS.md (lines 80-129 — the exact dataclass shapes)
  </read_first>
  <action>
    Add three frozen dataclasses to engine/src/habibos/result.py alongside the existing
    Refusal/Proceed/PollOutcome (same @dataclass(frozen=True) style, same module — D-08).
    ProposedAction carries `sku: str`, `action_type: str`, `delta_spend: float`,
    `marketplace: str = "CA"`, and a property `is_spend_increasing` returning
    `self.delta_spend > 0` (Open Q3: delta_spend is caller-computed; the gate treats it as
    given). GateRefusal mirrors Refusal but adds the fields GATE-04 needs to name the
    breached number and its source: `reason: str`, `code: str`,
    `breached_value: float | None = None`, `ceiling: float | None = None`,
    `source: str | None = None`; document the four `code` values
    ("no_threshold_set" | "no_margin" | "sku_not_in_catalog" | "over_ceiling"). GateVerdict
    is the typed PASS (never a bare True, mirror Proceed): `projected_tacos: float | None`,
    `ceiling: float | None`, `contribution_margin: float | None`, `auto_pass: bool = False`
    (auto_pass True for spend-decreasing actions per D-06). Reuse the existing
    `PREMIUM_SOURCE` constant for the GateRefusal `source` provenance — do not redefine it.
    Do NOT store per-SKU margin anywhere here (D-04: margin is computed live in Plan 03).
  </action>
  <verify>
    <automated>cd engine && uv run python -c "from habibos.result import ProposedAction, GateRefusal, GateVerdict; a=ProposedAction(sku='X', action_type='bid_up', delta_spend=5.0); print(a.is_spend_increasing, ProposedAction(sku='X', action_type='pause', delta_spend=-1.0).is_spend_increasing)"</automated>
  </verify>
  <acceptance_criteria>
    - engine/src/habibos/result.py contains `class GateRefusal`, `class GateVerdict`, `class ProposedAction`
    - all three are decorated `@dataclass(frozen=True)`
    - `ProposedAction(sku='X', action_type='bid_up', delta_spend=5.0).is_spend_increasing` is `True`
    - `ProposedAction(sku='X', action_type='pause', delta_spend=-1.0).is_spend_increasing` is `False`
    - `GateRefusal` has fields `breached_value`, `ceiling`, `source` (all defaulting `None`)
    - `GateVerdict` has fields `projected_tacos`, `ceiling`, `contribution_margin`, `auto_pass` (default `False`)
    - the existing `Refusal`, `Proceed`, `PollOutcome`, `SkuRow`, `Answer` definitions are unchanged
    - the verify command prints `True False` and exits 0
  </acceptance_criteria>
  <done>The three gate dataclasses import cleanly and is_spend_increasing branches correctly on delta_spend sign.</done>
</task>

<task type="auto">
  <name>Task 2: Write RED test_gate.py contract (GATE-01..05 + D-03/D-04)</name>
  <read_first>
    - engine/tests/test_tacos.py (the fixture-driven structure to mirror — REPO_ROOT/FIXTURE resolution, named-SKU constants, the unset-floor-via-marketplace trick around its None-ceiling test)
    - engine/src/habibos/tacos.py (lines 80-104 parse; lines 107-115 _ratio; lines 118-124 _present_flag — the grammar the gate will reproduce)
    - engine/src/habibos/result.py (the gate types added in Task 1)
    - .planning/phases/07-margin-tiered-safety-gate/07-PATTERNS.md (lines 226-253 — the test function names are the contract)
    - .planning/phases/07-margin-tiered-safety-gate/07-RESEARCH.md (Phase Requirements → Test Map; THE D-03 RESOLUTION worked numbers)
  </read_first>
  <action>
    Create engine/tests/test_gate.py mirroring test_tacos.py's fixture-driven layout: same
    `REPO_ROOT = Path(__file__).resolve().parent.parent`, `FIXTURE = REPO_ROOT / "tests" /
    "fixtures" / "profit_by_sku_30d.csv"`, and a `_frame()` helper calling
    `tacos.parse(FIXTURE, "COMPLETED")`. Import the future gate API from `habibos.gate`
    (functions `contribution_margin_pct`, `ceiling_tacos_pct`, `projected_tacos_pct`,
    `evaluate`) and the types from `habibos.result` (GateRefusal, GateVerdict,
    ProposedAction). Write these failing tests (names are the contract — RESEARCH Test Map):
    `test_contribution_margin_pre_ad` (GATE-01: CM% from window sums with ad_spend_sum added
    back), `test_profit_is_post_ad_addback` (D-03: assert EU-Z87B-ZRBZ CM% == 36.0 from
    (100.27+0.5)/279.95*100, proving the back-out), `test_ceiling_and_unset_floor_refuses`
    (GATE-02: ceiling = CM% − floor; AND that with the floor unset the gate returns
    GateRefusal(code="no_threshold_set") on every spend-up — exercise the empty floor via a
    marketplace with no min_net_margin_pct, e.g. "XX", the same None-ceiling trick
    test_tacos.py uses, so it is independent of the seeded live 15),
    `test_spend_direction_branch` (GATE-03: a spend-increasing ProposedAction with projected
    ≤ ceiling returns GateVerdict; spend-decreasing ProposedAction (delta_spend ≤ 0) returns
    GateVerdict(auto_pass=True) with no projection), `test_breach_refuses_never_clamps`
    (GATE-04: an over-ceiling spend-up returns GateRefusal(code="over_ceiling") whose
    breached_value equals the projected TACOS and whose `source` is set — assert the returned
    object is a GateRefusal, NOT a GateVerdict clamped to the ceiling),
    `test_sku_not_in_catalog_refuses` (GATE-05: a ProposedAction for a SKU absent from the
    catalog returns GateRefusal(code="sku_not_in_catalog")),
    `test_unreliable_cogs_refuses` (D-04: a SKU whose catalog cogs is 0/null OR whose
    total_sales_sum==0 — e.g. 9Z-KUHZ-FU2I or ASW-H50 — returns
    GateRefusal(code="no_margin")). Use the strict `>` breach semantics: a projected exactly
    equal to the ceiling must PASS. Tests reference a small catalog fixture path
    `tests/fixtures/sku_catalog_test.toml` (created in Plan 02) — for now reference the
    constant path so the tests fail RED on missing gate.py / missing fixture, not on a typo.
    These tests MUST currently FAIL (RED) because gate.py does not yet exist.
  </action>
  <verify>
    <automated>cd engine && uv run pytest tests/test_gate.py -q 2>&1 | grep -Eq "(ModuleNotFoundError|ImportError|errors|failed)" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - engine/tests/test_gate.py contains def test_contribution_margin_pre_ad, def test_profit_is_post_ad_addback, def test_ceiling_and_unset_floor_refuses, def test_spend_direction_branch, def test_breach_refuses_never_clamps, def test_sku_not_in_catalog_refuses, def test_unreliable_cogs_refuses
    - the file imports from `habibos.gate` and `habibos.result`
    - `cd engine && uv run pytest tests/test_gate.py -q` exits non-zero (RED — gate.py absent), printing `RED-OK` from the verify pipe
    - test_profit_is_post_ad_addback asserts the EU-Z87B-ZRBZ CM% == 36.0 (the (profit+ad_spend)/sales back-out), NOT profit/sales
    - test_breach_refuses_never_clamps asserts isinstance(result, GateRefusal) and result.breached_value is the projected value, never a clamped ceiling
  </acceptance_criteria>
  <done>All seven test functions exist, import the planned gate API, and fail RED on the absent module — the Nyquist baseline for Plan 03.</done>
</task>

<task type="auto">
  <name>Task 3: Repoint test_thresholds.py off the renamed key</name>
  <read_first>
    - engine/tests/test_thresholds.py (the file being modified — test_missing_threshold_returns_none at line 28-34, specifically line 30's `read("min_margin_pct","CA") is None`)
    - engine/config/thresholds.toml (line 27 min_margin_pct=""; line 38 days_of_cover_floor="" — the still-empty key to repoint to)
    - .planning/phases/07-margin-tiered-safety-gate/07-PATTERNS.md (lines 211-222 — the exact repoint)
  </read_first>
  <action>
    In engine/tests/test_thresholds.py::test_missing_threshold_returns_none, change the
    first assertion from `thresholds.read("min_margin_pct", "CA") is None` to
    `thresholds.read("days_of_cover_floor", "CA") is None` — `days_of_cover_floor` is the key
    that remains empty (`""`) after Plan 02 seeds `min_net_margin_pct = 15`, so the
    empty-refusal path stays covered. Leave the absent-key and absent-marketplace assertions
    unchanged. Add one new assertion locking the seeded floor:
    `assert thresholds.read("min_net_margin_pct", "CA") == 15.0`. This assertion will FAIL
    until Plan 02 seeds the key — that is the intended Wave 1 → Wave 2 dependency signal; note
    it in the SUMMARY as expected-RED-until-Plan-02. Do NOT change thresholds.py itself
    (its refuse-on-empty contract is unchanged).
  </action>
  <verify>
    <automated>cd engine && uv run pytest tests/test_thresholds.py::test_missing_threshold_returns_none -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - engine/tests/test_thresholds.py no longer references `read("min_margin_pct"` anywhere
    - test_missing_threshold_returns_none asserts `read("days_of_cover_floor", "CA") is None`
    - a new assertion `thresholds.read("min_net_margin_pct", "CA") == 15.0` is present (expected RED until Plan 02 seeds it; documented in SUMMARY)
    - `test_missing_threshold_returns_none` itself (the days_of_cover_floor / absent-key / absent-marketplace lines) passes; only the new seeded-floor assertion is RED pending Plan 02
  </acceptance_criteria>
  <done>The empty-key refusal coverage is repointed to days_of_cover_floor and the suite no longer references the renamed key; the seeded-floor lock is staged for Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator config (thresholds.toml) → engine | a missing/empty floor must surface as refusal, never a default |
| premium export artifact → engine parse | untrusted column shape crosses into money math |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | result.py gate types | mitigate | Frozen dataclasses (immutable verdicts); GateRefusal carries breached_value+ceiling+source so a refusal can never be silently rewritten as a clamp. Locked by test_breach_refuses_never_clamps (Task 2). |
| T-07-02 | Repudiation | test_gate.py RED baseline | mitigate | Tests assert the back-out arithmetic against the committed fixture (test_profit_is_post_ad_addback) so a fabricated margin can never pass review unnoticed. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — RESEARCH Package Legitimacy Audit confirms every dependency (polars, pydantic, tomllib, pytest) is already pinned and prior-audited. slopcheck N/A. |
</threat_model>

<verification>
- `cd engine && uv run pytest tests/test_gate.py -q` → RED (gate.py absent) — the intended Wave 1 baseline
- `cd engine && uv run pytest tests/test_thresholds.py -q` → the repointed empty-key assertion passes; the new seeded-floor assertion is RED until Plan 02 (documented)
- `cd engine && uv run python -c "from habibos.result import ProposedAction, GateRefusal, GateVerdict"` → imports clean
</verification>

<success_criteria>
- result.py exports the three gate dataclasses with the exact field shapes Plan 03 implements against
- test_gate.py holds the seven-function RED contract covering GATE-01..05 + D-03/D-04
- test_thresholds.py no longer references the renamed key and stages the seeded-floor lock
</success_criteria>

<output>
Create `.planning/phases/07-margin-tiered-safety-gate/07-01-SUMMARY.md` when done.
Note explicitly: which assertions are intentionally RED (gate.py absent; seeded floor pending Plan 02).
</output>
