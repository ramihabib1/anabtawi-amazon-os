---
phase: 07-margin-tiered-safety-gate
plan: 03
type: tdd
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - engine/src/habibos/gate.py
autonomous: true
requirements: [GATE-01, GATE-02, GATE-03, GATE-04, GATE-05]

must_haves:
  truths:
    - "the engine computes each SKU's pre-ad contribution margin from window sums (ad_spend added back), typed and tested"
    - "the engine derives ceiling = CM% − min_net_margin_pct; floor unset → GateRefusal('no threshold set') on every spend-up"
    - "a spend-increasing write passes only when projected TACOS ≤ ceiling; a spend-decreasing write auto-passes"
    - "an over-ceiling write returns GateRefusal naming the breached number + source, never a clamped value"
    - "a SKU absent from the catalog, or with 0/null COGS or zero sales, returns the correct typed GateRefusal"
    - "cd engine && uv run pytest tests/test_gate.py passes (all seven Plan 01 tests go GREEN)"
  artifacts:
    - path: "engine/src/habibos/gate.py"
      provides: "the hand-written money core: contribution_margin_pct, ceiling_tacos_pct, projected_tacos_pct, evaluate"
      contains: "def evaluate"
      min_lines: 80
  key_links:
    - from: "engine/src/habibos/gate.py"
      to: "engine.src.habibos.tacos"
      via: "import parse, PREMIUM_SOURCE_ID (reuse the validated window-sum reader)"
      pattern: "from .tacos import"
    - from: "engine/src/habibos/gate.py"
      to: "engine.src.habibos.thresholds"
      via: "thresholds.read('min_net_margin_pct', marketplace)"
      pattern: "thresholds\\.read"
    - from: "engine/src/habibos/gate.py"
      to: "engine/config/sku_catalog.toml"
      via: "tomllib catalog read for tier+cogs+canonical set"
      pattern: "tomllib"
---

<objective>
Implement `engine/src/habibos/gate.py` — the hand-written, pytest-covered money core that
is the structural twin of `tacos.py`. It computes each SKU's pre-ad contribution margin
(Path A back-out, D-03), derives the per-SKU TACOS ceiling (D-01), projects a spend-up's
worst-case TACOS (D-06), and returns a typed `GateVerdict`/`GateRefusal` — refusing, never
clamping, never defaulting, on every missing/over-ceiling input. This task turns the Plan 01
RED suite GREEN.

This is a TDD plan: the failing `test_gate.py` from Plan 01 is the RED; gate.py is the GREEN.
Purpose: Ship the load-bearing safety guardrail (the gap flagged in execution-plan §A.5).
Output: gate.py, all seven test_gate.py tests passing, full suite green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-margin-tiered-safety-gate/07-CONTEXT.md
@.planning/phases/07-margin-tiered-safety-gate/07-RESEARCH.md
@.planning/phases/07-margin-tiered-safety-gate/07-PATTERNS.md

<interfaces>
<!-- Reuse, don't re-roll. Extracted contracts the gate consumes: -->

From engine/src/habibos/tacos.py (REUSE — do not re-implement a reader):
  PREMIUM_SOURCE_ID = "57a0cb319c"
  def parse(artifact, status) -> pl.DataFrame | Refusal     # pydantic-validated window-sum reader
  EXPECTED_COLUMNS includes: ad_spend_sum, total_sales_sum, profit_sum   (Path A back-out needs only these)
  def _ratio(num, den) -> float|None   # None on zero/missing denom — clone this grammar
  def _present_flag(value, ceiling) -> str  # strict `value > ceiling` is breach; equal is ok — clone the `>`

From engine/src/habibos/result.py (Plan 01 added these — import them):
  ProposedAction(sku, action_type, delta_spend, marketplace="CA").is_spend_increasing
  GateRefusal(reason, code, breached_value=None, ceiling=None, source=None)
  GateVerdict(projected_tacos, ceiling, contribution_margin, auto_pass=False)
  PREMIUM_SOURCE = "DataDoe premium Profit by SKU & Date (57a0cb319c)"   # use for GateRefusal.source

From engine/src/habibos/thresholds.py:
  read(key, marketplace="CA") -> float | None   # None on empty "" -> refuse, never default

Catalog (engine/config/sku_catalog.toml, Plan 02): [sku."<SKU>"] -> {asin, tier, cogs}

The branch order (RESEARCH architecture diagram; PATTERNS lines 71-76):
  1. spend-decreasing (not is_spend_increasing) -> GateVerdict(auto_pass=True), NO reads
  2. sku ∉ catalog -> GateRefusal(code="sku_not_in_catalog")
  3. catalog cogs ≤ 0/null OR total_sales_sum == 0 -> GateRefusal(code="no_margin")
  4. floor = thresholds.read("min_net_margin_pct", mkt); None -> GateRefusal(code="no_threshold_set")
  5. ceiling = CM% − floor; projected = (ad_spend_sum + delta_spend)/total_sales_sum;
     projected > ceiling -> GateRefusal(code="over_ceiling", breached_value=projected, ceiling=ceiling, source=PREMIUM_SOURCE)
     else -> GateVerdict(projected_tacos=projected, ceiling=ceiling, contribution_margin=CM%)

Worked fixture check (test target): EU-Z87B-ZRBZ CM% = (100.27+0.5)/279.95*100 = 36.0; ceiling = 21.0.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the pure margin/ceiling/projection functions</name>
  <read_first>
    - engine/src/habibos/tacos.py (lines 1-58 module docstring/imports; 80-104 parse; 107-115 _ratio; 118-124 _present_flag — the exact grammar to clone)
    - engine/src/habibos/result.py (the gate types from Plan 01)
    - engine/tests/test_gate.py (the RED contract from Plan 01 — the function signatures the tests call)
    - .planning/phases/07-margin-tiered-safety-gate/07-PATTERNS.md (lines 26-69 — the function bodies to imitate)
    - .planning/phases/07-margin-tiered-safety-gate/07-RESEARCH.md (THE D-03 RESOLUTION; Code Examples)
  </read_first>
  <behavior>
    - contribution_margin_pct(profit_sum, ad_spend_sum, total_sales_sum): EU-Z87B-ZRBZ → 36.0; total_sales_sum==0 → None; any None input → None
    - ceiling_tacos_pct(cm_pct, floor): 36.0, 15 → 21.0
    - projected_tacos_pct(ad_spend_sum, delta_spend, total_sales_sum): None on zero/missing sales; otherwise round((ad_spend_sum+delta_spend)/total_sales_sum*100, 1)
    - the back-out ADDS ad_spend_sum back (D-03): never profit_sum/total_sales_sum raw
  </behavior>
  <action>
    Create engine/src/habibos/gate.py mirroring tacos.py's module shape (docstring citing
    CLAUDE.md hard rule 3 + D-01/D-02/D-03, `from __future__ import annotations`, import
    `parse` and `PREMIUM_SOURCE_ID` from `.tacos`, import the gate types + `PREMIUM_SOURCE`
    from `.result`, import `thresholds`, import `tomllib`). Implement three pure functions
    cloning the `_ratio` None-on-zero-denominator grammar exactly (same
    `round(... * 100, 1)`): `contribution_margin_pct(profit_sum, ad_spend_sum,
    total_sales_sum)` returning `round((profit_sum + ad_spend_sum) / total_sales_sum * 100,
    1)` — ad_spend_sum ADDED BACK (D-03 Path A) — and `None` when total_sales_sum is None or
    0, or when profit_sum/ad_spend_sum is None (Pitfall 4 divide-by-zero → refuse, never 0);
    `ceiling_tacos_pct(cm_pct, floor)` returning `round(cm_pct - floor, 1)` (D-01; caller
    refuses earlier if either is None); `projected_tacos_pct(ad_spend_sum, delta_spend,
    total_sales_sum)` returning `None` on zero/missing sales else
    `round((ad_spend_sum + delta_spend) / total_sales_sum * 100, 1)` (D-06 worst-case, sales
    held flat). Do NOT include ad spend in the cost basis any other way; do NOT clamp anything.
  </action>
  <verify>
    <automated>cd engine && uv run pytest tests/test_gate.py::test_contribution_margin_pre_ad tests/test_gate.py::test_profit_is_post_ad_addback -q</automated>
  </verify>
  <acceptance_criteria>
    - engine/src/habibos/gate.py contains `def contribution_margin_pct(`, `def ceiling_tacos_pct(`, `def projected_tacos_pct(`
    - gate.py imports `parse` from `.tacos` (does not re-implement the CSV reader)
    - a SKU with profit_sum=100.27, ad_spend_sum=0.5, total_sales_sum=279.95 yields contribution_margin_pct == 36.0
    - ceiling_tacos_pct(36.0, 15) == 21.0
    - contribution_margin_pct(p, a, 0) is None for any p, a (divide-by-zero → None, never 0)
    - test_contribution_margin_pre_ad and test_profit_is_post_ad_addback PASS
  </acceptance_criteria>
  <done>The three pure money functions exist, clone the None-on-zero grammar, add ad_spend back, and the CM%/back-out tests are GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement evaluate(ProposedAction) — the branch order + typed verdict/refusal</name>
  <read_first>
    - engine/src/habibos/tacos.py (lines 127-182 classify — the parse → lookup → typed-result orchestration to mirror; lines 118-124 the strict `>` breach grammar)
    - engine/src/habibos/gate.py (Task 1 functions in this same file)
    - engine/config/sku_catalog.toml (the catalog shape to read) and engine/tests/fixtures/sku_catalog_test.toml (the edge fixture the tests pass)
    - engine/tests/test_gate.py (the five remaining RED tests — spend_direction, breach, sku_not_in_catalog, unreliable_cogs, ceiling_and_unset_floor)
    - .planning/phases/07-margin-tiered-safety-gate/07-PATTERNS.md (lines 71-76 — the exact branch order)
  </read_first>
  <behavior>
    - spend-decreasing ProposedAction (delta_spend ≤ 0) → GateVerdict(auto_pass=True), no catalog/margin/floor read (Pitfall 5)
    - sku absent from the catalog → GateRefusal(code="sku_not_in_catalog")
    - catalog cogs ≤ 0/null OR total_sales_sum==0 → GateRefusal(code="no_margin")
    - floor unset (thresholds.read returns None, e.g. marketplace with no key) → GateRefusal(code="no_threshold_set") on every spend-up
    - projected > ceiling → GateRefusal(code="over_ceiling", breached_value=projected, ceiling=ceiling, source=PREMIUM_SOURCE); never a clamped GateVerdict
    - projected == ceiling → PASS (strict `>`); projected < ceiling → PASS
  </behavior>
  <action>
    In gate.py add a `_load_catalog(catalog_path) -> dict` reading sku_catalog.toml with
    `tomllib` (return the `[sku]` table; no silent export-rows fallback — D-05), and
    `evaluate(action: ProposedAction, frame, catalog_path, marketplace="CA") -> GateVerdict |
    GateRefusal` orchestrating the branch order EXACTLY as in the interfaces block: (1)
    spend-direction FIRST — `if not action.is_spend_increasing: return
    GateVerdict(projected_tacos=None, ceiling=None, contribution_margin=None,
    auto_pass=True)`, with no catalog/margin/floor read (Pitfall 5); (2) catalog lookup — SKU
    ∉ catalog → `GateRefusal("SKU not in catalog table", code="sku_not_in_catalog")` (D-05);
    (3) pull this SKU's `ad_spend_sum/profit_sum/total_sales_sum` from the parsed `frame` and
    the catalog `cogs`; if catalog cogs ≤ 0/null/missing OR total_sales_sum == 0 →
    `GateRefusal("no margin / unreliable COGS", code="no_margin", source=PREMIUM_SOURCE)`
    (D-03/D-04, Open Q1); (4) `floor = thresholds.read("min_net_margin_pct", marketplace)`;
    `None` → `GateRefusal("no threshold set", code="no_threshold_set")` (D-07, GATE-02); (5)
    `cm = contribution_margin_pct(...)` (refuse no_margin if it comes back None), `ceiling =
    ceiling_tacos_pct(cm, floor)`, `projected = projected_tacos_pct(ad_spend_sum,
    action.delta_spend, total_sales_sum)`; use the STRICT `>` — `projected > ceiling` →
    `GateRefusal("projected TACOS breaches the SKU's margin-derived ceiling",
    code="over_ceiling", breached_value=projected, ceiling=ceiling, source=PREMIUM_SOURCE)`
    (GATE-04 — name the breached number + source, NEVER return a value clamped to the
    ceiling); else `GateVerdict(projected_tacos=projected, ceiling=ceiling,
    contribution_margin=cm)`. The catalog source must be EXPLICIT (mirror answer_tacos.py
    lines 84-95 discipline) — a missing catalog file is an error, not a silent fallback.
  </action>
  <verify>
    <automated>cd engine && uv run pytest tests/test_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - engine/src/habibos/gate.py contains `def evaluate(` and `def _load_catalog(`
    - a spend-decreasing ProposedAction (delta_spend=-5) returns GateVerdict with auto_pass True and projected_tacos None
    - a ProposedAction for a SKU not in the catalog returns GateRefusal with code == "sku_not_in_catalog"
    - a SKU whose catalog cogs is 0 (or total_sales_sum==0) returns GateRefusal with code == "no_margin"
    - evaluate with a marketplace whose min_net_margin_pct is unset returns GateRefusal code == "no_threshold_set" for a spend-up
    - an over-ceiling spend-up returns GateRefusal code == "over_ceiling" with breached_value == the projected value and source set; isinstance is GateRefusal NOT GateVerdict
    - a projected exactly equal to ceiling returns GateVerdict (strict `>`)
    - `cd engine && uv run pytest tests/test_gate.py -q` exits 0 (all seven tests GREEN)
  </acceptance_criteria>
  <done>evaluate orchestrates the five-step branch order, refuses (never clamps/defaults) on every missing/over-ceiling input, and the full test_gate.py suite is GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression green</name>
  <read_first>
    - engine/tests/test_thresholds.py (the Plan 01 repoint + seeded-floor assertion — now that Plan 02 seeded the floor, the staged assertion must pass)
    - engine/pyproject.toml (the [tool.pytest.ini_options] — testpaths, pythonpath)
  </read_first>
  <action>
    Run the full engine suite and confirm nothing regressed: the Plan 01 seeded-floor
    assertion in test_thresholds.py (`thresholds.read('min_net_margin_pct','CA') == 15.0`) is
    now GREEN because Plan 02 seeded it, the repointed empty-key assertion
    (`days_of_cover_floor`) is GREEN, test_gate.py is fully GREEN, and the pre-existing
    test_tacos.py / test_lifecycle.py / test_find_cache.py suites still pass (the rename only
    touched the one assertion Plan 01 repointed). If any pre-existing test references
    `min_margin_pct`, that is an escaped reference the rename missed — fix it to the new key
    name (do NOT re-introduce the empty key). No money logic changes here — this is the
    Nyquist per-wave-merge gate.
  </action>
  <verify>
    <automated>cd engine && uv run pytest 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cd engine && uv run pytest` exits 0 (full suite green)
    - no test file references `min_margin_pct` (the old empty key) anywhere: `grep -rn "min_margin_pct" engine/ | grep -v "min_net_margin_pct"` returns nothing
    - test_gate.py, test_thresholds.py, test_tacos.py all pass
  </acceptance_criteria>
  <done>The full engine suite is green: gate.py GREEN, the threshold rename clean, no regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ProposedAction (caller-supplied delta_spend) → gate | the write proposal; a spend-up must be projected and gated, never trusted blind |
| premium export frame → gate compute | window-sum components feed the margin; schema drift must fail loud, not coerce |
| catalog + floor config → gate | a missing SKU / 0 COGS / empty floor must each refuse, never default |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Tampering | gate.evaluate over-ceiling path | mitigate (HIGH) | Strict `projected > ceiling` → GateRefusal naming breached_value + source; NEVER returns a value clamped to the ceiling. A spend-up cannot reach DRYRUN without a GateVerdict. Locked by test_breach_refuses_never_clamps. |
| T-07-07 | Repudiation | contribution_margin_pct (fabricated margin) | mitigate (HIGH) | Pre-ad back-out is hand-written + asserted against the committed fixture (test_profit_is_post_ad_addback); None on zero/missing sales → no_margin refusal, never 0/guess. Money math never computed in prose (CLAUDE.md rule 3). |
| T-07-08 | Spoofing | catalog lookup (unfiled SKU funded) | mitigate (HIGH) | SKU ∉ catalog → sku_not_in_catalog refusal BEFORE any margin/floor read; explicit catalog, no silent export-rows fallback (D-05). Locked by test_sku_not_in_catalog_refuses. |
| T-07-09 | Tampering | floor read (defaulted threshold) | mitigate (HIGH) | thresholds.read None → no_threshold_set refusal on every spend-up; never a default 25%. Locked by test_ceiling_and_unset_floor_refuses. |
| T-07-10 | Information Disclosure | gate compute logs | accept | gate.py reads no secret; the seller UUID is never touched (the export artifact is pre-fetched + UUID-sanitized per source-map §6). CLI logging is Plan 04's concern. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pure stdlib + already-pinned polars/pydantic/tomllib. RESEARCH audit confirms; slopcheck N/A. |
</threat_model>

<verification>
- `cd engine && uv run pytest tests/test_gate.py -q` → all seven GREEN
- `cd engine && uv run pytest` → full suite GREEN (per-wave-merge Nyquist gate)
- `grep -rn "min_margin_pct" engine/ | grep -v "min_net_margin_pct"` → empty
</verification>

<success_criteria>
- gate.py is the hand-written money core: pre-ad CM% (ad_spend added back), per-SKU ceiling, worst-case projection, typed verdict/refusal
- every missing/over-ceiling input refuses with the correct typed code, never defaults, never clamps (SC-1..5)
- the full engine suite is green
</success_criteria>

<output>
Create `.planning/phases/07-margin-tiered-safety-gate/07-03-SUMMARY.md` when done.
Record the final evaluate branch order and the rounding/precision chosen for ceiling + projected.
</output>
