---
phase: 07-margin-tiered-safety-gate
plan: 04
type: execute
wave: 3
depends_on: ["07-03"]
files_modified:
  - engine/scripts/gate_action.py
  - engine/tests/test_gate_action.py
autonomous: true
requirements: [GATE-03, GATE-04, GATE-05]

must_haves:
  truths:
    - "gate_action.py turns argv (sku, action-type, delta-spend, artifact, catalog, marketplace) into the gate verdict as JSON on stdout"
    - "the CLI contains NO money logic — it calls gate.evaluate inside logged_call and asdict's the typed result"
    - "a refused action exits with status 'refused' and a passed action with status 'pass' in the structured log; the seller UUID is never logged"
  artifacts:
    - path: "engine/scripts/gate_action.py"
      provides: "thin CLI mirroring answer_tacos.py: argv → gate.evaluate → JSON stdout"
      contains: "gate.evaluate"
    - path: "engine/tests/test_gate_action.py"
      provides: "CLI smoke test — a spend-up over ceiling emits over_ceiling JSON; a spend-down emits auto_pass"
      contains: "def test_"
  key_links:
    - from: "engine/scripts/gate_action.py"
      to: "engine.src.habibos.gate"
      via: "gate.evaluate(...) inside logged_call"
      pattern: "gate\\.evaluate"
    - from: "engine/scripts/gate_action.py"
      to: "engine.src.habibos.logging"
      via: "logged_call('gate_action', ...)"
      pattern: "logged_call"
---

<objective>
Add the thin `gate_action.py` CLI mirroring `answer_tacos.py` so the gate is invocable
directly / by the Phase 8 apply spine: argv → `gate.evaluate()` inside a `logged_call` →
`dataclasses.asdict` → JSON on stdout. No business logic in the script (all money math is
in gate.py). A small CLI smoke test locks the JSON contract.

Purpose: Give Phase 8's BUILD→DRYRUN handoff a callable gate seam; keep the one-in/one-out
structured-logging discipline (never logging the seller UUID).
Output: gate_action.py, test_gate_action.py.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-margin-tiered-safety-gate/07-CONTEXT.md
@.planning/phases/07-margin-tiered-safety-gate/07-RESEARCH.md
@.planning/phases/07-margin-tiered-safety-gate/07-PATTERNS.md

<interfaces>
<!-- The CLI shape to mirror, extracted from engine/scripts/answer_tacos.py: -->

answer_tacos.py structure (lines 1-127):
  REPO_ROOT = Path(__file__).resolve().parent.parent
  DEFAULT_CATALOG = REPO_ROOT / "config" / "..."         # -> config/sku_catalog.toml for the gate
  _build_parser(): --artifact, --export-id, --marketplace (default CA), --status, --catalog
  main(argv): habibos_logging.configure(); parse argv;
    with habibos_logging.logged_call("gate_action", source_id=..., marketplace=...) as end_fields:
        result = gate.evaluate(...)
        end_fields["status"] = "refused" if isinstance(result, GateRefusal) else "pass"
    json.dump(dataclasses.asdict(result), sys.stdout); sys.stdout.write("\n"); return 0

From engine/src/habibos/gate.py (Plan 03):
  PREMIUM_SOURCE_ID, parse, evaluate(action, frame, catalog_path, marketplace="CA")
From engine/src/habibos/result.py:
  ProposedAction(sku, action_type, delta_spend, marketplace="CA"), GateRefusal, GateVerdict
From engine/src/habibos/logging.py:
  configure(); logged_call(op, **start_fields) -> yields mutable end_fields dict   # never log seller UUID
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the thin gate_action.py CLI</name>
  <read_first>
    - engine/scripts/answer_tacos.py (read in full — the shape to mirror: REPO_ROOT/DEFAULT_CATALOG resolution lines 37+, _build_parser lines 52-67, the logged_call wrapper lines 97-127, the explicit-catalog discipline lines 84-95)
    - engine/src/habibos/gate.py (the evaluate signature + parse import from Plan 03)
    - engine/src/habibos/result.py (GateRefusal/GateVerdict/ProposedAction)
    - engine/src/habibos/logging.py (logged_call / configure signatures)
    - .planning/phases/07-margin-tiered-safety-gate/07-PATTERNS.md (lines 133-167 — the exact CLI shape)
  </read_first>
  <action>
    Create engine/scripts/gate_action.py mirroring answer_tacos.py exactly: shebang,
    `from __future__ import annotations`, import argparse/dataclasses/json/sys + Path, import
    `habibos.logging as habibos_logging`, `from habibos.result import GateRefusal, GateVerdict,
    ProposedAction`, `from habibos import gate`. Set `REPO_ROOT =
    Path(__file__).resolve().parent.parent` and `DEFAULT_CATALOG = REPO_ROOT / "config" /
    "sku_catalog.toml"`. `_build_parser()` adds `--artifact` (the downloaded premium export),
    `--export-id`, `--marketplace` (default CA), `--status` (default COMPLETED), `--catalog`
    (default DEFAULT_CATALOG), plus the ProposedAction fields `--sku` (required),
    `--action-type` (required), `--delta-spend` (required, float). `main(argv)` calls
    `habibos_logging.configure()` once, parses argv, calls `gate.parse(artifact, status)` to
    build the frame (surfacing a parse Refusal as JSON if it returns one), builds
    `ProposedAction(sku, action_type, delta_spend, marketplace)`, then inside
    `with habibos_logging.logged_call("gate_action", source_id=gate.PREMIUM_SOURCE_ID,
    marketplace=args.marketplace) as end_fields:` calls `gate.evaluate(action, frame,
    catalog_path, marketplace)` and sets `end_fields["status"] = "refused" if
    isinstance(result, GateRefusal) else "pass"`. Finally `json.dump(dataclasses.asdict(result),
    sys.stdout); sys.stdout.write("\n"); return 0`. NO ratios, NO ceiling math, NO decisions in
    the script — all logic is in gate.py. NEVER pass the seller UUID into logged_call fields
    (the gate reads no secret). The catalog source is EXPLICIT — a missing catalog file is an
    error surfaced cleanly, not a silent fallback (D-05).
  </action>
  <verify>
    <automated>cd engine && uv run python scripts/gate_action.py --artifact tests/fixtures/profit_by_sku_30d.csv --export-id testxx --sku EU-Z87B-ZRBZ --action-type pause --delta-spend -5 2>/dev/null | uv run python -c "import sys,json; d=json.load(sys.stdin); assert d.get('auto_pass') is True; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - engine/scripts/gate_action.py contains `gate.evaluate(` and `logged_call(`
    - the CLI contains NO ratio/ceiling/projection arithmetic (grep for `/ ` division in money context returns nothing; all math is imported from gate)
    - running it with a spend-decreasing action (--delta-spend -5) emits JSON with `auto_pass: true`
    - the logged_call op name is "gate_action" and no field carries the seller UUID
    - the verify command prints `OK` and exits 0
  </acceptance_criteria>
  <done>gate_action.py is a thin argv→evaluate→JSON wrapper with structured logging and no embedded money logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CLI smoke test locking the JSON verdict/refusal contract</name>
  <read_first>
    - engine/scripts/gate_action.py (Task 1 — its main(argv) entrypoint + JSON output shape)
    - engine/tests/test_gate.py (the verdict/refusal expectations to align the CLI assertions with)
    - engine/tests/conftest.py (existing fixtures/helpers — reuse the repo-root + fixture resolution)
    - engine/scripts/answer_tacos.py (how its CLI is invoked/tested, if a test exists, to mirror)
  </read_first>
  <behavior>
    - a spend-decreasing invocation emits JSON with auto_pass == true (GateVerdict)
    - a spend-increasing over-ceiling invocation emits JSON carrying code == "over_ceiling" and a non-null breached_value + source (GateRefusal)
    - a SKU not in the catalog emits JSON with code == "sku_not_in_catalog"
  </behavior>
  <action>
    Create engine/tests/test_gate_action.py invoking the CLI via `gate_action.main([...])`
    (capture stdout with capsys, parse the JSON) using the committed
    tests/fixtures/profit_by_sku_30d.csv and a catalog fixture. Assert three contract paths:
    (1) a spend-decreasing action (`--delta-spend` negative) → parsed JSON `auto_pass` is
    True; (2) a spend-increasing action whose Δspend pushes projected over the SKU's ceiling →
    parsed JSON `code == "over_ceiling"` with a non-null `breached_value` and `source` (GATE-04
    surfaced through the CLI); (3) a `--sku` absent from the catalog → parsed JSON
    `code == "sku_not_in_catalog"` (GATE-05). Point `--catalog` at the real
    config/sku_catalog.toml (or the test fixture from Plan 02) so the CLI exercises the live
    catalog read. Keep the test fast (no network — the fixture is local).
  </action>
  <verify>
    <automated>cd engine && uv run pytest tests/test_gate_action.py -q</automated>
  </verify>
  <acceptance_criteria>
    - engine/tests/test_gate_action.py exists and invokes gate_action.main(...) capturing stdout
    - it asserts the spend-decreasing path emits auto_pass true
    - it asserts the over-ceiling path emits code == "over_ceiling" with non-null breached_value and source
    - it asserts the absent-SKU path emits code == "sku_not_in_catalog"
    - `cd engine && uv run pytest tests/test_gate_action.py -q` exits 0
    - `cd engine && uv run pytest` (full suite) exits 0
  </acceptance_criteria>
  <done>The CLI JSON contract is locked by an automated test across pass / over-ceiling-refusal / absent-SKU paths; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| argv (operator/agent-supplied sku/delta-spend) → CLI | untrusted write proposal crosses into the gate |
| gate verdict → JSON stdout | the verdict must be surfaced verbatim; a refusal must serialize as a refusal, never as a pass |
| CLI op → structured log | the seller UUID must never enter the log fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11 | Tampering | gate_action.py JSON serialization | mitigate | The CLI dataclasses.asdict's the typed result and sets end_fields["status"] from isinstance(GateRefusal) — a refusal can never serialize as a pass. Locked by test_gate_action.py over-ceiling path. |
| T-07-12 | Information Disclosure | logged_call fields | mitigate | logged_call carries only op name + source_id + marketplace; the seller UUID is never passed (existing one-in/one-out contract). Asserted by the no-UUID acceptance criterion. |
| T-07-13 | Repudiation | CLI bypasses gate math | mitigate | The CLI contains NO money logic — all ratios/ceiling/projection are imported from gate.py (CLAUDE.md rule 3). Acceptance criterion greps for embedded arithmetic. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs — stdlib argparse/json/dataclasses + already-pinned engine stack. RESEARCH audit confirms; slopcheck N/A. |
</threat_model>

<verification>
- `cd engine && uv run python scripts/gate_action.py --artifact tests/fixtures/profit_by_sku_30d.csv --export-id x --sku EU-Z87B-ZRBZ --action-type pause --delta-spend -5` → JSON with auto_pass true
- `cd engine && uv run pytest tests/test_gate_action.py -q` → GREEN
- `cd engine && uv run pytest` → full suite GREEN
</verification>

<success_criteria>
- gate_action.py is a thin, logged, no-business-logic CLI surfacing the typed gate verdict as JSON
- the CLI JSON contract is locked by an automated test (pass / over-ceiling refusal / absent-SKU refusal)
- the seller UUID never enters the structured log
</success_criteria>

<output>
Create `.planning/phases/07-margin-tiered-safety-gate/07-04-SUMMARY.md` when done.
Note the CLI invocation signature (the full argv flag set) for the Phase 8 apply spine to consume.
</output>
