---
phase: 07-margin-tiered-safety-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/config/sku_catalog.toml
  - engine/config/thresholds.toml
  - engine/tests/fixtures/sku_catalog_test.toml
  - .claude/skills/anabtawi-context/SKILL.md
autonomous: true
requirements: [GATE-02, GATE-05]

must_haves:
  truths:
    - "sku_catalog.toml is parseable TOML with one row per SKU carrying sku/asin/tier/cogs"
    - "the floor key min_net_margin_pct = 15 exists under [marketplace.CA] and thresholds.read returns 15.0"
    - "inactive-remnant SKUs with no COGS are either absent from the catalog or carry cogs that triggers a no-margin refusal"
    - "the prose SKU table in SKILL.md notes it renders FROM sku_catalog.toml (single source of truth, D-05)"
  artifacts:
    - path: "engine/config/sku_catalog.toml"
      provides: "machine-readable per-SKU tier+COGS+canonical SKU set (D-04)"
      contains: "tier"
    - path: "engine/config/thresholds.toml"
      provides: "seeded net-margin floor min_net_margin_pct = 15 (D-07)"
      contains: "min_net_margin_pct = 15"
    - path: "engine/tests/fixtures/sku_catalog_test.toml"
      provides: "edge fixtures (a 0-COGS SKU, an absent SKU) for the no_margin / sku_not_in_catalog tests"
      contains: "cogs"
    - path: ".claude/skills/anabtawi-context/SKILL.md"
      provides: "render-from-TOML note (D-05 single source of truth)"
      contains: "sku_catalog.toml"
  key_links:
    - from: "engine/config/thresholds.toml"
      to: "engine/src/habibos/thresholds.py"
      via: "read('min_net_margin_pct','CA')"
      pattern: "min_net_margin_pct"
    - from: ".claude/skills/anabtawi-context/SKILL.md"
      to: "engine/config/sku_catalog.toml"
      via: "render-from note"
      pattern: "sku_catalog\\.toml"
---

<objective>
Author the machine-readable single source of truth the gate consumes and clear the
Phase 7 blocking precondition. Create `engine/config/sku_catalog.toml` (D-04: per-SKU
sku/asin/tier/cogs, reconciling the three existing SKU sources), seed
`min_net_margin_pct = 15` in `thresholds.toml` (D-07/D-07a, renaming the empty
`min_margin_pct`), create the small edge-case catalog fixture the Plan 01 tests reference,
and add the D-05 render-from note to the prose SKU table in `anabtawi-context/SKILL.md`.

Purpose: Provide the catalog + floor data the gate reads live; establish the single source
of truth so the prose table can never drift from the gate's input.
Output: New sku_catalog.toml, edited thresholds.toml, new test fixture, SKILL.md note.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-margin-tiered-safety-gate/07-CONTEXT.md
@.planning/phases/07-margin-tiered-safety-gate/07-RESEARCH.md
@.planning/phases/07-margin-tiered-safety-gate/07-PATTERNS.md

<interfaces>
<!-- The COGS authority: prose table in .claude/skills/anabtawi-context/SKILL.md lines 26-53. -->
<!-- 26 detailed rows (sku | asin | product | tier | cogs | note) + a line-53 remnant group. -->
<!-- Reconcile against engine/tests/fixtures/ca_catalog_skus.txt (30 SKUs). -->

Remnant SKUs WITHOUT COGS (SKILL.md line 53) — under D-03/D-05 these must REFUSE on funding:
  ZK-4NDS-MNA9, OA-26MX-IHV0, 5G-ZW6Q-WOZG, 9J-ASSK-BVKC, FO-SE3J-T74M
  -> either omit from the catalog (=> sku_not_in_catalog refusal) OR carry cogs = 0 (=> no_margin refusal).
  Pick ONE and state it in the SUMMARY; both are correct, surfaced behavior (D-05).

thresholds.read contract (engine/src/habibos/thresholds.py): an empty "" or absent value
  returns None; a numeric value is coerced to float. No change to thresholds.py.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author sku_catalog.toml from the prose COGS authority</name>
  <read_first>
    - .claude/skills/anabtawi-context/SKILL.md (lines 26-53 — the COGS authority: per-SKU sku/asin/tier/cogs; line 53 the no-COGS remnant group; lines 56-57 the "0/null COGS => unreliable" note)
    - engine/tests/fixtures/ca_catalog_skus.txt (the 30-SKU canonical set to reconcile against)
    - engine/config/thresholds.toml (TOML format/header-comment style to imitate)
    - .planning/phases/07-margin-tiered-safety-gate/07-PATTERNS.md (lines 171-192 — suggested TOML shape + reconciliation obligation)
    - docs/datadoe-source-map.md (the cogs_total_value / landed-COGS provenance for the header note)
  </read_first>
  <action>
    Create engine/config/sku_catalog.toml as the single machine-readable source of truth
    (D-04/D-05). Use a `[sku."<SKU>"]` table per row carrying exactly `asin`, `tier`, `cogs`
    (landed cogs_total_value, CAD) — store ONLY these plus the implicit SKU key; do NOT store
    a per-SKU margin (D-04: margin is computed live in Plan 03). Populate from the prose table
    in anabtawi-context/SKILL.md lines 27-52 (the 26 detailed rows with their tier + COGS;
    e.g. T8-2W2X-INOK / B0FXX3JVR5 / HERO / 26.77). Reconcile the three sources: the prose
    table is the COGS authority; ensure every SKU in ca_catalog_skus.txt that has a real COGS
    in the prose table is present. For the line-53 remnant group
    (ZK-4NDS-MNA9, OA-26MX-IHV0, 5G-ZW6Q-WOZG, 9J-ASSK-BVKC, FO-SE3J-T74M) which have NO COGS:
    OMIT them from the catalog so a funding attempt yields a sku_not_in_catalog refusal
    (D-05) — and write a header comment documenting this choice and the reconciliation. Open
    the file with a policy header comment (mirror thresholds.toml lines 1-23 style): state it
    is the single source of truth, that the prose SKILL.md table renders from it, that margin
    is computed live not stored, and the cogs_total_value (export b18aadfd, landed) provenance.
  </action>
  <verify>
    <automated>cd engine && uv run python -c "import tomllib; d=tomllib.load(open('config/sku_catalog.toml','rb')); rows=d['sku']; print(len(rows)); assert rows['T8-2W2X-INOK']['cogs']==26.77 and rows['T8-2W2X-INOK']['tier']=='HERO'; assert 'OA-26MX-IHV0' not in rows; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - engine/config/sku_catalog.toml parses as TOML (tomllib.load succeeds)
    - every row has keys `asin`, `tier`, `cogs`; NO `margin` key exists anywhere in the file
    - `sku."T8-2W2X-INOK"` has cogs == 26.77 and tier == "HERO"
    - the five no-COGS remnant SKUs (OA-26MX-IHV0, 5G-ZW6Q-WOZG, 9J-ASSK-BVKC, FO-SE3J-T74M, ZK-4NDS-MNA9) are absent from the [sku] table
    - a header comment states: single source of truth, prose renders from it, margin computed live, cogs landed provenance
    - the verify command prints the row count then `OK` and exits 0
  </acceptance_criteria>
  <done>The catalog is a parseable per-SKU tier+COGS table with no stored margin; no-COGS remnants are omitted so they refuse on funding.</done>
</task>

<task type="auto">
  <name>Task 2: Seed the net-margin floor + create edge fixtures + remove the stale tier-ceiling comment</name>
  <read_first>
    - engine/config/thresholds.toml (line 27 min_margin_pct=""; lines 22-23 the superseded tier-ceiling comment)
    - .planning/phases/07-margin-tiered-safety-gate/07-PATTERNS.md (lines 196-222 — the seed + the deprecated-comment removal)
    - .planning/phases/07-margin-tiered-safety-gate/07-RESEARCH.md (D-07/D-07a; State of the Art — the hero35/maintain28/cut18 comment is superseded by D-01a)
  </read_first>
  <action>
    In engine/config/thresholds.toml under [marketplace.CA], rename `min_margin_pct = ""` to
    `min_net_margin_pct = 15` (D-07/D-07a rename-in-place; the gate reads
    `min_net_margin_pct`). Keep the explanatory comment (minimum acceptable net margin % the
    gate protects). Update or remove the lines 22-23 comment that anticipates "Tier-aware
    ceilings (hero 35 / maintain 28 / cut 18) activate once the SKU→tier table is filled" —
    it is superseded by D-01a: there is NO per-tier ceiling table; the gate is one floor +
    the per-SKU formula `ceiling = CM% − min_net_margin_pct`. Replace it with a one-line note
    pointing at sku_catalog.toml + the gate formula. Then create
    engine/tests/fixtures/sku_catalog_test.toml — a tiny catalog the Plan 01 edge tests use:
    include (a) one SKU present in the premium-export fixture with a valid cogs (e.g.
    EU-Z87B-ZRBZ / cogs 29.06) for the happy-path/breach tests, (b) one SKU with `cogs = 0`
    to drive the no_margin refusal, and deliberately OMIT a SKU the tests will try to fund to
    drive the sku_not_in_catalog refusal. Match the sku_catalog.toml shape exactly so the gate
    parser is fixture-agnostic.
  </action>
  <verify>
    <automated>cd engine && uv run pytest tests/test_thresholds.py::test_missing_threshold_returns_none -q 2>&1 | tail -2 && uv run python -c "from habibos import thresholds; assert thresholds.read('min_net_margin_pct','CA')==15.0; import tomllib; t=tomllib.load(open('tests/fixtures/sku_catalog_test.toml','rb'))['sku']; assert any(r['cogs']==0 for r in t.values()); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - engine/config/thresholds.toml contains `min_net_margin_pct = 15` under [marketplace.CA] and no longer contains `min_margin_pct`
    - `thresholds.read('min_net_margin_pct','CA')` returns 15.0
    - the "hero 35 / maintain 28 / cut 18" tier-ceiling comment is removed/replaced (grep for "hero 35" returns nothing)
    - engine/tests/fixtures/sku_catalog_test.toml parses, contains at least one SKU with cogs == 0, and contains EU-Z87B-ZRBZ with a non-zero cogs
    - the verify command prints `OK` and exits 0
  </acceptance_criteria>
  <done>The floor is seeded (clearing the precondition), the stale tier-ceiling comment is gone, and the edge fixtures exist for the Plan 01 no_margin / sku_not_in_catalog tests.</done>
</task>

<task type="auto">
  <name>Task 3: Add the D-05 render-from note to SKILL.md</name>
  <read_first>
    - .claude/skills/anabtawi-context/SKILL.md (lines 22-57 — the prose SKU table + its existing 2026-06-12 provenance note)
    - .planning/phases/07-margin-tiered-safety-gate/07-RESEARCH.md (Runtime State Inventory — the two-direction obligation; D-05)
  </read_first>
  <action>
    Add a short note immediately above the prose SKU table in
    .claude/skills/anabtawi-context/SKILL.md stating that this table is now a RENDERING of
    `engine/config/sku_catalog.toml`, which is the single machine-readable source of truth
    the margin gate reads (D-04/D-05); edits to tier/COGS should be made in the TOML and
    mirrored here, not the reverse. Do NOT delete or restructure the prose rows (they stay
    human-readable); only add the render-from note so the SKU set + COGS can never silently
    diverge from the gate's input. Keep the existing "COGS = cogs_total_value … landed"
    provenance line.
  </action>
  <verify>
    <automated>grep -q "sku_catalog.toml" .claude/skills/anabtawi-context/SKILL.md && grep -qi "source of truth\|renders\|rendering" .claude/skills/anabtawi-context/SKILL.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/anabtawi-context/SKILL.md references `engine/config/sku_catalog.toml` as the single source of truth above the SKU table
    - the note states the prose table renders FROM the TOML (D-05)
    - the existing prose SKU rows (lines 27-52) are not deleted
    - the verify command prints `OK` and exits 0
  </acceptance_criteria>
  <done>The prose SKU table is documented as a rendering of sku_catalog.toml, closing the D-05 drift risk.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator config (sku_catalog.toml / thresholds.toml) → engine | the canonical SKU set + floor; a missing SKU or empty floor must refuse, never default |
| prose SKILL.md ↔ machine catalog | drift between human table and gate input is a silent-funding risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Spoofing | sku_catalog.toml canonical set | mitigate | A SKU absent from the catalog yields sku_not_in_catalog refusal (D-05) — no SKU can be silently funded by living only in the export rows. No-COGS remnants are omitted, so they refuse. Locked by test_sku_not_in_catalog_refuses (Plan 01). |
| T-07-04 | Tampering | thresholds.toml floor | mitigate | Floor seeded as a reviewed committed number; thresholds.read's refuse-on-empty contract unchanged so an accidental blanking re-arms the "no threshold set" refusal (locked by test_ceiling_and_unset_floor_refuses). |
| T-07-05 | Repudiation | prose↔TOML drift | mitigate | D-05 render-from note makes the TOML authoritative; the prose table can no longer silently disagree with the gate input. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs — config/data + a doc note only. RESEARCH audit confirms stack is pinned + prior-audited. |
</threat_model>

<verification>
- `cd engine && uv run python -c "import tomllib; tomllib.load(open('config/sku_catalog.toml','rb'))"` → parses
- `cd engine && uv run python -c "from habibos import thresholds; print(thresholds.read('min_net_margin_pct','CA'))"` → 15.0
- `grep -c "hero 35" engine/config/thresholds.toml` → 0 (stale comment removed)
- the seeded-floor assertion staged in Plan 01 Task 3 now passes
</verification>

<success_criteria>
- sku_catalog.toml is the parseable single source of truth (tier + COGS + canonical set; no stored margin)
- min_net_margin_pct = 15 clears the Phase 7 blocking precondition; thresholds.read returns 15.0
- edge fixtures exist for the no_margin / sku_not_in_catalog refusal tests
- SKILL.md prose table documented as a rendering of the TOML (D-05)
</success_criteria>

<output>
Create `.planning/phases/07-margin-tiered-safety-gate/07-02-SUMMARY.md` when done.
State explicitly: how the no-COGS remnant group was handled (omit vs cogs=0) and the final
catalog SKU count vs the 30-SKU ca_catalog_skus.txt reconciliation.
</output>
