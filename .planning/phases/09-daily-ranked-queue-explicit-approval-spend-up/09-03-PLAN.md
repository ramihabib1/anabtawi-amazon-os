---
phase: 09-daily-ranked-queue-explicit-approval-spend-up
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - engine/src/habibos/result.py
  - engine/src/habibos/rank_queue.py
autonomous: true
requirements: [QUEUE-01]
user_setup: []

must_haves:
  truths:
    - "Proposed PPC changes are ranked by expected weekly $ impact, descending"
    - "A change with no defensible $ estimate sorts LAST, with a None estimate (never a fabricated 0)"
    - "Every ranked row carries provenance citing the DataDoe source + window behind its numbers"
  artifacts:
    - path: "engine/src/habibos/result.py"
      provides: "QueueRow dataclass (mirrors SkuRow) for ranker output"
      contains: "class QueueRow"
    - path: "engine/src/habibos/rank_queue.py"
      provides: "expected-$/wk estimator + dollars-first ranker reusing the tacos.classify sort idiom"
      contains: "def rank"
      min_lines: 60
  key_links:
    - from: "engine/src/habibos/rank_queue.py"
      to: "engine/src/habibos/result.py"
      via: "from .result import QueueRow"
      pattern: "QueueRow"
    - from: "engine/tests/test_rank_queue.py"
      to: "engine/src/habibos/rank_queue.py"
      via: "from habibos import rank_queue"
      pattern: "rank_queue"
---

<objective>
Build the pure, hand-written, pytest-covered queue ranker — the expected-weekly-$ estimator plus
the dollars-first sort — and add the `QueueRow` typed result it emits. Turns Plan 01's
`test_rank_queue.py` GREEN.

Purpose: QUEUE-01 / SC-1 — the daily queue renders proposed PPC changes "dollar-ranked … no-$-
estimate rows last." This is the genuinely new money logic of the phase: an expected-$/wk impact
estimator that MUST be hand-written + pytest-covered, provenance-cited, and return `None` (never
a guess) when the estimate is undefined (CLAUDE.md hard rules 3 + 4). It reuses the
`tacos.classify` dollars-first sort idiom verbatim (D-07) and the `tacos._ratio` None-on-undefined
grammar. The estimator's exact formula is Claude's discretion (CONTEXT) but MUST be traceable to
reviewed code and the candidate components — never a chat estimate. Engine stays pure (the agent
feeds candidate data + live components in).

Output: a `QueueRow` dataclass in `result.py`, `engine/src/habibos/rank_queue.py`, and a GREEN
`test_rank_queue.py`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-daily-ranked-queue-explicit-approval-spend-up/09-CONTEXT.md
@.planning/phases/09-daily-ranked-queue-explicit-approval-spend-up/09-PATTERNS.md

<interfaces>
The SkuRow analog to mirror for QueueRow (result.py:174-189):
@dataclass(frozen=True)
class SkuRow: seller_sku; acos; tacos; roi: float|None; acos_flag; tacos_flag: str; suspect=False

The dollars-first sort idiom to REUSE (tacos.py:175, this IS D-07):
rows.sort(key=lambda x: (x.tacos is None, -(x.tacos or 0)))   # None rows LAST, highest first
For Phase 9 the key becomes: (x.expected_weekly_usd is None, -(x.expected_weekly_usd or 0))

The None-on-undefined ratio idiom to CLONE (tacos.py:107-115, three witnesses incl. gate.py:69-73,97-101):
def _ratio(num, den): return None if (num is None or den is None or den == 0) else round(num/den*100, 1)
The estimator clones this "zero/missing denominator -> None, never 0 or a guess" shape.

The per-SKU row-build loop pattern (tacos.py:151-173):
present = {r["sku"]: r for r in frame.to_dicts()}; iterate candidates; build a typed row each.

Provenance is non-optional (Answer.export_id at result.py:200-204; tacos.py:176-182):
every returned row names the DataDoe source + window behind the number (D-07, hard rule 5).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the QueueRow dataclass to result.py</name>
  <files>engine/src/habibos/result.py</files>
  <read_first>
    - engine/src/habibos/result.py:1-8,174-205 (the module anti-fabrication contract + SkuRow/Answer dataclasses to mirror)
    - engine/tests/test_rank_queue.py (the RED contract — shows exactly which QueueRow fields the ranker emits)
    - .planning/phases/09-daily-ranked-queue-explicit-approval-spend-up/09-PATTERNS.md (Shared Patterns -> Typed-result trust spine, lines 195-198)
  </read_first>
  <action>
    Add a frozen `QueueRow` dataclass to `engine/src/habibos/result.py` immediately after `SkuRow`,
    mirroring SkuRow's frozen style + None-means-undefined docstring convention. Fields the renderer
    and tests need (D-07 row schema): `sku: str`, `entity: str` (the campaign/target/adGroup label),
    `action_type: str` (pause | negative | bid_down | bid_up | budget_up | harvest | negative_phrase),
    `current: float | None` (current bid/budget), `proposed: float | None` (proposed bid/budget),
    `expected_weekly_usd: float | None` (the estimator output — None when undefined, NEVER 0/guess),
    `provenance: str` (the DataDoe source + window behind the numbers), and `cls: str = "auto"` (the
    reversibility class the renderer in Plan 04 sets: "auto" | "needs-approval"). Docstring: an
    `expected_weekly_usd` of None means "no defensible estimate" and sorts LAST (D-07), distinct from
    0. Do NOT add a new refusal type (the inventory/conversion refusals reuse GateRefusal). Do NOT
    change any existing dataclass.
  </action>
  <verify>
    <automated>cd engine &amp;&amp; uv run python -c "from habibos.result import QueueRow; r=QueueRow(sku='X', entity='c', action_type='bid_up', current=1.0, proposed=1.5, expected_weekly_usd=None, provenance='src'); assert r.expected_weekly_usd is None and r.cls=='auto'; print('QueueRow ok')"</automated>
  </verify>
  <acceptance_criteria>
    - result.py contains `class QueueRow` as a frozen dataclass
    - QueueRow carries fields: sku, entity, action_type, current, proposed, expected_weekly_usd, provenance, cls
    - `expected_weekly_usd` is typed `float | None`; `cls` defaults to "auto"
    - importing QueueRow does not break existing imports: `cd engine && uv run pytest tests/test_tacos.py tests/test_gate.py -q` stays green
  </acceptance_criteria>
  <done>QueueRow exists in result.py mirroring SkuRow; the ranker and renderer have their typed output contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement rank_queue.py — the expected-$/wk estimator + dollars-first ranker</name>
  <files>engine/src/habibos/rank_queue.py</files>
  <read_first>
    - engine/src/habibos/tacos.py:1-31,107-115,127-182 (the module anti-fabrication banner; `_ratio` None-on-undefined; the classify row-build loop + the D-07 sort key at line 175 + provenance at 176-182)
    - engine/src/habibos/result.py (QueueRow added in Task 1 — the output type)
    - engine/tests/test_rank_queue.py (the RED contract this task turns GREEN — the fixture, the sort assertion, the None-not-0 assertion, the provenance assertion)
    - engine/tests/fixtures/queue_candidates.json (the candidate set the estimator consumes — from Plan 01)
    - .planning/phases/09-daily-ranked-queue-explicit-approval-spend-up/09-PATTERNS.md (the rank_queue.py section, lines 31-57; No-Analog table line 244 on the estimator formula)
  </read_first>
  <action>
    Create `engine/src/habibos/rank_queue.py` as a PURE money-core module (no socket, no fetch).
    Open with the hard-rule-3 / hard-rule-4 banner (mirror tacos.py:1-31) stating: this is a hand-
    written, pytest-covered money module; a candidate with no defensible $ estimate gets
    `expected_weekly_usd = None`, NEVER a guess (D-07, SC-1); every row cites provenance.
    Implement two functions:
      (1) `estimate_weekly_usd(candidate: dict) -> float | None` — the expected-weekly-$ impact
      estimator. Clone the `tacos._ratio` None-on-undefined shape EXACTLY: any zero/missing
      denominator or missing component returns None ("undefined"), never 0 or a substituted value.
      Choose a defensible formula from the candidate's own components (Claude's discretion per CONTEXT
      — e.g. for a bid/budget raise, projected incremental weekly sales-at-the-SKU's-conversion minus
      the incremental spend, scaled to a 7-day window; document the formula in the docstring and tie
      each input to a candidate field so the test can rebuild the expected value). The formula is
      reviewed code, not prose.
      (2) `rank(candidates: list[dict], source: str, window_from: str, window_to: str) -> list[QueueRow]`
      — iterate candidates (mirror the tacos.classify present-loop), build a QueueRow per candidate
      with `expected_weekly_usd = estimate_weekly_usd(candidate)` and `provenance = f"{source} ({window_from}
      -> {window_to})"`, then sort with the D-07 key `key=lambda x: (x.expected_weekly_usd is None,
      -(x.expected_weekly_usd or 0))` so highest expected-$/wk is first and None-estimate rows are LAST.
      Leave `cls` at its QueueRow default ("auto") — Plan 04's queue.py sets the real class tag. Add
      `__all__ = ["estimate_weekly_usd", "rank"]`. Do NOT compute any reversibility class here (that is
      queue.py's job), do NOT render (no formatting), do NOT open a socket.
  </action>
  <verify>
    <automated>cd engine &amp;&amp; uv run pytest tests/test_rank_queue.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - rank_queue.py contains `def estimate_weekly_usd(` and `def rank(`
    - the estimator returns None (not 0) on a zero/missing denominator — assert in test_rank_queue.py
    - `rank(...)` output is sorted by expected_weekly_usd descending with None rows last (the D-07 key)
    - every returned QueueRow has a non-empty `provenance` citing source + window
    - rank_queue.py contains NO `import requests` / `http` / socket / fetch (engine opens no socket)
    - `cd engine && uv run pytest tests/test_rank_queue.py` exits 0
  </acceptance_criteria>
  <done>rank_queue.rank emits dollar-ranked QueueRows (highest expected-$/wk first, None-estimate last), each provenance-cited; the estimator returns None on undefined input; test_rank_queue.py is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent candidate data -> rank_queue | the agent's MCP-derived candidate components cross into the estimator; a missing component must not produce a fabricated $ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Repudiation | expected_weekly_usd estimator | mitigate | the estimate is hand-written + pytest-covered (the test rebuilds it from the candidate's own components, test_tacos `_pct`-mirror pattern); a fabricated $ cannot pass review (hard rule 3) |
| T-09-03-02 | Spoofing | undefined estimate | mitigate | zero/missing denominator -> None (clones tacos._ratio), never a guessed 0; None rows sort last, never silently dropped (D-07, hard rule 4) |
| T-09-03-03 | Information Disclosure | provenance string | mitigate | every QueueRow cites the DataDoe source + window; no seller UUID enters the row (provenance is source-id + dates only) |
| T-09-03-SC | Tampering | npm/pip/cargo installs | accept | no package installs — reuses polars/existing deps; nothing added to uv/pip |
</threat_model>

<verification>
- `cd engine && uv run pytest tests/test_rank_queue.py -q` exits 0.
- `cd engine && grep -nE "requests|urllib|http\\.client|socket|fetch" src/habibos/rank_queue.py` returns nothing.
- `cd engine && uv run pytest -q` stays green (result.py addition is additive; existing dataclasses untouched).
</verification>

<success_criteria>
- QueueRow exists in result.py; rank_queue.py emits dollar-ranked, provenance-cited QueueRows.
- The estimator returns None (never a guess) on undefined input; None rows sort last.
- test_rank_queue.py is GREEN and the full suite stays green.
</success_criteria>

<output>
Create `.planning/phases/09-daily-ranked-queue-explicit-approval-spend-up/09-03-SUMMARY.md` when done.
</output>
