---
phase: 09-daily-ranked-queue-explicit-approval-spend-up
plan: 04
type: execute
wave: 3
depends_on: ["09-03"]
files_modified:
  - engine/src/habibos/queue.py
autonomous: true
requirements: [QUEUE-01, QUEUE-02, QUEUE-03]
user_setup: []

must_haves:
  truths:
    - "Each ranked row is tagged auto (standing approval) or needs-approval (explicit) by blast radius"
    - "Spend-ups and phrase/broad negatives tag needs-approval; pauses/exact-negatives/bid-downs tag auto (D-02: phrase/broad negatives require explicit approval, by blast radius)"
    - "The rendered queue is one dollar-ranked table: rank | SKU | entity | action | current -> proposed | expected $/wk | provenance | class (D-07: one dollar-ranked class-tagged table, no-$ rows last)"
    - "The renderer computes no money math — it tags + formats already-ranked typed rows (D-04: pure renderer, no socket)"
  artifacts:
    - path: "engine/src/habibos/queue.py"
      provides: "class-tagger (auto vs needs-approval) + dollar-ranked table renderer over QueueRow"
      contains: "def render"
      min_lines: 50
  key_links:
    - from: "engine/src/habibos/queue.py"
      to: "engine/src/habibos/result.py"
      via: "from .result import QueueRow"
      pattern: "QueueRow"
    - from: "engine/tests/test_queue.py"
      to: "engine/src/habibos/queue.py"
      via: "from habibos import queue"
      pattern: "queue.classify_row|queue.render"
---

<objective>
Build the queue renderer + reversibility class-tagger — the layer that tags each ranked QueueRow
`auto` (standing approval) or `needs-approval` (explicit approval) by blast radius, and renders
the one dollar-ranked, class-tagged table (D-07). Turns Plan 01's `test_queue.py` GREEN.

Purpose: QUEUE-01 (the rendered table), QUEUE-02 (reversibility-classed autonomy: reversibles
auto, spend-ups + phrase/broad negatives explicit), QUEUE-03 (the magnitude cap belongs to the
auto class). The class tag is derived from action_type + blast radius exactly as
`ProposedAction.is_spend_increasing` derives a flag from data (result.py:92-94): spend-UP and
phrase/broad negatives -> needs-approval (D-02, blast-radius driven); pause / exact-match negative
/ bid_down / contested-term pause -> auto (D-01, standing approval). The renderer carries NO money
math (it tags + formats already-ranked rows — the ranking is rank_queue.py's, the money is the
gate's): mirror the no-business-logic-in-render rule (answer_tacos._serialize at answer_tacos.py:70-74).

Output: `engine/src/habibos/queue.py` and a GREEN `test_queue.py`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-daily-ranked-queue-explicit-approval-spend-up/09-CONTEXT.md
@.planning/phases/09-daily-ranked-queue-explicit-approval-spend-up/09-PATTERNS.md

<interfaces>
The QueueRow the renderer consumes (added in Plan 03, result.py):
QueueRow(sku, entity, action_type, current, proposed, expected_weekly_usd: float|None, provenance, cls="auto")

The data-derived-flag pattern to mirror (result.py:92-94):
@property
def is_spend_increasing(self) -> bool: return self.delta_spend > 0

The no-business-logic-in-render rule (answer_tacos.py:70-74): the renderer dataclasses.asdict's
typed results and formats — it computes NO ratios, NO ranks (those come from rank_queue.py).

Class mapping (CONTEXT D-01/D-02/D-06/D-08):
  auto (standing approval, shown already-actioned — autonomy != silence, 08 D-11):
    pause, negative (exact), bid_down, contested-term pause
  needs-approval (explicit approval — blast radius, not just reversibility):
    bid_up, budget_up, harvest (a spend-up), negative_phrase / negative_broad

Render row schema (D-07): rank | SKU | entity | action | current -> proposed | expected $/wk | provenance | class.
Dollars-first, one screen (CLAUDE.md rank-by-dollars). Provenance cites source + window (hard rule 5).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement queue.py — the class-tagger + dollar-ranked table renderer</name>
  <files>engine/src/habibos/queue.py</files>
  <read_first>
    - engine/src/habibos/result.py:64-94,174-205 (ProposedAction.is_spend_increasing data-derived flag to mirror; QueueRow from Plan 03)
    - engine/scripts/answer_tacos.py:70-74 (the no-business-logic-in-render _serialize rule — render formats typed results, computes nothing)
    - engine/scripts/render_dashboard.py:118-152 (the existing render step — terminal table vs markdown is Claude's discretion per D-06; read for the rendering convention)
    - engine/src/habibos/tacos.py:175 (the dollars-first order already applied by rank_queue — the renderer preserves it)
    - engine/tests/test_queue.py (the RED contract this task turns GREEN — the class-tag assertions + the row schema assertions)
    - .planning/phases/09-daily-ranked-queue-explicit-approval-spend-up/09-PATTERNS.md (the queue.py section, lines 60-77)
  </read_first>
  <action>
    Create `engine/src/habibos/queue.py` as a PURE utility/renderer (no socket, NO money math, NO
    re-ranking). Open with a docstring stating: the class tag is derived from action_type + blast
    radius (D-01/D-02); the renderer formats already-ranked QueueRows and computes nothing (the
    no-business-logic-in-render rule, mirror answer_tacos._serialize). Implement:
      (1) `classify_row(row: QueueRow) -> str` — return the reversibility class string. needs-approval
      when action_type is a spend-UP (bid_up, budget_up, harvest) OR a wider-blast negative
      (negative_phrase, negative_broad). auto for pause, negative (exact), bid_down, contested-term
      pause. Drive this off action_type membership (define two frozensets NEEDS_APPROVAL_ACTIONS and
      AUTO_ACTIONS so the mapping is reviewable + the test can assert each); an UNKNOWN action_type
      defaults to needs-approval (fail safe toward the human gate — never auto-apply an unmodeled
      action). Mirror the is_spend_increasing data-derived-flag style.
      (2) `tag(rows: list[QueueRow]) -> list[QueueRow]` — return new QueueRows with `cls` set from
      classify_row (use dataclasses.replace since QueueRow is frozen); preserve the incoming order
      (already dollar-ranked by rank_queue — do NOT re-sort).
      (3) `render(rows: list[QueueRow]) -> str` — format the tagged rows as ONE dollar-ranked,
      class-tagged table with columns rank | SKU | entity | action | current -> proposed | expected
      $/wk | provenance | class (D-07). A None expected_weekly_usd renders as an em-dash / "—"
      (no-estimate), not 0. Terminal table vs markdown is Claude's discretion (D-06) — keep dollars-
      first, one screen. The renderer reads QueueRow fields only; it computes no ratio, no rank, no
      class (classify_row owns the class, rank_queue owns the order). Add `__all__ = ["classify_row",
      "tag", "render", "NEEDS_APPROVAL_ACTIONS", "AUTO_ACTIONS"]`. No socket, no fetch.
  </action>
  <verify>
    <automated>cd engine &amp;&amp; uv run pytest tests/test_queue.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - queue.py contains `def classify_row(`, `def tag(`, and `def render(`
    - classify_row returns "needs-approval" for bid_up / budget_up / harvest / negative_phrase / negative_broad
    - classify_row returns "auto" for pause / negative / bid_down
    - an unknown action_type returns "needs-approval" (fail-safe toward the human gate)
    - render output contains all D-07 columns (rank, SKU, entity, action, current -> proposed, expected $/wk, provenance, class) and renders a None estimate as "—" not 0
    - queue.py contains NO ratio math, NO re-sort, NO socket
    - `cd engine && uv run pytest tests/test_queue.py` exits 0
  </acceptance_criteria>
  <done>queue.classify_row tags each row by blast radius (spend-up + phrase/broad negative -> needs-approval; reversibles -> auto; unknown -> needs-approval), tag/render preserve the dollar order, the table carries all D-07 columns, and test_queue.py is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ranked rows -> class tag | the reversibility class decides whether a row auto-applies or waits for a human; a mis-tag could auto-apply a spend-up |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Elevation of Privilege | classify_row class tag | mitigate | spend-ups + phrase/broad negatives -> needs-approval (explicit gate); an UNKNOWN action_type defaults to needs-approval (fail-safe), never auto; the mapping is two reviewable frozensets the test asserts |
| T-09-04-02 | Tampering | render | mitigate | no-business-logic-in-render — the renderer computes no money/rank/class; it formats typed QueueRows (answer_tacos._serialize rule), so a render bug cannot fabricate a $ or a class |
| T-09-04-03 | Information Disclosure | render output | mitigate | the table prints provenance (source-id + window) only; no seller UUID is a QueueRow field, so none can render |
| T-09-04-SC | Tampering | npm/pip/cargo installs | accept | no package installs — stdlib + existing result.py only; nothing added to uv/pip |
</threat_model>

<verification>
- `cd engine && uv run pytest tests/test_queue.py -q` exits 0.
- `cd engine && grep -nE "requests|urllib|http\\.client|socket|/100|\\* 100|round\\(" src/habibos/queue.py` returns nothing (no money math, no socket in the renderer).
- `cd engine && uv run pytest -q` stays green.
</verification>

<success_criteria>
- queue.py tags rows auto vs needs-approval by blast radius (unknown -> needs-approval).
- render emits one dollar-ranked table with all D-07 columns; None estimates render as "—".
- The renderer computes no money math; test_queue.py is GREEN; the full suite stays green.
</success_criteria>

<output>
Create `.planning/phases/09-daily-ranked-queue-explicit-approval-spend-up/09-04-SUMMARY.md` when done.
</output>
