---
phase: 09-daily-ranked-queue-explicit-approval-spend-up
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/config/thresholds.toml
  - engine/tests/test_thresholds.py
  - engine/tests/test_cover_gate.py
  - engine/tests/test_rank_queue.py
  - engine/tests/test_queue.py
  - engine/tests/fixtures/queue_candidates.json
autonomous: true
requirements: [QUEUE-01, QUEUE-03, EXEC-05]
user_setup: []

must_haves:
  truths:
    - "The magnitude cap is LIVE at 50% — a reversible move steeper than -50% is refused, not clamped"
    - "The stock-cover floor is LIVE at 42 days (6 weeks) — a sub-cover SKU is refused from any spend-up"
    - "The conversion floor is readable by the engine at 12% — a sub-benchmark SKU is refused from any spend-up"
    - "The Phase-9 test contracts exist and FAIL RED on the missing modules (rank_queue, queue, cover_gate)"
  artifacts:
    - path: "engine/config/thresholds.toml"
      provides: "max_reversible_pct_change=50, days_of_cover_floor=42, conversion_benchmark=12 (all in [marketplace.CA])"
      contains: "max_reversible_pct_change = 50"
    - path: "engine/tests/test_thresholds.py"
      provides: "assertions the three dials read back as floats 50.0/42.0/12.0"
      contains: "conversion_benchmark"
    - path: "engine/tests/test_cover_gate.py"
      provides: "RED contract for the inventory+conversion spend-up gate"
      contains: "def test_"
    - path: "engine/tests/test_rank_queue.py"
      provides: "RED contract for the dollar-ranked queue estimator"
      contains: "def test_"
    - path: "engine/tests/test_queue.py"
      provides: "RED contract for the class-tagging renderer"
      contains: "def test_"
  key_links:
    - from: "engine/src/habibos/thresholds.py"
      to: "engine/config/thresholds.toml [marketplace.CA]"
      via: "thresholds.read(key, marketplace)"
      pattern: "conversion_benchmark|days_of_cover_floor|max_reversible_pct_change"
---

<objective>
Flip the two seeded-permissive config dials to their real Phase-9 values, ADD the conversion
benchmark to the machine-readable threshold mirror, and lay down the RED test contracts that
Waves 2-4 implement against.

Purpose: D-01 (magnitude cap to 50) and D-03 (cover floor to 42 days) are "build-the-mechanism-
then-flip-the-dial" config changes — the machinery already exists and is pytest-covered from
Phases 7-8. D-05's `conversion_benchmark` (12%) currently lives ONLY in `state/targets.md`; the
engine reads thresholds via `thresholds.read`, so it MUST be ADDED to
`thresholds.toml[marketplace.CA]` before the conversion gate (Plan 02) can read it. This is the
sequenced cross-file dependency PATTERNS.md flagged. The RED contracts make every later wave a
GREEN-the-suite exercise (CLAUDE.md hard rule 3: every money/write module ships with its pytest
analog, written test-first).

Output: a live `thresholds.toml`, updated `test_thresholds.py` assertions, three RED test files,
and one candidate fixture the ranker/queue tests assert against.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-daily-ranked-queue-explicit-approval-spend-up/09-CONTEXT.md
@.planning/phases/09-daily-ranked-queue-explicit-approval-spend-up/09-PATTERNS.md

<interfaces>
The threshold reader (UNCHANGED) the new dials plug into. From thresholds.py:19-31:
read(key, marketplace="CA") -> float | None    # "" or absent -> None (refuse), else float

The seeded-permissive dials TODAY (thresholds.toml:44,55) — both empty "":
days_of_cover_floor = ""           # line 44 — seeded empty, refuse-never-default (empty REFUSES)
max_reversible_pct_change = ""     # line 55 — seeded permissive (empty PASSES, see magnitude.py:1-23)

conversion_benchmark currently ONLY in state/targets.md:34 — NOT yet in thresholds.toml.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip the two dials + ADD conversion_benchmark to thresholds.toml</name>
  <files>engine/config/thresholds.toml</files>
  <read_first>
    - engine/config/thresholds.toml (the file being modified — full file, esp. lines 30-55 comment convention + the two empty dials at 44 and 55)
    - engine/src/habibos/magnitude.py:1-23 (the documented empty="permissive" asymmetry for max_reversible_pct_change)
    - engine/src/habibos/gate.py:198-204 (the documented empty="refuse" asymmetry for floors)
    - state/targets.md:21-34 (min_weeks_cover_maintain: 6 = the 42-day source; conversion_benchmark: 12% PROVISIONAL note to mirror)
    - .planning/phases/09-daily-ranked-queue-explicit-approval-spend-up/09-PATTERNS.md (the thresholds.toml section, lines 153-164)
  </read_first>
  <action>
    Edit `engine/config/thresholds.toml` in place inside the `[marketplace.CA]` block. Three changes.
    (1) D-01 — set `max_reversible_pct_change` from `""` to `50` (line 55). Keep its existing
    multi-line comment; append one line noting it is flipped live in Phase 9 so a reversible
    bid/budget-down steeper than -50% per action is refused and routed to explicit review (equal-to-cap
    passes, strict greater-than breach).
    (2) D-03 — set `days_of_cover_floor` from `""` to `42` (line 44). Update its comment to: a SKU is
    refused from any spend-INCREASE below this cover (on-hand + inbound divided by velocity); 42 = 6
    weeks x 7 (D-03, maintain tier from targets.md:23); refuse-never-default — a missing floor REFUSES,
    never assumes (the asymmetry vs the magnitude cap).
    (3) D-05 — ADD a new key `conversion_benchmark = 12` to `[marketplace.CA]`. Comment it: conversion-
    rate floor % below which a SKU is refused from any spend-INCREASE (D-05); PROVISIONAL, mirrors
    state/targets.md:34, retune to portfolio top-quartile after a sales_and_traffic pull; hard refusal
    this phase, not advisory.
    Keep the existing per-key WHY+units+asymmetry comment style (thresholds.toml:30-55). Do NOT touch
    min_net_margin_pct, acos_ceiling_pct, tacos_ceiling_pct, or zero_conversion_spend_trigger.
  </action>
  <verify>
    <automated>cd engine &amp;&amp; uv run python -c "from habibos import thresholds; assert thresholds.read('max_reversible_pct_change','CA')==50.0; assert thresholds.read('days_of_cover_floor','CA')==42.0; assert thresholds.read('conversion_benchmark','CA')==12.0; print('dials live')"</automated>
  </verify>
  <acceptance_criteria>
    - thresholds.toml [marketplace.CA] contains `max_reversible_pct_change = 50` (not "")
    - thresholds.toml [marketplace.CA] contains `days_of_cover_floor = 42` (not "")
    - thresholds.toml [marketplace.CA] contains `conversion_benchmark = 12`
    - `thresholds.read("conversion_benchmark","CA")` returns 12.0 (float), not None
    - min_net_margin_pct, acos_ceiling_pct, tacos_ceiling_pct unchanged (still 15/30/15)
  </acceptance_criteria>
  <done>The three dials read back as 50.0 / 42.0 / 12.0 floats; the conversion benchmark exists in the machine mirror so Plan 02's cover gate can read it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the live-dial assertions to test_thresholds.py</name>
  <files>engine/tests/test_thresholds.py</files>
  <read_first>
    - engine/tests/test_thresholds.py (the file being modified — full file, esp. the days_of_cover_floor "" assertion at line 33 which this task INVERTS)
    - engine/config/thresholds.toml (just edited in Task 1 — the values being asserted)
  </read_first>
  <behavior>
    - test: thresholds.read("max_reversible_pct_change","CA") == 50.0 and is a float
    - test: thresholds.read("days_of_cover_floor","CA") == 42.0 and is a float
    - test: thresholds.read("conversion_benchmark","CA") == 12.0 and is a float
    - the existing test_missing_threshold_returns_none must STOP asserting days_of_cover_floor=="" (it is now 42); the refuse-on-empty path stays covered by the still-absent key and the absent-marketplace assertions
  </behavior>
  <action>
    Edit `engine/tests/test_thresholds.py`. Remove the now-stale `assert thresholds.read("days_of_cover_floor","CA") is None` line (line 33) and its comment — that dial is now live. Add a new test
    `test_phase9_dials_live` asserting the three Phase-9 values read back as the exact floats
    50.0 / 42.0 / 12.0 with `isinstance(..., float)`, docstring citing D-01/D-03/D-05. Keep the
    `a_key_that_does_not_exist` and absent-marketplace `ZZ` assertions (they still prove
    refuse-on-empty/absent). Do not change test_reads_seeded_ceiling or test_default_marketplace_is_ca.
  </action>
  <verify>
    <automated>cd engine &amp;&amp; uv run pytest tests/test_thresholds.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_thresholds.py contains `def test_phase9_dials_live`
    - test_thresholds.py asserts `conversion_benchmark` reads 12.0
    - `cd engine && uv run pytest tests/test_thresholds.py` exits 0
    - test_thresholds.py no longer asserts days_of_cover_floor is None
  </acceptance_criteria>
  <done>test_thresholds.py passes with the three Phase-9 dials asserted live; the refuse-on-empty path remains covered via an absent key.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write the three RED contract test files + the candidate fixture</name>
  <files>engine/tests/test_cover_gate.py, engine/tests/test_rank_queue.py, engine/tests/test_queue.py, engine/tests/fixtures/queue_candidates.json</files>
  <read_first>
    - engine/tests/test_magnitude_cap.py (the two-branch refuse/pass shape + GateRefusal(over_cap, breached_value never clamped) — the cover-gate test analog)
    - engine/tests/test_gate.py:1-60 (the no_threshold_set + strict-equal-passes + GateRefusal edges — the cover-gate analog)
    - engine/tests/test_tacos.py:31-71,108-117,186-192 (fixture-driven real-numbers pattern, dollars-first sort assertion with None last, provenance assertion — the rank_queue/queue analog)
    - engine/src/habibos/result.py:64-133,174-205 (ProposedAction, GateRefusal, GateVerdict, SkuRow — the typed grammar the contracts reference; QueueRow is ADDED here in Plan 03)
    - engine/tests/fixtures/sku_catalog_test.toml (the committed catalog fixture the gate tests reuse)
    - .planning/phases/09-daily-ranked-queue-explicit-approval-spend-up/09-PATTERNS.md (the Test files section, lines 167-189)
  </read_first>
  <action>
    Create three RED test files plus one fixture. All import the not-yet-existing modules so they FAIL
    RED until Waves 2-4 (the intended Nyquist baseline — note it in each docstring). No seller UUID
    literal anywhere; no network.
    (a) `engine/tests/test_cover_gate.py` — import `from habibos import cover_gate`. Mirror
    test_magnitude_cap.py two-branch shape + test_gate.py no_threshold_set edge. Pin these contracts
    against the cover gate's pure predicate (final signature decided in Plan 02; mirror
    `magnitude.check(action, floor)`): (1) a spend-UP on a SKU whose days_of_cover < 42 →
    `GateRefusal(code="cover_below_floor")` whose `breached_value` is the actual cover number, NEVER
    clamped to 42; (2) cover EXACTLY == 42 → PASS (None or GateVerdict — strict `<` breach, equal
    passes, mirror gate.py:226); (3) a spend-UP on a SKU converting below 12% →
    `GateRefusal(code="conversion_below_floor")` surfacing the actual conversion %; (4) a MISSING floor
    (thresholds.read → None) → `GateRefusal(code="no_threshold_set")`, never a default (refuse-never-
    default, D-03); (5) a spend-DOWN (delta_spend <= 0) → PASS without reading any floor (the cover gate
    only guards spend-UP, mirror gate.evaluate spend-direction-first). Add the named edge case: GG-0DC1
    at 1 FBA unit (cover far below 42) → cover_below_floor refusal.
    (b) `engine/tests/test_rank_queue.py` — import `from habibos import rank_queue` and
    `from habibos.result import QueueRow` (added in Plan 03). Mirror test_tacos.py: point at the new
    `queue_candidates.json` fixture; assert rows sort by expected weekly $ impact DESCENDING with
    None-estimate rows LAST (the D-07 sort key `(x.expected_weekly_usd is None, -(x.expected_weekly_usd
    or 0))`); assert a candidate with an undefined/zero-denominator estimate gets `expected_weekly_usd
    is None` (NOT 0, NOT a guess — the `_ratio` None-on-undefined idiom); assert every row carries a
    non-empty `provenance` string citing source + window.
    (c) `engine/tests/test_queue.py` — import `from habibos import queue`. Assert the class-tagging
    (D-01/D-02): a spend-UP row (bid_up / budget_up / harvest) tags `needs-approval`; a phrase/broad
    negative tags `needs-approval`; a pause / exact-match negative / bid_down tags `auto`. Assert the
    render row schema carries the keys rank, sku, entity, action, current→proposed, expected_weekly_usd,
    provenance, class.
    (d) `engine/tests/fixtures/queue_candidates.json` — a small hand-authored candidate set (3-5 rows)
    carrying the fields the estimator needs (sku, action_type, current and proposed bid-or-budget, the
    components behind the $ estimate, a provenance source+window string), including ONE row whose
    estimate is undefined (zero/missing denominator) so the None-sorts-last edge is exercised. Sanitized
    component sums only.
  </action>
  <verify>
    <automated>cd engine &amp;&amp; uv run pytest tests/test_cover_gate.py tests/test_rank_queue.py tests/test_queue.py -q; test $? -ne 0 &amp;&amp; echo "RED as intended (missing modules)"</automated>
  </verify>
  <acceptance_criteria>
    - test_cover_gate.py imports `from habibos import cover_gate` and asserts a cover_below_floor refusal with breached_value never clamped
    - test_rank_queue.py asserts None-estimate rows sort LAST and an undefined estimate is None not 0
    - test_queue.py asserts spend-up + phrase/broad-negative tag `needs-approval`, reversibles tag `auto`
    - queue_candidates.json exists, valid JSON, contains one undefined-estimate row, no seller UUID
    - the three test files collect and FAIL RED (modules absent) — `pytest` returns non-zero; this is the intended Wave-1 baseline, NOT a defect
  </acceptance_criteria>
  <done>The cover-gate, ranker, and queue contracts exist as RED tests with a committed candidate fixture; Waves 2-4 turn them GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator config -> engine | thresholds.toml values become live gate dials; a wrong value loosens a guardrail |
| test fixture -> engine | queue_candidates.json feeds the ranker; a fabricated $ component could mask a bug |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering | days_of_cover_floor / conversion_benchmark dials | mitigate | refuse-never-default — an empty floor REFUSES (gate.py:198-204 grammar reused by cover_gate); test_thresholds asserts the live values so a silent revert to "" is caught |
| T-09-01-02 | Tampering | max_reversible_pct_change | mitigate | strict `> cap` breach never clamps (magnitude.py:65); flipping to 50 is asserted by test_thresholds; equal-to-cap passes is the documented semantics |
| T-09-01-03 | Repudiation | queue_candidates.json fixture | mitigate | the ranker test rebuilds expected $ from the fixture's own components (the test_tacos `_pct`-mirror pattern) so a fabricated estimate cannot pass review |
| T-09-01-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan — config + tests only; nothing added to uv/pip |
</threat_model>

<verification>
- `cd engine && uv run pytest tests/test_thresholds.py -q` exits 0 (the dials are live).
- `cd engine && uv run python -c "from habibos import thresholds; print(thresholds.read('conversion_benchmark','CA'))"` prints 12.0.
- The three new test files import their target modules and fail RED (modules absent) — the intended Wave-1 baseline.
- No existing test regresses: `cd engine && uv run pytest tests/test_magnitude_cap.py tests/test_gate.py tests/test_tacos.py -q` stays green (no source files changed yet).
</verification>

<success_criteria>
- thresholds.toml carries the three live Phase-9 dials (50 / 42 / 12) in [marketplace.CA].
- test_thresholds.py asserts all three live and exits 0.
- test_cover_gate.py, test_rank_queue.py, test_queue.py exist as RED contracts importing cover_gate / rank_queue / queue.
- queue_candidates.json committed with a None-estimate edge row and no secret.
</success_criteria>

<output>
Create `.planning/phases/09-daily-ranked-queue-explicit-approval-spend-up/09-01-SUMMARY.md` when done.
</output>
