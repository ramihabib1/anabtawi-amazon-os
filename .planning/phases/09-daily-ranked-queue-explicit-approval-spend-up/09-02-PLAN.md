---
phase: 09-daily-ranked-queue-explicit-approval-spend-up
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - engine/src/habibos/cover_gate.py
autonomous: true
requirements: [EXEC-05]
user_setup: []

must_haves:
  truths:
    - "A spend-up on a SKU below 6-week stock cover is refused, surfacing the actual cover number (D-03: stock-cover floor 42 days / 6 weeks)"
    - "A spend-up on a SKU below the 12% conversion benchmark is refused, surfacing the actual conversion % (D-05: conversion floor 12%, hard refusal)"
    - "A spend-down is never blocked by the cover gate (it only guards spend-up)"
    - "A missing cover/conversion floor refuses (refuse-never-default), never assumes a value (D-04: the cover/conversion numbers are read live by the agent via MCP and passed into the pure engine gate — engine opens no socket)"
    - "An over-floor breach is never clamped — the breaching value is surfaced verbatim"
  artifacts:
    - path: "engine/src/habibos/cover_gate.py"
      provides: "pure inventory+conversion spend-up gate: (action, cover, conversion, floors) -> GateVerdict | GateRefusal"
      contains: "def check"
      min_lines: 60
  key_links:
    - from: "engine/src/habibos/cover_gate.py"
      to: "engine/src/habibos/result.py"
      via: "from .result import GateRefusal, GateVerdict, ProposedAction"
      pattern: "GateRefusal"
    - from: "engine/tests/test_cover_gate.py"
      to: "engine/src/habibos/cover_gate.py"
      via: "from habibos import cover_gate"
      pattern: "cover_gate.check"
---

<objective>
Build the hand-written, pytest-covered inventory + conversion spend-up gate — the EXEC-05
guardrail that refuses any budget/bid raise onto a SKU below the 6-week stock-cover floor or
below the 12% conversion benchmark, and turns Plan 01's `test_cover_gate.py` GREEN.

Purpose: EXEC-05 — "never scale ad spend onto a SKU below stock-cover or conversion benchmark
thresholds." This is the gate that refuses GG-0DC1 (1 FBA unit) from any scale-up (D-03), and a
sub-12%-converting SKU from a raise (D-05). It is a NEW pure predicate module that REUSES the
existing typed `GateRefusal` / `GateVerdict` grammar (NO new dataclass — CONTEXT: "reuse it").
The engine opens NO socket: the agent reads live cover/conversion via the DataDoe/AgentCentral
MCP (D-04) and passes the numbers in; this module only decides (CLAUDE.md hard rule 1).

Output: `engine/src/habibos/cover_gate.py` and a GREEN `test_cover_gate.py`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-daily-ranked-queue-explicit-approval-spend-up/09-CONTEXT.md
@.planning/phases/09-daily-ranked-queue-explicit-approval-spend-up/09-PATTERNS.md

<interfaces>
The typed grammar to REUSE (from result.py:64-133) — do NOT add a new refusal type:
ProposedAction(sku, action_type, delta_spend, marketplace="CA", entity_type=None, entity_id=None, params={})
  .is_spend_increasing -> delta_spend > 0
GateRefusal(reason, code, breached_value=None, ceiling=None, source=None)
GateVerdict(projected_tacos, ceiling, contribution_margin, auto_pass=False)

The SHAPE analog — magnitude.check's pure predicate (magnitude.py:44-72):
check(action, max_reversible_pct_change=None) -> None | GateRefusal
  - cap None -> permissive pass (the OPPOSITE of what cover_gate does — see below)
  - strict `> cap` breach -> GateRefusal(over_cap, breached_value=pct, ceiling=cap), never clamps

The branch-order analog — gate.evaluate (gate.py:116-238):
  1. spend-direction FIRST (spend-down auto-passes with NO read, D-06)
  2..7. refuse on each missing/over-limit input, never default, never clamp
  Floor read: floor = thresholds.read(key, mkt); None -> GateRefusal("no threshold set", code="no_threshold_set")

The threshold reader (thresholds.py:19-31): read(key, marketplace="CA") -> float | None.
The live floors (now set by Plan 01): days_of_cover_floor=42, conversion_benchmark=12.

CRITICAL asymmetry: cover_gate follows gate.evaluate's refuse-on-None floor grammar (gate.py:198-204),
NOT magnitude's permissive-on-None seed. A missing floor REFUSES (D-03 refuse-never-default).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement cover_gate.py — the inventory + conversion spend-up gate</name>
  <files>engine/src/habibos/cover_gate.py</files>
  <read_first>
    - engine/src/habibos/magnitude.py (full — the pure-predicate SHAPE: docstring banner, `(action, floor) -> None | GateRefusal`, strict-breach-never-clamp at lines 65-71)
    - engine/src/habibos/gate.py:1-53,116-238 (the multi-branch refuse-never-default/never-clamp `evaluate` order; spend-direction-first at 149-156; floor-read-None-refuses at 198-204; strict breach at 226-233)
    - engine/src/habibos/result.py:64-133 (ProposedAction, GateRefusal, GateVerdict — REUSE, no new type)
    - engine/src/habibos/thresholds.py:19-31 (read(key, marketplace) refuse-on-empty contract)
    - engine/tests/test_cover_gate.py (the RED contract this task turns GREEN — written in Plan 01)
    - .planning/phases/09-daily-ranked-queue-explicit-approval-spend-up/09-PATTERNS.md (the cover_gate.py section, lines 80-110)
  </read_first>
  <action>
    Create `engine/src/habibos/cover_gate.py` as a PURE predicate module (no socket, no I/O beyond
    the threshold read), the structural twin of `magnitude.py` with `gate.evaluate`'s branch order.
    Open with the hard-rule-3 module-docstring banner (mirror magnitude.py:1-23) stating: this gate
    refuses a spend-UP onto a SKU below the stock-cover floor (D-03) or the conversion benchmark
    (D-05); it follows the margin-gate's refuse-on-None floor grammar (the asymmetry vs magnitude's
    permissive seed); the live cover/conversion numbers are read by the AGENT via MCP (D-04) and
    passed in — the engine opens no socket. Copy the numbered "Branch order (refuse, never clamp,
    never default)" docstring block from gate.py:124-146 adapted to this gate.
    Implement `check(action, days_of_cover, conversion_pct, marketplace=None) -> GateVerdict | GateRefusal`
    where `days_of_cover` and `conversion_pct` are the caller-supplied live MCP numbers (either may be
    None when the live read failed). Branch order:
      1. spend-DECREASING (not action.is_spend_increasing) -> GateVerdict(auto_pass=True), NO floor
         read (a spend-down cannot over-scale stock/conversion — mirror gate.py:149-156).
      2. cover floor = thresholds.read("days_of_cover_floor", mkt); None -> GateRefusal(reason="no
         threshold set — days_of_cover_floor unset", code="no_threshold_set") — refuse-never-default
         (D-03; mirror gate.py:198-204).
      3. days_of_cover is None (live read failed/no data) -> GateRefusal(reason="no data — days_of_cover
         unavailable", code="no_cover_data") — never assume cover (anti-fabrication, hard rule 4).
      4. STRICT days_of_cover < cover_floor -> GateRefusal(reason="stock cover below the 6-week floor",
         code="cover_below_floor", breached_value=days_of_cover, ceiling=cover_floor) — equal-to-floor
         PASSES (strict `<`, mirror gate.py:226 strict `>`); the breaching cover number is surfaced
         verbatim, NEVER clamped to 42.
      5. conversion floor = thresholds.read("conversion_benchmark", mkt); None -> GateRefusal(code=
         "no_threshold_set", reason naming conversion_benchmark) — refuse-never-default (D-05).
      6. conversion_pct is None -> GateRefusal(code="no_conversion_data") — never assume conversion.
      7. STRICT conversion_pct < conversion_floor -> GateRefusal(code="conversion_below_floor",
         breached_value=conversion_pct, ceiling=conversion_floor) — equal passes, never clamped.
      8. both pass -> GateVerdict(projected_tacos=None, ceiling=None, contribution_margin=None,
         auto_pass=False) (a typed positive PASS — never a bare True/None; this gate computes no TACOS
         so the TACOS fields are None, the margin gate owns that math). Use `mkt = marketplace if
         marketplace is not None else action.marketplace` (mirror gate.py:147). Add `__all__ = ["check"]`.
    Do NOT add a new dataclass to result.py — reuse GateRefusal/GateVerdict. Do NOT open a socket,
    fetch, or read inventory files — cover/conversion arrive as arguments (D-04).
  </action>
  <verify>
    <automated>cd engine &amp;&amp; uv run pytest tests/test_cover_gate.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - cover_gate.py contains `def check(` with the documented branch-order docstring
    - cover_gate.py imports `from .result import GateRefusal, GateVerdict, ProposedAction` and adds NO new dataclass to result.py
    - cover_gate.py contains NO `import requests`, NO `http`, NO socket / fetch (engine opens no socket)
    - a spend-up at days_of_cover=1 (GG-0DC1) returns GateRefusal(code="cover_below_floor") with breached_value==1 (never 42)
    - a spend-up at days_of_cover exactly 42 PASSES (strict `<` breach)
    - a missing floor (thresholds.read None, exercised via an absent marketplace) returns GateRefusal(code="no_threshold_set")
    - a spend-down returns GateVerdict(auto_pass=True) with no floor read
    - `cd engine && uv run pytest tests/test_cover_gate.py` exits 0
  </acceptance_criteria>
  <done>cover_gate.check refuses sub-cover and sub-conversion spend-ups (surfacing the breaching value, never clamped), auto-passes spend-downs, refuses on a missing floor or missing live data, and test_cover_gate.py is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent MCP read -> cover_gate | live days_of_cover / conversion_pct cross from the agent's MCP call into the pure gate; a stale or missing number must not silently pass |
| operator config -> cover_gate | the cover/conversion floors come from thresholds.toml; a missing floor must refuse, not default |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Elevation of Privilege | cover_gate.check spend-up path | mitigate | a restock-gated / sub-conversion SKU spend-up is hard-refused (cover_below_floor / conversion_below_floor); equal-to-floor passes, strict-breach refuses, surfaced verbatim — never clamped |
| T-09-02-02 | Tampering | missing cover/conversion floor | mitigate | refuse-never-default — `thresholds.read` None -> GateRefusal(no_threshold_set) (gate.py:198-204 grammar), never a default 42/12 |
| T-09-02-03 | Spoofing | stale/missing live cover number | mitigate | days_of_cover/conversion_pct None -> GateRefusal(no_cover_data / no_conversion_data); the gate never assumes cover/conversion (hard rule 4) |
| T-09-02-04 | Information Disclosure | the pure gate | accept | cover_gate reads no secret and opens no socket; the seller UUID never enters this module (verified: no MCP I/O here) |
| T-09-02-SC | Tampering | npm/pip/cargo installs | accept | no package installs — reuses existing result.py + thresholds.py; nothing added to uv/pip |
</threat_model>

<verification>
- `cd engine && uv run pytest tests/test_cover_gate.py -q` exits 0.
- `cd engine && grep -nE "requests|urllib|http\\.client|socket" src/habibos/cover_gate.py` returns nothing (engine opens no socket).
- `cd engine && uv run pytest -q` stays green overall (no regression; result.py untouched, so test_gate/test_magnitude unaffected).
</verification>

<success_criteria>
- cover_gate.py exists as a pure predicate refusing sub-cover and sub-conversion spend-ups.
- The breaching cover/conversion value is surfaced verbatim, never clamped.
- A spend-down auto-passes; a missing floor or missing live number refuses.
- test_cover_gate.py is GREEN; the full suite stays green.
</success_criteria>

<output>
Create `.planning/phases/09-daily-ranked-queue-explicit-approval-spend-up/09-02-SUMMARY.md` when done.
</output>
