---
phase: 09-daily-ranked-queue-explicit-approval-spend-up
plan: 05
type: execute
wave: 4
depends_on: ["09-02", "09-03", "09-04"]
files_modified:
  - engine/src/habibos/harvest.py
  - engine/tests/test_harvest.py
  - engine/scripts/queue_run.py
  - engine/tests/test_queue_run.py
  - .claude/skills/ppc-analysis/SKILL.md
autonomous: false
requirements: [WRITE-04, QUEUE-02, QUEUE-04, EXEC-02]
user_setup:
  - service: datadoe-agentcentral
    why: "The daily queue reads live velocity / days-of-cover / FBA inventory / conversion + applies gate-passed spend-up writes via actions_start; the spend-up Ads action types must be org-enabled."
    dashboard_config:
      - task: "Enable the spend-up Ads action types (TARGETS_UPDATE bid-up, CAMPAIGNS_UPDATE budget-up, TARGETS_ADD) in DataDoe Settings -> Actions for the CA profile"
        location: "DataDoe Settings -> Actions"

must_haves:
  truths:
    - "A spend-up executes only after passing the margin gate AND the cover/conversion gate AND receiving explicit batch approval (D-06: batch-approve = one deliberate explicit confirm; D-04: the agent does the MCP I/O, the engine/CLI seam opens no socket)"
    - "An over-ceiling raise is refused, not clamped (the margin gate's GateRefusal surfaces the breaching value) (D-11: funded winner raised up to its gate ceiling; over-ceiling refused-not-clamped)"
    - "An efficiency verdict never acts on an immature window — judge-later waits for >=7d SP / >=14d SB/SD (D-10: two-class attribution — act-now vs judge-later matured window; D-09: harvest graduation needs >=2 orders AND ACOS <= ceiling over a matured window)"
    - "A term live in >1 ENABLED campaign is flagged and blocked from a bid-up until consolidated to the lowest-ACOS owner (D-08: lowest-ACOS owns the contested term; bid-up blocked until consolidation completes)"
    - "The standing-approved reversibles auto-apply and appear on the queue already-actioned (autonomy != silence)"
  artifacts:
    - path: "engine/src/habibos/harvest.py"
      provides: "pure two-class attribution (act-now vs judge-later) + internal-keyword-competition + harvest-graduation predicates"
      contains: "def is_matured"
      min_lines: 70
    - path: "engine/scripts/queue_run.py"
      provides: "thin CLI: live MCP fixtures -> rank -> margin+cover gate -> render -> batch-approval -> apply spine"
      contains: "def main"
      min_lines: 80
    - path: ".claude/skills/ppc-analysis/SKILL.md"
      provides: "daily-queue operating-rhythm section pointing at queue_run.py + the live-read tools"
      contains: "queue_run"
  key_links:
    - from: "engine/scripts/queue_run.py"
      to: "engine/src/habibos/apply.py"
      via: "apply.apply(action, owned_source=..., start_dryrun=..., start_apply=..., get_status=..., find_echo=...)"
      pattern: "apply\\.apply"
    - from: "engine/scripts/queue_run.py"
      to: "engine/src/habibos/gate.py + cover_gate.py"
      via: "gate.evaluate + cover_gate.check before any apply"
      pattern: "cover_gate\\.check|gate\\.evaluate"
    - from: "engine/scripts/queue_run.py"
      to: "engine/src/habibos/rank_queue.py + queue.py"
      via: "rank_queue.rank -> queue.tag -> queue.render"
      pattern: "rank_queue\\.rank|queue\\.render"
---

<objective>
Wire the daily ranked queue end-to-end and add the two-class attribution + internal-keyword-
competition + harvest-graduation logic — the standing operating rhythm that surfaces every
proposed PPC change dollar-ranked, auto-applies standing-approved reversibles, and funds gate-
passed winners up to their ceiling under one explicit batch approval.

Purpose: closes the four remaining Phase-9 requirements. WRITE-04 — a spend-up executes only
after the margin gate AND explicit approval (an over-ceiling raise refused, not clamped, D-11).
QUEUE-02 — reversibles carry standing approval (auto-apply after dryRun), spend-ups + new
campaigns wait for explicit approval. QUEUE-04 — the two-class attribution model (act-now
reversible stop-loss vs judge-later matured-window efficiency; "3 clean days" = 3 days of
MATURED data, D-10). EXEC-02 — fund winners to their gate ceiling AND consolidate internal
keyword competition (a term live in >1 ENABLED campaign is flagged and blocked from a bid-up
until consolidated to the lowest-matured-ACOS owner, D-08). The spend-up reuses the Phase-8
apply spine UNCHANGED (denylist -> margin gate -> magnitude -> ledger -> dryRun -> apply ->
poll -> FIND-echo -> log). The CLI is THE SEAM: Python opens no socket; the agent's live
velocity/cover/conversion reads (D-04) + the spine's dryRun/apply/poll/FIND arrive as LOCAL
files threaded in as zero-arg callables (CLAUDE.md hard rule 1).

Output: `harvest.py` (pure, pytest-covered) + `queue_run.py` (thin CLI) + tests + the SKILL
operating-rhythm section.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-daily-ranked-queue-explicit-approval-spend-up/09-CONTEXT.md
@.planning/phases/09-daily-ranked-queue-explicit-approval-spend-up/09-PATTERNS.md
@.planning/phases/09-daily-ranked-queue-explicit-approval-spend-up/09-02-SUMMARY.md
@.planning/phases/09-daily-ranked-queue-explicit-approval-spend-up/09-03-SUMMARY.md
@.planning/phases/09-daily-ranked-queue-explicit-approval-spend-up/09-04-SUMMARY.md
@brain/raw/2026-06-16_ppc-coverage-gap.md

<interfaces>
The apply spine to REUSE UNCHANGED (apply.py:147-159) — the spend-up calls this verbatim:
apply.apply(action, *, owned_source, start_dryrun, start_apply, get_status, find_echo,
            confirm_canary=False, catalog_path=None, gate_frame=None, ad_product="SPONSORED_PRODUCTS")
            -> AppliedResult | Refusal | GateRefusal
  Spend-up: build ProposedAction(delta_spend>0, entity_type, entity_id, params) + pass gate_frame
  + catalog_path so gate.evaluate projects; over-ceiling -> GateRefusal(over_ceiling) NOT clamped.

The gates to call BEFORE any apply:
  gate.evaluate(frame, action, catalog_path, marketplace) -> GateVerdict | GateRefusal   (Phase 7)
  cover_gate.check(action, days_of_cover, conversion_pct, marketplace) -> GateVerdict | GateRefusal  (Plan 02)

The ranker + renderer:
  rank_queue.rank(candidates, source, window_from, window_to) -> list[QueueRow]   (Plan 03)
  queue.tag(rows) -> list[QueueRow]; queue.render(rows) -> str; queue.classify_row(row) -> str  (Plan 04)

The thin-CLI shape to MIRROR (apply_action.py — full file):
  - THE SEAM header verbatim (apply_action.py:13-16): Python opens NO socket; agent MCP responses
    are LOCAL files threaded as zero-arg callables; a parallel HTTP client is FORBIDDEN.
  - _load(path) (apply_action.py:48-50): read a local JSON fixture the agent downloaded.
  - REPO_ROOT / DEFAULT_OWNED / DEFAULT_CATALOG (apply_action.py:42-45).
  - logged_call + refusal-can-never-pass (apply_action.py:142-159): end_fields["status"] derived
    from result TYPE; NEVER pass the seller UUID into log fields (hard rule 5).

Live-read MCP tools the AGENT calls (D-04 — engine never calls them):
  get_sales_velocity, get_days_of_cover, get_fba_inventory, get_sales_and_traffic.

Matured-window thresholds (D-09/D-10): >=7d for SP, >=14d for SB/SD. Obviously-dead act-now signals
(do NOT require maturity): inactive listing / zero lifetime orders / ENABLED on a discontinued SKU.
Harvest graduation (D-09): >=2 attributed orders AND ACOS <= the SKU's gate ceiling, over a matured window.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement harvest.py — two-class attribution + internal-competition + graduation predicates</name>
  <files>engine/src/habibos/harvest.py, engine/tests/test_harvest.py</files>
  <read_first>
    - engine/src/habibos/result.py:64-133 (ProposedAction, GateRefusal, GateVerdict — the typed grammar these predicates reuse)
    - engine/src/habibos/gate.py:116-156 (the spend-direction-first branch + refuse-never-default style to mirror)
    - engine/src/habibos/magnitude.py (the pure-predicate `(args) -> None | GateRefusal` SHAPE)
    - engine/tests/test_magnitude_cap.py (the two-branch refuse/pass test analog for the new predicates)
    - brain/raw/2026-06-16_ppc-coverage-gap.md (the real internal-competition + funded-winner + harvest intel these predicates operationalize)
    - .planning/phases/09-daily-ranked-queue-explicit-approval-spend-up/09-PATTERNS.md (No-Analog table lines 244-246: two-class + consolidation logic)
  </read_first>
  <action>
    Create `engine/src/habibos/harvest.py` as a PURE predicate module (no socket, no I/O). Open with
    the hard-rule-3 banner. Implement four reviewable predicates the CLI drives:
      (1) `is_matured(ad_product: str, days_of_data: int) -> bool` — True when days_of_data >= the
      matured threshold for the ad product (>=7 for SPONSORED_PRODUCTS, >=14 for SPONSORED_BRANDS /
      SPONSORED_DISPLAY). Define the thresholds as a reviewable mapping (D-09/D-10). An unknown ad
      product is NOT matured (fail-safe — never act on an unrecognized window).
      (2) `is_act_now(signal: dict) -> bool` — True ONLY on an obviously-dead signal that does NOT
      depend on a matured window: `inactive_listing` True, OR `lifetime_orders == 0`, OR
      `enabled_on_discontinued_sku` True (D-10). Everything else is judge-later.
      (3) `judge_later_blocked(action: ProposedAction, ad_product: str, days_of_data: int) -> None |
      GateRefusal` — for an efficiency verdict (a spend-up / bid change that is NOT act-now), refuse
      with `GateRefusal(code="immature_window", breached_value=days_of_data, ceiling=<threshold>)`
      when not is_matured; otherwise None (the #1 pitfall: never act on an immature window, D-10).
      (4) `consolidation_blocked(action: ProposedAction, term_campaign_acos: dict[str, float]) ->
      None | GateRefusal` — `term_campaign_acos` maps each ENABLED campaignId running this term to its
      matured-window ACOS. When the term is live in >1 ENABLED campaign AND the action is a bid-UP,
      refuse with `GateRefusal(code="internal_competition", reason naming the lowest-ACOS owning
      campaign + the others to pause first)` — the bid-up is BLOCKED until consolidation completes
      (D-08, no bidding against yourself). The owning campaign = the lowest matured-window ACOS; the
      others are a reversible pause (standing approval). When the term is live in exactly one campaign,
      return None (nothing to consolidate). Also add `graduates(orders: int, acos: float | None,
      ceiling: float | None) -> bool` — a search term graduates to a funded keyword only when orders
      >= 2 AND acos is not None AND ceiling is not None AND acos <= ceiling (D-09: proven conversion
      AND efficiency before any new dollar). Reuse GateRefusal — add NO new dataclass. Add `__all__`.
    Also create `engine/tests/test_harvest.py` (analog test_magnitude_cap.py two-branch shape): assert
    is_matured 6d SP -> False, 7d SP -> True, 13d SB -> False, 14d SB -> True; is_act_now on each dead
    signal -> True and on a healthy signal -> False; judge_later_blocked at 5d SP -> GateRefusal(
    immature_window) with breached_value 5, at 7d SP -> None; consolidation_blocked on a bid-up with a
    term in 2 campaigns -> GateRefusal(internal_competition), in 1 campaign -> None; graduates(2,
    acos<=ceiling) -> True, graduates(1,...) -> False, graduates(2, acos>ceiling) -> False.
  </action>
  <verify>
    <automated>cd engine &amp;&amp; uv run pytest tests/test_harvest.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - harvest.py contains `def is_matured(`, `def is_act_now(`, `def judge_later_blocked(`, `def consolidation_blocked(`, `def graduates(`
    - judge_later_blocked at 5d SP returns GateRefusal(code="immature_window", breached_value=5)
    - consolidation_blocked on a bid-up with the term in 2 ENABLED campaigns returns GateRefusal(code="internal_competition")
    - graduates(2, acos, ceiling) is True only when acos <= ceiling; graduates(1, ...) is False
    - harvest.py imports `from .result import GateRefusal, ProposedAction` and adds NO new dataclass
    - harvest.py contains NO socket / fetch (engine opens no socket)
    - `cd engine && uv run pytest tests/test_harvest.py` exits 0
  </acceptance_criteria>
  <done>harvest.py provides the matured-window, act-now-vs-judge-later, internal-competition, and graduation predicates as pure typed refusals; test_harvest.py is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Build queue_run.py — the daily-queue orchestrator CLI (rank -> gate -> render -> batch-approval -> apply)</name>
  <files>engine/scripts/queue_run.py, engine/tests/test_queue_run.py</files>
  <read_first>
    - engine/scripts/apply_action.py (the THIN-CLI shape to mirror EXACTLY — the seam header, _load, REPO_ROOT/DEFAULT_OWNED/DEFAULT_CATALOG, logged_call + refusal-can-never-pass at 142-159)
    - engine/scripts/gate_action.py (the second thin-CLI analog — argv -> evaluate -> JSON)
    - engine/src/habibos/apply.py:147-303 (the spine the spend-up calls UNCHANGED — its branch order + the zero-arg callable seam)
    - engine/src/habibos/rank_queue.py + queue.py + cover_gate.py + harvest.py (the modules this CLI wires — from Plans 02/03/04 + Task 1)
    - engine/src/habibos/gate.py:116-238 (gate.evaluate the spend-up routes through; over_ceiling refuse-not-clamp)
    - brain/raw/2026-06-16_ppc-coverage-gap.md (the funded-winner candidates + GG-0DC1 restock-gated SKU the live run will surface)
    - .planning/phases/09-daily-ranked-queue-explicit-approval-spend-up/09-PATTERNS.md (the queue_*.py CLI section, lines 113-149: the seam, _load, spend-up integration)
  </read_first>
  <action>
    Create `engine/scripts/queue_run.py` as a THIN wiring CLI with NO business logic (mirror
    apply_action.py / gate_action.py — the money/ranking/gating all live in the pytest-covered
    modules). COPY VERBATIM the seam header (apply_action.py:13-16), `_load` (48-50), and
    REPO_ROOT/DEFAULT_OWNED/DEFAULT_CATALOG (42-45). It orchestrates two modes via argparse:
      MODE render (default): read the agent-downloaded local fixtures — the candidate set (sku /
      entity / action_type / current+proposed / $ components), the per-SKU live cover+conversion map
      (from get_days_of_cover / get_sales_and_traffic, D-04), the per-term campaign-ACOS map, the gate
      frame + catalog — then: rank_queue.rank(candidates, source, window_from, window_to) ->
      queue.tag(rows) -> queue.render(rows) printed to stdout (the dollar-ranked, class-tagged daily
      table, D-07). For each spend-up candidate, BEFORE tagging it actionable, run the read-only gate
      chain and annotate any refusal on the row (margin gate.evaluate, cover_gate.check on the SKU's
      live cover/conversion, harvest.judge_later_blocked on the window maturity, harvest.
      consolidation_blocked on the term map) so a refused raise renders with its GateRefusal reason and
      is NOT auto-actionable. For each AUTO-class row (a standing-approved reversible — pause / exact
      negative / bid_down / contested-term pause), render mode MUST actually auto-apply it (QUEUE-02 —
      "standing-approved reversibles auto-apply after dryRun"): call `apply.apply(action, ...)` UNCHANGED
      with the same Phase-8 spine signature as the apply mode (the reversible is delta_spend<=0 so the
      margin gate auto-passes; the magnitude cap — now LIVE at 50 — refuses an over-cap reversible). Then
      stamp the row's actioned status from the returned AppliedResult/Refusal and render it already-
      actioned. The auto-apply is the real spine call, NOT a display-only label (autonomy != silence) —
      a needs-approval row is NEVER auto-applied in render mode (it waits for apply mode + `--approve`).
      MODE apply: given an explicitly-approved spend-up line (the batch-approval gate, D-06 — the CLI
      requires an explicit `--approve` flag; absent -> it refuses to apply any needs-approval row and
      says so), construct a ProposedAction(delta_spend>0, entity_type, entity_id, params) and call
      `apply.apply(action, owned_source=args.owned, catalog_path=args.catalog, gate_frame=<loaded
      frame>, start_dryrun=lambda:_load(args.dryrun_resp), start_apply=lambda:_load(args.apply_resp),
      get_status=lambda:_load(args.status_resp), find_echo=lambda:_load(args.find_echo))` UNCHANGED —
      the full Phase-8 spine runs as-is (the margin gate inside refuses an over-ceiling raise, NOT
      clamps, D-11). Wrap the apply in `logged_call("queue_run", marketplace=...)`; derive
      `end_fields["status"]` from the result TYPE so a refusal can NEVER serialize as applied (mirror
      apply_action.py:157-159). NEVER pass the seller UUID into any log field, the table, or stdout
      (hard rule 5). `json.dump(dataclasses.asdict(result), sys.stdout)` for the apply result.
    Also create `engine/tests/test_queue_run.py` — drive `main([...])` with committed local fixtures
    (reuse the existing tests/fixtures/actions_* + the new queue_candidates.json): assert the render
    mode prints a dollar-ranked table with a needs-approval class on a spend-up and a refusal reason on
    a sub-cover SKU (GG-0DC1); MONKEYPATCH/SPY `apply.apply` and assert that in RENDER mode an AUTO-class
    reversible row INVOKES `apply.apply` (the real spine call, not just a label) and the row renders with
    its actioned status, while a needs-approval (spend-up) row in render mode WITHOUT `--approve` does
    NOT invoke `apply.apply`; assert the apply mode WITHOUT `--approve` refuses a needs-approval row
    (no apply.apply called) and WITH `--approve` on a passing spend-up returns an applied result; assert
    the seller UUID never appears in stdout. Use the test monkeypatch pattern from test_apply.py for the
    data/decisions/brain seams (the spy on apply.apply lets the test verify the call without a live write). (This task is `autonomous: false` at the plan level because the LIVE run
    needs the org-enable of the spend-up Ads action types + Rami's explicit batch approval — see the
    human-verify checkpoint in Task 3; the CODE + tests here are fully autonomous.)
  </action>
  <verify>
    <automated>cd engine &amp;&amp; uv run pytest tests/test_queue_run.py -x -q &amp;&amp; uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - queue_run.py contains `def main(` and the COPIED seam header forbidding a parallel HTTP client
    - queue_run.py render mode calls rank_queue.rank -> queue.tag -> queue.render and prints a dollar-ranked table
    - queue_run.py render mode CALLS `apply.apply(...)` for each auto-class (standing-approved reversible) row after dryRun — not merely displays it as already-actioned — and the row then renders with its actioned status
    - queue_run.py render mode does NOT call `apply.apply(...)` for a needs-approval (spend-up) row (it waits for apply mode + `--approve`)
    - queue_run.py runs gate.evaluate + cover_gate.check + harvest.judge_later_blocked + harvest.consolidation_blocked before marking a spend-up actionable
    - queue_run.py apply mode without `--approve` refuses any needs-approval row (apply.apply not called); with `--approve` it calls apply.apply UNCHANGED
    - end_fields["status"] is derived from the result type (a refusal can never serialize as applied)
    - queue_run.py contains NO `import requests` / `http` / socket / fetch (the seam)
    - `cd engine && grep -rn "AMAZON_CA_SELLER_ID\|seller_id\|sellerId" scripts/queue_run.py` returns nothing
    - test_queue_run.py asserts (via an `apply.apply` spy/monkeypatch) that render mode INVOKES `apply.apply` for an auto-class row and does NOT for a needs-approval row, plus the no-approve refusal path, the GG-0DC1 sub-cover refusal on the table, and no seller UUID in stdout
    - `cd engine && uv run pytest -q` exits 0 (full suite green)
  </acceptance_criteria>
  <done>queue_run.py renders the daily dollar-ranked class-tagged queue, runs the full read-only gate chain on each spend-up, and applies an explicitly-approved raise through the Phase-8 spine unchanged; test_queue_run.py + the full suite are GREEN.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Live daily-queue dry-run + first explicit-approval spend-up (org-enable + batch approval)</name>
  <files>state/decisions.md, brain/raw/</files>
  <read_first>
    - engine/scripts/queue_run.py (the CLI being run live — from Task 2)
    - brain/raw/2026-06-16_ppc-coverage-gap.md (the funded-winner candidates + GG-0DC1 restock-gated SKU)
    - brain/raw/2026-06-20_datadoe-write-path-gating.md (the 4-gate chain + Settings->Actions org-enable precondition)
  </read_first>
  <what-built>
    The daily ranked queue is code-complete and pytest-green: queue_run.py renders a dollar-ranked,
    class-tagged table; the margin gate + cover/conversion gate + two-class attribution + internal-
    competition consolidation all refuse before any spend-up; an approved raise runs the Phase-8 apply
    spine unchanged (over-ceiling refused, not clamped). The SKILL.md daily operating-rhythm section
    points at queue_run.py + the live-read tools.
  </what-built>
  <action>
    PAUSE for human verification — this is a checkpoint, not autonomous work. The code + tests are
    already complete and green (Tasks 1-2). Here the agent runs the queue LIVE against the real account
    and Rami gives the one explicit batch approval (D-06). Steps: (1) the agent confirms the spend-up
    Ads action types are org-enabled in DataDoe Settings -> Actions for the CA profile; (2) pulls the
    live reads, runs queue_run.py render mode, and shows Rami the dollar-ranked table; (3) on Rami's
    explicit approval of ONE gate-passed funded winner, runs apply mode WITH --approve and reconciles
    it; (4) demonstrates an over-ceiling raise is refused-not-clamped. Do NOT auto-advance — wait for
    the resume signal.
  </action>
  <how-to-verify>
    1. Confirm the spend-up Ads action types are org-enabled in DataDoe Settings -> Actions for the CA
       profile (TARGETS_UPDATE bid-up, CAMPAIGNS_UPDATE budget-up, TARGETS_ADD). A live run against a
       disabled type returns a 412 action_type_disabled refusal — surfaced cleanly, never retried.
    2. Have the agent pull the live reads (get_sales_velocity / get_days_of_cover / get_fba_inventory /
       get_sales_and_traffic) + the candidate set, download them to local files, and run queue_run.py in
       render mode. Verify the table is dollar-ranked, no-$-estimate rows last, each row class-tagged
       (auto / needs-approval) with provenance, and that GG-0DC1 (1 FBA unit) renders REFUSED (sub-cover),
       NOT actionable.
    3. Pick ONE funded winner from brain/raw/2026-06-16_ppc-coverage-gap.md whose raise passes the margin
       gate + cover/conversion gate + matured window. Have the agent dryRun it (actions_start dryRun:true),
       confirm the dryRun VALIDATED, then run queue_run.py apply mode WITH `--approve` (the one deliberate
       explicit confirm, D-06) and verify it reconciles (poll COMPLETED + FIND-echo) and logs to
       state/decisions.md + brain/raw.
    4. Verify an over-ceiling raise is REFUSED (GateRefusal over_ceiling surfacing the breaching projected
       TACOS), NOT clamped — try a deliberately-too-high raise on a thin-margin SKU.
  </how-to-verify>
  <verify>
    <human-check>Rami confirms: the live render is dollar-ranked + class-tagged + provenance-cited; GG-0DC1 renders refused sub-cover; ONE gate-passed spend-up reconciled (poll COMPLETED + FIND-echo) and logged to decisions.md + brain/raw; an over-ceiling raise refused-not-clamped.</human-check>
  </verify>
  <acceptance_criteria>
    - the live render mode prints a dollar-ranked, class-tagged, provenance-cited table with no-$-estimate rows last
    - GG-0DC1 (1 FBA unit) renders REFUSED (cover_below_floor), not actionable
    - exactly ONE gate-passed funded winner is applied via --approve and reconciles (terminal COMPLETED + FIND-echo)
    - the applied write logs a dated line to state/decisions.md + a brain/raw note carrying the actionId
    - a deliberately-over-ceiling raise returns GateRefusal(over_ceiling) surfacing the breaching projected TACOS, not a clamped value
  </acceptance_criteria>
  <done>The daily queue is proven live: a dollar-ranked render, GG-0DC1 refused sub-cover, one explicitly-approved gate-passed spend-up reconciled + logged, and an over-ceiling raise refused-not-clamped.</done>
  <resume-signal>Type "approved" once the live render + one gate-passed spend-up reconciled (or describe what refused/failed and why).</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Add the daily-queue operating-rhythm section to ppc-analysis SKILL.md</name>
  <files>.claude/skills/ppc-analysis/SKILL.md</files>
  <read_first>
    - .claude/skills/ppc-analysis/SKILL.md (the file being modified — full file, esp. the "Acting (Phase 2, optional)" tail and the "NUMBERS COME FROM THE ENGINE" banner to match the voice)
    - engine/scripts/queue_run.py (the CLI the section documents — from Task 2)
    - .claude/skills/datadoe-query/SKILL.md (the live-read tool references the section cross-links)
  </read_first>
  <action>
    Add a "Daily ranked queue (the standing operating rhythm)" section to
    `.claude/skills/ppc-analysis/SKILL.md`. Document: the queue is on-open / on-demand (no cron); the
    agent pulls the live reads (get_sales_velocity / get_days_of_cover / get_fba_inventory /
    get_sales_and_traffic), downloads them to local files, and runs `cd engine && uv run python
    scripts/queue_run.py` in render mode to produce the dollar-ranked, class-tagged table (rank | SKU |
    entity | action | current -> proposed | expected $/wk | provenance | class). Standing-approved
    reversibles auto-apply (shown already-actioned); spend-ups + phrase/broad negatives wait for ONE
    explicit batch approval (`--approve`). State the gate chain every spend-up passes (margin gate ->
    cover/conversion gate -> matured-window -> internal-competition consolidation) and that an over-
    ceiling raise is REFUSED not clamped. Cross-link the engine-not-prose banner (numbers come from
    queue_run.py, not chat). Keep the existing "render and interpret the engine's typed JSON, never
    eyeball a ratio" discipline.
  </action>
  <verify>
    <automated>grep -q "queue_run" /Users/mareekhalila/Documents/anabtawi-amazon-os/.claude/skills/ppc-analysis/SKILL.md &amp;&amp; echo "SKILL section added"</automated>
  </verify>
  <acceptance_criteria>
    - ppc-analysis SKILL.md contains a "Daily ranked queue" section naming `queue_run.py`
    - the section lists the four live-read MCP tools (get_sales_velocity / get_days_of_cover / get_fba_inventory / get_sales_and_traffic)
    - the section states reversibles auto-apply and spend-ups wait for explicit `--approve`
    - the section states an over-ceiling raise is refused, not clamped
  </acceptance_criteria>
  <done>The ppc-analysis SKILL documents the daily-queue operating rhythm, the live reads, the class-based autonomy, and the refuse-not-clamp spend-up discipline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent MCP -> queue_run | live velocity/cover/conversion + dryRun/apply/poll/FIND responses cross from the agent's MCP calls into the CLI as local files |
| operator approval -> spend-up | the explicit `--approve` batch gate is the only path that fires a real spend-up write |
| queue_run -> Amazon (actions_start) | the spend-up apply is the live mutating write; the full gate chain protects it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05-01 | Elevation of Privilege | spend-up without approval | mitigate | apply mode without `--approve` refuses any needs-approval row (apply.apply not called); WRITE-04 — a spend-up needs the margin gate AND explicit approval |
| T-09-05-02 | Tampering | over-ceiling raise | mitigate | gate.evaluate inside apply.apply returns GateRefusal(over_ceiling) surfacing the breaching projected TACOS — refused, NEVER clamped (D-11 / GATE-04, gate.py:226-233) |
| T-09-05-03 | Elevation of Privilege | restock-gated / sub-conversion SKU scale-up | mitigate | cover_gate.check refuses sub-6-week-cover (GG-0DC1) and sub-12%-conversion spend-ups before the table marks them actionable (EXEC-05, Plan 02) |
| T-09-05-04 | Tampering | over-cap reversible auto-apply | mitigate | magnitude.check inside the spine refuses a >50% reversible move (cap now LIVE per Plan 01 D-01) — never clamped; over-cap routes to explicit review |
| T-09-05-05 | Spoofing | immature-window efficiency verdict | mitigate | harvest.judge_later_blocked refuses an efficiency verdict on <7d SP / <14d SB/SD (the #1 pitfall, D-10); act-now fires only on obviously-dead non-window signals |
| T-09-05-06 | Tampering | bidding against yourself | mitigate | harvest.consolidation_blocked flags a term live in >1 ENABLED campaign and blocks the bid-up until consolidated to the lowest-ACOS owner (EXEC-02, D-08) |
| T-09-05-07 | Repudiation | double-apply | mitigate | apply.apply reuses the content-hash idempotency ledger (08 D-10) — a re-run is a no-op; an in-flight actionId resumes at poll, never re-fires start |
| T-09-05-08 | Information Disclosure | logs / table / stdout | mitigate | logged_call carries op + marketplace only; the seller UUID is never passed into any log field, the table, or stdout (hard rule 5) — asserted by test + grep |
| T-09-05-SC | Tampering | npm/pip/cargo installs | accept | no package installs — reuses existing engine deps (polars/pydantic/tenacity); nothing added to uv/pip; RESEARCH Package Legitimacy Gate N/A (no new packages) |
</threat_model>

<verification>
- `cd engine && uv run pytest tests/test_harvest.py tests/test_queue_run.py -q` exits 0.
- `cd engine && uv run pytest -q` stays green across the whole suite (the spine + gate + Plans 02-04 modules all reused, none regressed).
- `cd engine && grep -nE "requests|urllib|http\\.client|socket|fetch" scripts/queue_run.py src/habibos/harvest.py` returns nothing (engine opens no socket).
- `cd engine && grep -rn "AMAZON_CA_SELLER_ID\|sellerId\|seller_id" scripts/queue_run.py` returns nothing (no secret).
- The live checkpoint (Task 3) confirms a dollar-ranked render, GG-0DC1 refused sub-cover, one gate-passed spend-up reconciled + logged, and an over-ceiling raise refused-not-clamped.
</verification>

<success_criteria>
- A spend-up executes only after the margin gate + cover/conversion gate + explicit `--approve` (over-ceiling refused, not clamped).
- The two-class attribution model blocks efficiency verdicts on immature windows; act-now fires only on obviously-dead signals.
- A contested term blocks a bid-up until consolidated to the lowest-ACOS owner.
- Standing-approved reversibles auto-apply and render already-actioned; the daily table is dollar-ranked + class-tagged + provenance-cited.
- harvest.py + queue_run.py are pytest-green; the full suite stays green; the SKILL documents the rhythm.
</success_criteria>

<output>
Create `.planning/phases/09-daily-ranked-queue-explicit-approval-spend-up/09-05-SUMMARY.md` when done.
</output>
